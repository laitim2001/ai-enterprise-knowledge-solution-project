# {PROJECT_NAME} — env 範本
# Copy 做 .env 填實際值。.env 已 gitignore,絕不 commit 真實 secret。

# === 核心服務 ===
# DATABASE_URL={connection string — 留空則 in-memory fallback}
# {SERVICE}_API_KEY={}
# {SERVICE}_ENDPOINT={}

# === Feature flags / 本機 override ===
# {FLAG}=false

# === 本機 dev 繞坑(按平台)===
# {例:某 flag 繞第三方 quota / tier 限制}
