"""W24-wave-c1 F2-F4 `/admin/*` route group per ADR-0026 Option B.

Settings 6-tab hub Connections + Identity & Auth + API Keys & Quotas tabs:

- F2 `connections.py` — 9-provider CRUD + test + rotate
- F3 `identity.py` — Entra + App registration + MSAL + role mapping + policy
- F4 `api_keys.py` + `usage_stats.py` — 4-stat strip + per-provider quotas
"""
