---
phase: W20-frontend-wave-a
plan_ref: ./plan.md
status: active
last_updated: 2026-05-16
---

# Phase W20 — Checklist

> Atomic checkbox(每 item ≤ 0.5–2 hour effort)。Status:`active` from kickoff 2026-05-16(Chris directive + 6 ADRs Accepted + AskUserQuestion A1 pick = the authorization)。
> 每 item done 後 `[ ]→[x]` + commit ref;延後項標 🚧 + reason(per CLAUDE.md §10 sacred rule — 唔可以刪未勾選 item)。
> Deliverable ↔ ADR mapping:F0=ADR Status flip + spec amend / F1=ADR-0032 absorb / F2=ADR-0030 absorb / F3=ADR-0031 Option B / F4=ADR-0028 / F5=ADR-0025 minus Access / F6=ADR-0028 §5.5.3b / F7=ADR-0014 polish / F8=cross-cutting / F9=closeout。

## F0 — Kickoff cascade(ADR-0025/0028/0031 Status flip + spec amendment — landed at kickoff)

- [x] F0.1 `docs/adr/0025-kb-detail-8-tabs.md` Status **Proposed → Accepted**(consensus pick — 7-tab `-Access` Wave A scope note added;Implementation Deliverables row — Wave A ships 7-tab,Wave C1 activates Access tab)
- [x] F0.2 `docs/adr/0028-kb-new-5-step-wizard.md` Status **Proposed → Accepted**(no-op verify — W19 F6 `6a34a41` already flipped)
- [x] F0.3 `docs/adr/0031-chat-advanced-surfaces.md` Status **Proposed → Accepted**(no-op verify — W19 F6 `6a34a41` already flipped)
- [x] F0.4 `docs/adr/README.md` — rows for 0025/0028/0031 Status「Proposed」→「Accepted」(no-op verify — W19 F6 already synced)
- [x] F0.5 `docs/architecture.md` — 6 inline-tagged §5 amendments landed kickoff `40964b6`(§5.2 Chat ADR-0031 / §5.3 Dashboard ADR-0030 absorbed / §5.4 KB List polish / §5.5 KB Detail 7-tab + Access deferral / §5.5.3 `/kb/new` 5-step + Multimodal Tier 1+2 / §5.10 Login Brand panel + Forgot password disabled affordance)
- [x] F0.6 `docs/01-planning/W20-frontend-wave-a/{plan,checklist,progress}.md` created `status: active` 2026-05-16 kickoff `40964b6`
- [x] F0.7 `docs/12-ai-assistant/01-prompts/01-session-start.md` §10 W20+ row split → W20 active row + W21+ candidates;§12 W20 milestones row + Update history row + Last-Updated `40964b6`

## F1 — `<AppShell>` topbar + sidebar polish per ADR-0032 absorbed scope(C09)

- [x] F1.1 NEW `frontend/components/nav/notifications-menu.tsx` landed `(this commit)`— `<Bell>` trigger + `<DropdownMenu>` + counter badge + `useQuery(['notifications'])` retry:false + 60s refetchInterval + MOCK_NOTIFICATIONS fallback on 404 + Mark all read button(disabled if backend absent → wrapped `<DisabledAffordance>`)+ See all → `<DisabledAffordance>`(no `/notifications` route Wave A)+ unread-dot + relative time formatter + file header docstring
- [x] F1.2 Workspace switcher disabled affordance landed `(this commit)` — `<DisabledAffordance reason="Multi-workspace support — Tier 2 per architecture.md §11" tier2Trigger="multi-tenancy">` 包住 disabled `<button>` 顯示 `Briefcase` + `Ricoh · RAPO` label + `ChevronDown`(`hidden sm:inline-flex`);W19 §2.3 leak fixed。Language toggle ALSO migrated `(this commit)` to `<DisabledAffordance reason="Multi-language (JP / ZH) — coming in a later tier" tier2Trigger="i18n machinery">`(S1 catalog item consume shared component)
- [x] F1.3 Sidebar Tools sub-section landed `(this commit)` — NAV_ITEMS → NAV_SECTIONS structure(`{title, items}[]` — Main: Dashboard/Chat/KBs + Tools: Eval/Traces)+ NEW `NavGroupHeader` sub-component(`aria-hidden="true"`,visual-only — same `<nav aria-label="Primary">` landmark preserved);W18 Vitest test 5-items / aria-current / focus-mode toggle 不變
- [x] F1.4 Sidebar Labs section **deliberately omitted**(W19 F5.4 Option C — no `/labs/*` routes shipped)— code comment 標 future Tier 2 enablement = add third NavSection behind env flag
- [x] F1.5 NEW `frontend/components/ui/disabled-affordance.tsx` landed `(this commit)` — shared component per W19 F5 §4 spec;props `variant` ∈ {`p1-strict` default, `p3-preview`} + `reason` + `tier2Trigger?` + `showBadge?` (p3 only) + `className?`;p1-strict 用 `opacity-60 pointer-events-none`,p3-preview 用 `opacity-75` + optional inline `TIER 2` badge(`bg-accent/10 text-accent border-accent/30` — rounded from spec's `bg-accent/12` per Karpathy §1.2 simplicity,Tailwind default opacity scale)
- [x] F1.6 100% tokens(`Grep '\[oklch'` across `frontend/` = **0** preserved W15→W18→W20 F1);`pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` "No ESLint warnings or errors";`pnpm test:unit` 6 files / 18 tests pass(W20 baseline post-CH-002 — no regression)
- [x] F1.7 File header docstrings on both NEW files(per CLAUDE.md §3.2);Vitest test scaffolding **deferred → F8.4** per plan F1.7 literal "(F8 carries full pass)" — F8 batches the test files(W18 F1→F8.4 precedent)

## F2 — `/dashboard` real cards per ADR-0030 absorbed scope(C09 + C07 + C02 + C06)

- [x] F2.1 Backend NEW `backend/api/routes/health.py` landed `(this commit)` — `/health` extracted from inline `server.py` + extended payload from `{status: "ok"}` to `{status: "ok"|"degraded", components: {azure_search, azure_openai, cohere, langfuse, postgres}: {status, latency_ms, detail}}`;5 ComponentStatus values(ok / not_configured / degraded / error);Pydantic v2 schemas + `app.include_router(health.router)`;mypy strict clean(same baseline as `feedback.py` — only pre-existing langfuse-stub error)。Config-state-only check Wave A scope per Karpathy §1.2 simplicity;real-I/O pings deferred Wave B+(`latency_ms` schema field preserved)
- [x] F2.2 Backend NEW `backend/tests/api/test_health_route.py` landed `(this commit)` — **7/7 pytest pass**:all-green path + 2 degraded(retrieval_engine None + embedder None)+ 3 not_configured(Cohere optional + no DATABASE_URL + Langfuse no client)+ response-schema contract;coverage ≥ 80% on new route per CLAUDE.md §3.1 H6
- [x] F2.3 Frontend `dashboard/page.tsx` rewrite landed `(this commit)` — 4-stat strip(`<StatCard>` × 4 + skeleton — KBs/Documents/Chunks/Storage MB)+ 5 cards:(b) Knowledge bases card top-5 KB list + name link + per-row doc count + empty-state + "View all →" link;(c) Recent queries Q6 Open CTA → /chat;(d) Latest evaluation no-cache CTA → /eval;(e) System health per-component dots(5 dots semantic-token colours via `statusDotClass` + `statusLabel` + `title={comp.detail}` inline tooltip;`useQuery` 60s refetchInterval);(f) Quick actions 4 buttons preserved
- [x] F2.4 Loading skeletons(`<StatCardSkeleton>` × 4 + `<Skeleton>` per card)+ error banners(KB card destructive text + health card destructive dot)+ empty states first-class(no-KBs message + Q6 CTA + no-eval-run CTA)
- [x] F2.5 Tokens 100%(`bg-success` / `bg-muted-foreground/40` / `bg-accent` / `bg-destructive` semantic only — no hardcoded oklch);`pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` "No ESLint warnings or errors";`Grep '\[oklch'` = **0**(W15→W18→W20 F1→F2 milestone preserved — 1 accidental docstring occurrence reworded before commit per W18 F1.6 precedent)
- [x] F2.6 Vitest `dashboard.test.tsx` extended W18 baseline 2 tests → **5 tests**(+3 NEW per plan F2.6 literal:4-stat strip aggregate values + 5 per-component dots + top-5 KB list);`pnpm test:unit` 6 files / **21 tests pass**(W20 baseline post-F1 18 → 21)
- [x] F2.7 File header docstrings on NEW `health.py` + rewritten `dashboard/page.tsx`(W18 F4 → W20 F2 evolution + per-component dots scope + 4-stat strip + semantic-token note per CLAUDE.md §3.2)

## F3 — `/chat` advanced surfaces per ADR-0031 Option B server-side Conversation History(C10 + C08)

- [x] F3.1 NEW backend `backend/api/schemas/conversation.py` landed `(this commit)` — 7 Pydantic v2 models(Conversation + Message + ConversationCreate + ConversationUpdate + MessageCreate + ConversationDetail + ConversationListResponse)+ tz-aware `_utcnow()` helper;mypy strict clean
- [x] F3.2 NEW backend `backend/conversations/` module landed `(this commit)` — `__init__.py` barrel + `store.py`(Protocol + InMemoryConversationStore + `make_conversation_store` factory)+ `postgres_store.py`(`PostgresConversationStore` per ADR-0023)。**Deviation from plan path**:`backend/persistence/postgres_conversations.py` → `backend/conversations/postgres_store.py`(matches existing `api.auth.users_store` shape,not the unprecedented `persistence/` namespace)。Tables `conversations` + `messages` w/ user-idx + conv-idx + CASCADE FK + idempotent `CREATE TABLE IF NOT EXISTS`(no Alembic — consistent with W17 F1)。`anyio.to_thread.run_sync` wraps sync psycopg ops。In-memory fallback when `DATABASE_URL` unset
- [x] F3.3 NEW backend `backend/api/routes/conversations.py` landed `(this commit)` — 6 endpoints all `Depends(get_current_user)`-gated;wired into `server.py`(`tags=["conversations"]`,`dependencies=_auth`);cross-user 404 isolation enforced at store layer;auto-title on first user message;factory `@lru_cache(maxsize=1)` `get_conversation_store()` dependency
- [x] F3.4 Backend pytest `backend/tests/api/test_conversations_route.py` landed `(this commit)` — **12/12 pass**;coverage ≥ 80% per CLAUDE.md §3.1 H6(create-defaults / create-with-fields / list-user-filtered-sorted / list-paginated / get-with-messages / patch-rename-clear-kb / delete-removes / auto-title-first-user-message / assistant-no-retitle / cross-user-404 / missing-404 / citations-round-trip)。`app.dependency_overrides` pattern
- [x] F3.5 Frontend NEW `frontend/lib/api/conversations.ts` + NEW `frontend/components/chat/conversation-history.tsx` landed `(this commit)` — left collapsible pane with `localStorage['ekp-chat-history-collapsed']` persistence(deviation from "AppShell focus-mode" — see progress.md F3b Deviations);`useQuery(['conversations', 'list'])` 30s staleTime + invalidate-after-mutation;"New chat" button → `POST /conversations`(lazy on first user send via `ensureConversation()`);list-item click loads via `GET /conversations/{id}` + remaps to local Message shape;auto-title server-side first-50-char slice;double-click inline rename(Enter commit / Escape cancel / blur commit)+ shadcn `<Dialog>` delete confirmation。401 graceful fallback。`ApiClient.delete<T = void>` method added for 204-No-Content endpoints
- [x] F3.6 Frontend `chat/page.tsx` rewrite landed `(this commit)` — SSE `streamQuery` logic preserved exactly;persistence layer added:user prompt POSTed via `appendMessage` before SSE starts;assistant turn POSTed with content + collected citations after `done` event。Both writes `.catch(() => {})` best-effort so a transient persistence-layer blip doesn't block the SSE render(on-page state is the active-session source of truth)
- [x] F3.7 Frontend 3 citation placement modes landed `(this commit)` — `inline` / `footnote` / `sidebar` toggle in `<ChatHeader>` fieldset(aria-pressed pills)+ persisted to `localStorage['ekp-citation-mode']`;3 render branches:`inline` = 2-col CitationCard grid(W3 preserved)、`footnote` = `<ol>` w/ `<CitationPill>` indices + summary、`sidebar` = right `<aside>` (lg-only) showing latest assistant turn's citations
- [x] F3.8 Frontend NEW `frontend/components/chat/inline-image-card.tsx` landed `(this commit)` — thin extracted button wrapping `<img>` thumbnail;CitationCard reuses it instead of inlined `<button><img>`;consumes existing W17 `ImageRef` schema(`embedded_images[0]`)
- [x] F3.9 Frontend NEW `frontend/components/chat/image-gallery.tsx` landed `(this commit)` — aggregates `citations.embedded_images[0]` across ALL conversation messages into a 3-col grid below the chat stream;click → page-level `<ScreenshotModal>`(modal now shows alt_text overlay = OCR text per `ImageRef.alt_text` from W2 F3 screenshot pipeline)
- [x] F3.10 Frontend NEW `frontend/components/chat/citation-pill.tsx` landed `(this commit)` — `[n]` pill with vanilla popover(100ms hover-grace + `onFocus`/`onBlur` keyboard a11y)showing chunk title + doc title + section path + score + "Open source document →" deep-link;deviation = vanilla popover not shadcn `<Popover>` primitive(not yet in repo;Karpathy §1.2 add when 2nd use site appears)
- [x] F3.11 Frontend NEW `frontend/components/chat/feedback-bar.tsx` landed `(this commit)` — thumbs-up one-shot write + thumbs-down inline disclosure with `<select>` tag dropdown(inaccurate / incomplete / off-topic / other)+ textarea + Send;tag prefixed into existing W8 `POST /feedback` `comment` field as `[tag] text…`(no backend schema change per Karpathy §1.2);status state machine idle → expanded → submitting → submitted / error
- [x] F3.12 Frontend NEW `frontend/components/chat/crag-strip.tsx` landed `(this commit)` — `<Sparkles>` + "CRAG triggered — N iteration(s)" chip;dormant in Tier 1 SSE path(stream is L3-only per architecture.md §3.5);wiring stays in place for Wave B+ L3 enable;`crag_reasoning` deliberately omitted per F3.13 deferral
- [x] F3.13 Backend — verified `(this commit)`: `backend/api/schemas/query.py` line 56-57 already has `crag_triggered: bool` + `crag_iterations: int`(W4 CRAG L2 baseline);**no schema change required**;`crag_reasoning` deliberately NOT added(Wave B+ candidate — F3.12 frontend renders "CRAG triggered — N iterations" without reasoning tooltip per Karpathy §1.2 simplicity)
- [x] F3.14 Tokens 100% `tokens.ts`;`Grep '\[oklch'` across `frontend/` = **0**(W15→W18→W20 F1+F2+F3b milestone preserved);`pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` "No ESLint warnings or errors"
- [x] F3.15 Vitest **W20 baseline 6 files / 21 tests preserved**(no regression);F3b component tests scaffold deferred → F8.4 batches per plan F3.15 literal `(F8.4 batches)` + F1.7 / W18 F1 precedent — `conversation-history.test.tsx` + `inline-image-card.test.tsx` + `image-gallery.test.tsx` + `citation-pill.test.tsx` + `feedback-bar.test.tsx` + `crag-strip.test.tsx` 🚧 deferred F8.4
- [x] F3.16 File header docstrings on all 7 NEW files(`lib/api/conversations.ts` + 6 `components/chat/*.tsx`)+ the rewritten `app/(app)/chat/page.tsx` per CLAUDE.md §3.2

## F4 — `/kb` list polish + `/kb/new` 5-step wizard per ADR-0028(C09 + C02 + C01)

- [x] F4.1 Backend `backend/api/schemas/kb.py` extend `KbConfig` landed `(this commit)` — 4 NEW Tier 1 fields per plan literal:`extract_embedded_images: bool = False` + `slide_screenshots: bool = True` + `dedup_strategy: Literal['sha256', 'none'] = 'sha256'` + `return_images_in_chat: bool = False`;extended class docstring documents the Wave A active vs forward-compat seam split(extract is active end-to-end;slide/dedup forward-compat until uploader plumbing;return_images is query-time read);mypy strict clean(no new errors — same baseline as W17 F1)。Frontend `lib/api/kb.ts` `KbConfig` interface + `DEFAULT_KB_CONFIG` synced
- [x] F4.2 Backend `backend/ingestion/orchestrator.py` landed `(this commit)` — `ingest()` accepts optional `kb_config: KbConfig | None = None`;when `kb_config.extract_embedded_images=False` short-circuits `ScreenshotExtractor.extract` to an empty list(uploader never called for that doc)。Backward-compat = `kb_config=None` keeps W2 baseline → existing 11 pytest cases preserved unchanged。`backend/api/routes/documents.py` `_run_ingest_pipeline` fetches `service.get(kb_id)` and passes the config(defensive try/except falls back to W2 baseline if the lookup fails — same fail-open shape as `_is_dup`)。3 forward-compat flags(slide_screenshots / dedup_strategy / return_images_in_chat)documented in orchestrator module docstring as Wave B+ wiring seams(uploader=None today per R12)。NEW pytest `test_ingest_kb_config_extract_embedded_images_false_skips_extraction` + `test_ingest_kb_config_extract_embedded_images_true_preserves_w2_path` → **13/13 orchestrator tests pass**(11 existing + 2 new)
- [x] F4.3 Frontend `frontend/app/(app)/kb/page.tsx` landed `(this commit)` — grid view preserved(W14 baseline)+ table view toggle(`<LayoutGrid>` / `<List>` button group,`aria-pressed`)persisted to `localStorage['ekp-kb-list-view']`;filter bar gained status filter dropdown(All / Indexed only / Empty only / Degraded only)alongside the existing search + sort;empty-state copy updated to mention filter clear。Table renders the same `deriveStatus` outputs as the cards(no duplicate logic)+ tabular-nums for numeric columns;`<KbTableSkeleton>` mirrors `<KbGridSkeleton>` for the loading shape
- [x] F4.4 Frontend `frontend/app/(app)/kb/new/page.tsx` rewrite landed `(this commit)` — 5-step wizard(Source / Parsing / Chunking / Multimodal / Review);Stepper indicator `aria-current="step"` on the active node + `aria-label="Wizard steps"` landmark;Step 4 Multimodal renders 4 Tier 1 toggles via NEW `<ToggleRow>` + shadcn `<Switch>`(extract_embedded_images / slide_screenshots / dedup_strategy / return_images_in_chat)AND 3 Tier 2 `<DisabledAffordance variant="p3-preview" showBadge>` chips(caption generation / image clustering / provenance ledger — sourced from W19 F5 27-affordance Tier 2 catalog rows 18-20);Step 5 Review file picker + summary `<dl>` + Stage progress indicator + POST /kb → POST /kb/{id}/documents sequence → redirect `/kb/[id]`(unchanged logic from W12);Step 2 Parsing has a `<DisabledAffordance tier2Trigger="parser profile picker">` placeholder for Wave B+ Docling profile picker
- [x] F4.5 Stepper navigation landed `(this commit)` — `next()` + `back()` helpers replace the old per-step `setStep(N)` calls;`next()` gates on the current step's validator output(`validateStep1` / `validateStep2` / `validateStep3` / `validateStep5`)— Step 4 has no validator(all toggles default-valid)and Step 5 has the file-picker validator;reuses the same `<Field>` + `<Stage>` + `<Summary>` primitives the W12 wizard had(Karpathy §1.3 — additive content,no refactor)
- [x] F4.6 Tokens 100%(`bg-success` / `bg-muted-foreground/40` / `bg-accent/15` semantic only — no hardcoded oklch);`pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` "No ESLint warnings or errors";`Grep '\[oklch'` across `frontend/` = **0**(W15→W18→W20 F1+F2+F3b+F4 milestone preserved)
- [x] F4.7 Vitest **W20 baseline 6 files / 21 tests preserved**(no regression);F4 component tests scaffold deferred → F8.4 batches per plan F4.7 literal "(F8.4 batches)" + W18 F1 / W20 F1.7 / W20 F3.15 precedent — `kb-new-wizard.test.tsx`(5-step navigation + Tier 2 disabled affordance render)+ list view toggle test 🚧 deferred F8.4
- [x] F4.8 File header docstrings on rewritten files(`schemas/kb.py` extended `KbConfig` docstring + W20 F4.1 footnote;`orchestrator.py` extended module docstring with W20 F4.2 section;`kb/page.tsx` + `kb/new/page.tsx` W20 F4.3/F4.4 history notes;`lib/api/kb.ts` KbConfig interface comments)per CLAUDE.md §3.2

## F5 — `/kb/[id]` 7-tab Detail refactor per ADR-0025 minus Access(C09 + C01 + C02 + C03)

- [x] F5.1 Backend `POST /kb/{kb_id}/archive` endpoint landed `(this commit)` — added `KbStatus.archived: bool = False` schema field + `KBStorageBackend.set_archived` Protocol method + InMemory + Postgres impl(idempotent `ALTER TABLE ADD COLUMN IF NOT EXISTS archived BOOLEAN`);`KBService.archive(kb_id, archived=True)`;route returns 200 idempotent + 404 on missing kb;`api/routes/documents.py` `_refuse_if_archived` helper guards upload + reindex paths(returns 403 `kb.archived`);read paths(GET docs/chunks/query/retrieval test)deliberately allowed so chat surface keeps citing。**5/5 pytest pass**:`test_new_kb_defaults_to_not_archived` + `test_archive_happy_path` + `test_archive_idempotent` + `test_archive_missing_kb_returns_404` + `test_service_archive_can_unarchive`
- [x] F5.2 Backend `GET /kb/{kb_id}/images` enriched landed `(this commit)` — paginated `{items, total, limit, offset}` response;walks `engine.list_documents(kb_id)` + per-doc `engine.list_chunks` to aggregate embedded_images_json + dedupes by `checksum_sha256`(image identity);`HybridSearcher.list_chunks` select clause extended additively with `embedded_images_json`(W17 F4.1 existing callers unaffected — Pydantic dropping unknown field);schemas in `api/schemas/listing.py` `KbImageItem` + `KbImagesResponse` — Tier 1 maps `{id=sha256, url=blob_url, doc_id, doc_name=doc_title, ocr_text=alt_text}` + 3 forward-compat null fields(page_num / screenshot_type / created_at);405 deferred — endpoint shape compiles with `uploader=None` reality(R12)— returns empty for Tier 1。**4/4 pytest pass**:`test_list_kb_images_empty_index_returns_empty_list` + `test_list_kb_images_missing_kb_returns_404` + `test_list_kb_images_aggregates_and_dedupes` + `test_list_kb_images_malformed_json_is_skipped`
- [x] F5.3 Backend `POST /chunking-preview` endpoint landed `(this commit)` — NEW `api/routes/chunking.py` + 3 NEW schemas in `listing.py`(`ChunkingPreviewRequest`/`Item`/`Response`);**deviation**:Wave A consumes `sample_text` only(per Karpathy §1.2 simplicity — `sample_doc_id` returns 200 + `note` Wave B+ seam since uploader=None means we can't re-fetch parsed bytes from blob storage today);`_build_synthetic_parser_result` translates raw text → `ParserResult` w/ Markdown-style heading detection(`#`/`##`/`###`)so the chunker still produces section-bounded chunks;reuses `LayoutAwareChunker(target_tokens=chunk_size)` from W2 F2;`overlap` accepted but echoed in `note` as ignored(heading-bounded chunker has no overlap window — Wave B+ seam)。Wired into `server.py` w/ auth dependency。**5/5 pytest pass**:`test_chunking_preview_happy_path` + `test_chunking_preview_heading_detection` + `test_chunking_preview_sample_doc_id_seam_returns_note` + `test_chunking_preview_empty_body_returns_422` + `test_chunking_preview_overlap_echoed_with_note`
- [x] F5.4 Frontend `frontend/app/(app)/kb/[id]/page.tsx` 7-tab refactor landed `(this commit)` — `VALID_TABS` 5→7(`documents` + `chunks` + `images` NEW + `chunking-lab` NEW + `pipeline` + `retrieval` + `settings`);`TAB_LABEL` synced;7 `<TabsTrigger>` icons(`<Image>` + `<Scissors>` NEW)+ 7 `<TabsContent>` mappings;url-driven via `?tab=` searchParam(W14 pattern preserved);Tabs 1/2/5/6/7 implementations preserved verbatim per Karpathy §1.3 surgical(NO touch to DocumentsTab/ChunksTab/PipelineTab/RetrievalTab logic)
- [x] F5.5 Frontend Tab 3 Images landed `(this commit)` — NEW `<ImagesTab>` component(inline in `kb/[id]/page.tsx`)consumes `kbApi.listImages(kb.kb_id, 200, 0)`;3-col grid(4-col md+)of thumbnails with `doc_name` + `ocr_text` two-line preview;click → shadcn `<Dialog>` modal showing full image + OCR text overlay box;empty-state surfaces R12 context("Images surface after the screenshot pipeline runs end-to-end…")
- [x] F5.6 Frontend Tab 4 Chunking Lab landed `(this commit)` — NEW `<ChunkingLabTab>` component(inline in `kb/[id]/page.tsx`);textarea sample text + Strategy select + chunk_size input + "Preview" button → `kbApi.chunkingPreview(...)`;result chunks render as expandable `<details>` blocks with section path + chunk_text in a `<pre>`;"Apply" button wrapped in `<DisabledAffordance variant="p3-preview" showBadge>` Tier 2 chip(per plan F5.6 literal "re-chunking pending");`note` from response surfaces in `<CardDescription>` when set(sample-doc-id seam or overlap-ignored)
- [x] F5.7 Frontend Tab 7 Settings Danger zone landed `(this commit)` — NEW `<ArchiveAction>` row added to existing `<DangerZone>`(now takes `kb: KbStatus` not `kbId: string`);`useMutation(kbApi.archive)` real backend wire;confirmation `<Dialog>` with KB-id call-out + cancel + "Archive KB" CTA;success → invalidates `['kb', kb_id]` + `['kb', 'list']` + sonner toast + closes modal;`kb.archived === true` flips button to "Archived" disabled + shows "Already archived" `<Badge>`;archive row uses `<Archive>` icon and shows a info-strip(not destructive `text-destructive`)since archive is reversible
- [x] F5.8 Access tab disabled affordance landed `(this commit)` — 8th `<TabsTrigger value="access" disabled aria-disabled="true">` rendered OUTSIDE `VALID_TABS` array(so `?tab=access` can't route to it);wrapped in `<DisabledAffordance variant="p1-strict" reason="RBAC pending Wave C1 per ADR-0027 Option A backend" tier2Trigger="RBAC + audit log + group membership">`;`<Lock>` icon matches Tier 2 catalog row 4。Tab is visible but click is a no-op(Radix `<TabsTrigger disabled>` short-circuits)
- [x] F5.9 Tokens 100%(no hardcoded oklch);`pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` "No ESLint warnings or errors";`Grep '\[oklch'` across `frontend/` = **0**(W15→W18→W20 F1+F2+F3b+F4+F5 milestone preserved);existing 32 docs route tests + 13 orchestrator tests + 5 archive tests + 4 images tests + 5 chunking-preview tests = **59/59 backend pass**;Frontend Vitest **6 files / 21 tests pass**(W20 baseline preserved — no regression from F5 surface)
- [x] F5.10 Vitest **W20 baseline 6 files / 21 tests preserved**;F5 component tests scaffold deferred → F8.4 batches per plan F5.10 literal "(F8.4 batches)" + F1.7 / W20 F3.15 / W20 F4.7 precedent — `kb-detail-tabs.test.tsx`(7-tab nav + Access disabled assertion + Images grid render + Chunking Lab preview render)🚧 deferred F8.4
- [x] F5.11 File header docstrings on rewritten files(`api/routes/chunking.py` 17-line module docstring;`api/schemas/listing.py` W20 F5.2 + F5.3 section comments + per-class docstrings explaining Tier 1 reality;`api/routes/documents.py` `_refuse_if_archived` + `list_kb_images` docstrings;`kb_management/storage.py` + `postgres_backend.py` `set_archived` docstrings;`kb_management/service.py` `archive` docstring;frontend `kb/[id]/page.tsx` per-tab section comments)per CLAUDE.md §3.2

## F6 — `/kb-upload/[id]` re-ingestion wizard polish per ADR-0028 §5.5.3b(C09)

- [x] F6.1 Frontend `frontend/app/(app)/kb/[id]/upload/page.tsx` rewrite landed `(this commit)` — **single-step → 3-step wizard skeleton**(plan literal「existing 3-step skeleton」實際係 single-step file picker per F6 deviation entry plan §7;rebuilt from scratch per AskUserQuestion Option 1 picked 2026-05-17):**Step 1 Source**(file picker .docx/.pdf/.pptx,reuse F4 Step 5 file-picker shape)+ **Step 2 Multimodal**(read-only display of KB existing config via `kbApi.get(kbId)` — 4 Tier 1 toggles all `disabled` + `aria-readonly` + dedup_strategy 用 mono badge 而唔係 select、3 Tier 2 disabled affordances 用 `<DisabledAffordance variant="p3-preview" showBadge>` reuse F4 Step 4 / W20 F1.5、"Edit settings" link 去 `/kb/[id]?tab=settings`)+ **Step 3 Review**(summary table 11 rows + Stage 1-stage progress for `POST /kb/{id}/documents` only + redirect `/kb/[id]` on success)
- [x] F6.2 DRY — F4 wizard `<Field>` / `<Stepper>` / `<ToggleRow>` / `<Stage>` / `<Summary>` 係 file-local primitives(冇 export),inline-redeclared per W13 register strategy `(this commit)`;**rule-of-3 promotion trigger NOW hit**(4th wizard usage observed:F4 KB Pipeline + W13 Register + W18 F5 Pipeline + W20 F6 Re-ingestion)— extract to shared `frontend/components/ui/stepper.tsx` (+ Field/Stage)為 **Wave B+ candidate**(避免 Wave A ripple change to F4 + Register per Karpathy §1.3 surgical)
- [x] F6.3 Tokens 100% `tokens.ts`;`Grep '\[oklch'` across `frontend/` = **0**(W15→W18→W20 F1+F2+F3b+F4+F5+F6 milestone preserved);`pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` "No ESLint warnings or errors"
- [x] F6.4 Vitest test scaffold deferred → F8.4 batches per plan F6.4 literal + F1.7/F3.15/F4.7/F5.10 precedent — `kb-upload-wizard.test.tsx`(3-step navigation + read-only Multimodal display + Stage progress)🚧 deferred F8.4
- [x] F6.5 File header docstring updated `(this commit)` — 22-line docstring 解釋 W12 baseline single-step → W20 F6 3-step wizard promotion + per-KB(not per-doc)Multimodal config rationale + rule-of-3 Wave B+ promotion note + tokens 100% preserved milestone per CLAUDE.md §3.2

## F7 — `/login` + `/register` polish per ADR-0014(C11 + C09)

- [x] F7.1 `frontend/app/login/page.tsx` rewrite landed `(this commit)` — **strict design fidelity refactor per `references/design-mockups/ekp-page-auth.jsx` mockup**(AskUserQuestion Option 2 picked 2026-05-17 — CLAUDE.md §3.2.1 design fidelity rule):**SSO primary**(`Button size="lg"` 自製 4-quadrant Microsoft SVG icon,top of form)+ `<DividerWithLabel label="OR continue with email">`(NEW local component using shadcn `<Separator>` flanking the label)+ email/password form secondary + **Forgot password inline next to Password label**(`<div className="flex items-center">` wrapping the Label + the affordance — Label `flex-1` so the affordance hugs right);Forgot password uses shared `<DisabledAffordance variant="p3-preview" reason="Password recovery — coming in a later tier (post-Beta)" tier2Trigger="Tier 2 — per ADR-0014" showBadge>` (TIER 2 chip + tooltip + aria-disabled);Sign-in submit `Button size="lg"` w/ `className="bg-accent text-accent-foreground hover:bg-accent/90"` (mockup coral accent — shadcn no `variant="accent"` exists so className overrides default primary per Karpathy §1.2 no-new-variant-for-single-use);Brand panel preserved(`<BrandPanel>` from W13 — H2 token discipline already locked);**bottom mono dashed "Auth modes (Tier 1)" `<aside>` block** surfaces hybrid-auth contract for operator awareness(3 bullets:Hybrid Entra ID + httpOnly cookie + Mock-auth default)。Mock-auth default dev reality unchanged(SSO button still calls `useAuthStore.signIn` — mock_msal in dev / real MSAL Beta+)
- [x] F7.2 `frontend/app/register/page.tsx` polish landed `(this commit)` — **visual polish per mockup,backend contract preserved**(AskUserQuestion Option 2 picked 2026-05-17;mockup 2-step email-link vs backend 3-step 6-digit code conflict resolved by §4 authority ordering — backend wins):**Step 1 field order** Email/Password/Confirm/Display name → **Full name → Email → Password → Confirm password**(swap matches mockup);**Email + Password inline hint copy**(`<p className="mt-1 text-xs text-muted-foreground">` under each Input):email「We'll send a 6-digit verification code · Beta cohort restricted to `@ricoh.com`」/ password「Scrypt-hashed via ADR-0022 · 8+ chars, 1 uppercase, 1 digit or symbol」(hint matches **current validation**, not mockup's `≥ 12 chars` — keeping backend contract per Karpathy §1.3 surgical);**NEW Terms of Use + Privacy Policy checkbox**(`info.acceptedTerms: boolean` state + `<input type="checkbox">` w/ inline `<a>` links + `aria-describedby` + `errors.acceptedTerms` validation required to submit Step 1);**Step 3 KB selector migrated to shared `<DisabledAffordance variant="p3-preview" reason="Multi-KB selector — Tier 1 ships with a single shared KB per Q7 default" tier2Trigger="multi-KB / multi-workspace" showBadge>`**(was inline `cursor-not-allowed` with title tooltip);Stepper / Step 2 6-digit code input / Step 3 Welcome CTA → /dashboard / W17 F2 ADR-0022 backend POST contract / state machine — all **preserved verbatim**(no functional change per AskUserQuestion Option 2 picked 2026-05-17)
- [x] F7.3 Tokens 100% `tokens.ts`;`Grep '\[oklch'` across `frontend/` = **0**(W15→W18→W20 F1+F2+F3b+F4+F5+F6+F7 milestone preserved through 7 deliverables);`pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` "No ESLint warnings or errors"
- [x] F7.4 Vitest **W20 baseline 6 files / 21 tests preserved** post-F7(no regression from login + register surface changes);F7 component test scaffold deferred → F8.4 batches per plan F7.4 literal + F1.7/F3.15/F4.7/F5.10/F6.4 precedent — `login.test.tsx`(SSO primary order + Forgot password DisabledAffordance render + Auth modes block present)+ `register.test.tsx`(field-order assertion + Terms checkbox required-to-submit + Step 3 KB selector DisabledAffordance render)🚧 deferred F8.4
- [x] F7.5 File header docstrings updated `(this commit)` — `login/page.tsx` 33-line header documents W20 F7.1 strict-fidelity realign rationale + W17 F2 cookie/CSRF transport preservation + W18 F7 dashboard redirect preservation;`register/page.tsx` 36-line header documents W20 F7.2 visual polish surfaces + W18 F7 dashboard preservation + W20 F6 rule-of-3 wizard-promote note per CLAUDE.md §3.2

## F8 — Cross-cutting:responsive + a11y + dark-mode + tests + COMPONENT_CATALOG + PAGE_INVENTORY

- [x] F8.1 Responsive spot-check on new W20 surfaces landed `(commit 1)` — `<NotificationsMenu>`(F1 — `hidden sm:inline-flex` chips + Radix `<DropdownMenu>` responsive-aware)+ `<DisabledAffordance>` chips(F1.5 — inline-flex baseline,wraps naturally)+ `/dashboard` 4-stat strip(F2 — `grid grid-cols-2 md:grid-cols-4` responsive)+ `/chat` Conversation History sidebar(F3b — `hidden lg:block` collapsible pane + `localStorage` persisted state)+ `/kb/new` 5-step wizard(F4.4 — `flex-1` step layout + responsive form fields)+ `/kb/[id]` 7-tab nav(F5.4 — Radix `<TabsList>` overflow handling)+ `/kb/[id]/upload` 3-step(F6 — same `max-w-3xl` pattern)+ `/login`(F7.1 — `flex-col md:flex-row` split)+ `/register`(F7.2 — same split)。Multi-viewport interactive browser smoke walkthrough = user pre-Beta(R8 caveat shape per W18 — same "smoke-user-deferred" carve-out continues)
- [x] F8.2 a11y spot-check on new W20 surfaces landed `(commit 1)` — full screen-reader audit Tier 2 / CO_W15_F3_aria_full_audit defer。Spot-check verifies:F6 upload page 4 aria/role usages(aria-label / aria-current="step" / aria-readonly / aria-hidden on Microsoft SVG)+ F7 login 2 aria/role(aria-hidden on Microsoft SVG + aria-label on Auth modes aside)+ F7 register 2 aria/role(aria-describedby on Terms checkbox + Stepper aria-current="step")+ F1 `<NotificationsMenu>` Radix DropdownMenu role/aria-haspopup inherited + F1.5 `<DisabledAffordance>` aria-disabled+title+aria-label all set + F3b Conversation History `<nav aria-label="Primary">` landmark preserved + F4.4 wizard step `aria-label="Wizard steps"` landmark + step indicator `aria-current="step"` + F5 Radix `<TabsList>`/`<TabsTrigger>` role inherited + Access tab `disabled` + `aria-disabled="true"`
- [x] F8.3 Dark-mode `[oklch`=0 re-check landed `(commit 1)` — `Grep '\[oklch'` across `frontend/` = **0**(W15→W18→W20 F1+F2+F3b+F4+F5+F6+F7 milestone preserved through 7 deliverables + F8.1-F8.3 verify);next-themes mechanism unchanged;dark-mode interactive 9-view walkthrough = user pre-Beta smoke per CO_W15_F3_dark_mode_visual_verify (W17 partial close — mechanism + grep verified)
- [x] F8.4 Vitest expansion landed `(commit 2 of 3)` — **W20 baseline 6 files/21 tests → 14 files/37 tests**(8 NEW test files batch);37 tests pass(plan target「40+」minor undershoot but per W18 F8.4 precedent acceptable — each NEW file has 1-3 tests covering render-smoke + key a11y attribute + interaction:**notifications-menu** 2 tests(Bell trigger + DropdownMenu fetched items)+ **disabled-affordance** 3 tests(p1-strict aria-disabled+opacity+pointer-events + p3-preview TIER 2 badge + showBadge omit branch)+ **conversation-history** 2 tests(header + New chat button + fetched list)+ **kb-new-wizard** 2 tests(5-step Stepper + Multimodal Step 4 Tier 1 + 3 TIER 2 badge)+ **kb-detail-tabs** 1 test(7 active + 1 Access disabled via `aria-disabled`)+ **kb-upload-wizard** 2 tests(3-step Stepper + Step 2 Multimodal read-only + Edit settings link)+ **login** 1 test(SSO primary + Divider + Forgot password TIER 2 + Auth modes aside)+ **register** 3 tests(Stepper + field order + Hint copy + Terms checkbox required-to-submit gating)。Stepper-vs-Step-heading getByText clash fixed via `within(screen.getByLabelText('Wizard steps'))` scope on F4/F6 tests。`pnpm test:unit` 14/14 files / 37/37 tests pass
- [x] F8.5 Playwright E2E updates landed `(commit 3 of 3)` — 3 specs updated:**`app-shell-path.spec.ts`** +2 NEW test cases(「AppShell topbar shows NotificationsMenu + workspace switcher disabled affordance (W20 F1)」asserts `aria-label="notifications"` Bell trigger + `aria-label` carries the affordance reason on Workspace switcher; 「/kb/[id] 7-tab refactor renders Access disabled affordance OUTSIDE VALID_TABS (W20 F5)」asserts 8 `role="tab"` total + Access tab `aria-disabled="true"`)· **`golden-path.spec.ts`** +3 NEW test cases(「V1 Chat page renders the Conversation History pane + advanced surfaces (W20 F3b)」asserts `<heading "Conversations">` + `<button "New chat">` + 3 citation-mode pills first visible; 「V8 Login renders W20 F7.1 strict-fidelity surfaces」asserts SSO primary button + Divider + Auth modes aside; 「V9 Register renders W20 F7.2 polish」asserts Full name first field + Work email label + Hint copy + Terms checkbox)· **`visual-baseline.spec.ts`** +2 NEW snapshot cases(`/kb/new` Step 1 Source + `/chat` Conversation History pane — both via `toHaveScreenshot()` so first user run via `PW_CHANNEL=chrome pnpm test:e2e:update-snapshots` captures the baseline file under `tests/e2e/visual-baseline.spec.ts-snapshots/`)。`pnpm exec tsc --noEmit` exit 0(specs compile-check pass);interactive run via `PW_CHANNEL=chrome pnpm test:e2e`(ADR-0017 Plan B — system Chrome,not bundled R8-blocked Chromium) = user pre-Beta smoke per W18 CO_W15_F4_interactive_flow_E2E precedent;chat persistence end-to-end test(create conv → reload → still shown)deferred Wave B+(needs reliable backend conversations API + user/session bootstrap in mock-auth-dev mode — out of Wave A budget)
- [x] F8.6 `docs/02-architecture/COMPONENT_CATALOG.md` Status rows appended landed `(commit 1)` — 8 components C01/C02/C03/C05/C07/C08/C09/C10 status notes appended with W20 Wave A amendment per ADR-0025/0028/0031 + ADR-0030/0032 absorbed scope:**C01** orchestrator `kb_config` branch(F4.2);**C02** `KbStatus.archived` + idempotent Postgres ALTER TABLE + KbConfig +4 multimodal fields(F5.1 + F4.1);**C03** `list_chunks` `embedded_images_json` extension(F5.2);**C05** CragStrip frontend wire on existing crag_triggered+iterations schema(F3b,no backend change);**C07** `/health` extracted/extended per-component dots(F2.1);**C08** +10 NEW endpoints(6 conversations + archive + images + chunking-preview + health = 18→28 total)+ `_refuse_if_archived` guard + 59/59 backend pytests pass(F3a + F5.1 + F5.2 + F5.3 + F2.1);**C09** F1-F7 full Wave A surface refactor + rule-of-3 wizard primitive promotion trigger note;**C10** F3b advanced surfaces detail
- [x] F8.7 `references/design-mockups/PAGE_INVENTORY.md` Cn mapping flip landed `(commit 1)` — 8 W20-implemented routes status flipped「Wireable today」→「Implemented W20 F#」:**#1** `/dashboard` (W20 F2) · **#2** `/chat` (W20 F3b) · **#3** `/kb` (W20 F4.3) · **#4** `/kb/new` (W20 F4.4) · **#5** `/kb/[id]` 7-tab `-Access` (W20 F5,Access disabled Wave C1) · **#7** `/kb-upload/[id]` actual `/kb/[id]/upload` (W20 F6) · **#13** `/login` strict-fidelity refactor (W20 F7.1) · **#14** `/register` polish (W20 F7.2) + topbar dropdowns row (W20 F1 NotificationsMenu)。Wave B candidates(#6 Doc Detail / #8 Eval / #9 Traces list / #10 Traces detail)+ Wave C candidates(#11 Settings / #12 Users)deferral notes added per ADR-0026/0027/0029/0030 governance

## F9 — Phase closeout + W21+ rolling-JIT trigger

- [ ] F9.1 W20 phase Gate verdict landed(PASS / PARTIAL PASS / FAIL with explicit rationale per W18 pattern)
- [ ] F9.2 W20 `progress.md` Retro — 7 sections written(What worked / What didn't & friction / Surprises / Decisions / Carry-overs to W21+ / Time tracking / Spec-ref alignment)
- [ ] F9.3 ADR-0025 + ADR-0028 + ADR-0031 status verified `Accepted`(F0 flip;verify-no-op at closeout)+ Implementation Deliverables checkboxes ticked(7-tab `-Access` shipped Wave A per ADR-0025;5-step wizard per ADR-0028;Option B server-side Conversation History per ADR-0031)
- [ ] F9.4 W20 `plan.md` + `checklist.md` + `progress.md` frontmatter `status: active` → `closed`
- [ ] F9.5 W21+ phase folder **NOT pre-created**(rolling-JIT per CLAUDE.md §10 R1)— kickoff candidates noted in `progress.md` Day-N retro section("W21+ — NOT pre-created":W21-frontend-wave-b per W19 F4 §2;OR W22-frontend-wave-c1 per F4 §3.6 split decision;OR W16 F1-F4 if Track A IT cred lands)
- [ ] F9.6 `session-start.md` hygiene catch-up — §3 C01/C02/C03/C05/C07/C08/C09/C10 status(W20 Wave A amendments)+ §10 W20 row(closed, Gate verdict)+ W21+ not-pre-created note + §11 carry-overs(W20-closed items + any new 🚧 deferrals)+ §12 milestones row + Last-Updated + Update-history;`COMPONENT_CATALOG.md` + `PAGE_INVENTORY.md` already touched in F8.6 + F8.7
- [ ] F9.7 New OQ surfaced(if any) → sync `decision-form.md` per R4;ADR-0030 + ADR-0032 SKIPPED footnote preserved per W19 F6 closeout

---

## Cross-Cutting(must verify each commit + at closeout)

- [ ] CC1 Each commit references `progress.md` Day-N entry(R2)
- [ ] CC2 Component tag in commit message — F1 = C09 / F2 = C09+C07+C02+C06 / F3 = C10+C08+C02 / F4 = C09+C02+C01 / F5 = C09+C01+C02+C03 / F6 = C09 / F7 = C11+C09 / F8 = cross-cutting / F9 = governance
- [ ] CC3 OQ status sync to `decision-form.md`(R4)— **no-op expected**:Q6 / Q15 / Q11 all stay Open(W20 doesn't resolve any);ADR-0017 occurrences if any new R8 hit → log
- [ ] CC4 Risk register — no new W20 risk expected;the `npx playwright install chromium` block stays under ADR-0017 Plan B realised(use `PW_CHANNEL=chrome`)
- [ ] CC5 CLAUDE.md §5.1 H1 check — Wave A authorized by ADR-0025/0028/0031 Accepted(W19 F6);no other architectural change;F1-F8 implement the amended §5.2 + §5.3 + §5.4-§5.5 + §5.5.5 + §5.10-§5.11 — additive content + 7-tab refactor + 5-step wizard + server-side Conversation History;NO Tier 2 leak(Access tab disabled affordance / Workspace switcher disabled affordance / Multimodal Tier 2 disabled affordances / Labs hidden / Forgot password disabled affordance)
- [ ] CC6 CLAUDE.md §5.2 H2 check — **no new dependency**(per ADR-0031 Option B uses existing `psycopg>=3.2` per ADR-0023;no Key Vault SDK / no Entra Graph SDK — those are Wave C);shadcn/ui + `tokens.ts` unchanged
- [ ] CC7 CLAUDE.md §3.2 conventions — `tsc --noEmit` exit 0 + `next lint` "No ESLint warnings or errors" on all F1-F8 changes;no `any`;shadcn/ui only;design tokens via `tokens.ts`(`[oklch`=0);App Router only;Server Components default;Client Components have `"use client"` + docstring
- [ ] CC8 CLAUDE.md §3.1 conventions — Python 3.12+ + mypy --strict clean + async by default + Pydantic v2 + structlog JSON + absolute imports;function length soft cap 50 lines;pytest + pytest-asyncio;coverage ≥ 80% on F3 `/conversations` + F5.1/F5.2/F5.3 new endpoints
- [ ] CC9 CLAUDE.md §5.5 H5 — no secret committed;no hardcoded tenant/subscription/resource;`/conversations` user_id from `get_current_user` (no PII;no plaintext-prompt logging changed)
- [ ] CC10 CLAUDE.md §5.4 H4 — Tier 2 boundary held:Workspace switcher disabled / Labs hidden / Access tab disabled affordance / Multimodal Tier 2 affordances disabled / Forgot password disabled / no GraphRAG / multi-agent / multi-tenancy / multi-modal-search / auto-sync / fine-tune leak
- [ ] CC11 Karpathy §1.3 surgical — preserve Tab 1/2/5/6/7 unchanged in F5(`git mv` if needed);additive content only(NEW Tab 3 Images + NEW Tab 4 Chunking Lab);no "順手" refactor of adjacent code

---

**Lifecycle reminder**:呢份 checklist 衍生自 `plan.md` deliverables。新加 deliverable 必須先入 plan + §7 changelog,然後再加 checklist item。延後 item 標 🚧 + reason,**唔可以刪**未勾選 `[ ]`。
