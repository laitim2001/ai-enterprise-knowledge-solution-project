---
phase: W17-beta-hardening
plan_ref: ./plan.md
status: closed
last_updated: 2026-05-10
---

# Phase W17 — Checklist

> Atomic checkbox(每 item ≤ 0.5–2 hour effort)。Status:`active` 2026-05-10 → `closed` 2026-05-10(F7 closeout — W17 phase Gate = PASS;🚧 F1.5b + F3.5b R8/Azure-key-bound runtime verifications deferred W18+/CO17)。
> 每 item done 後 `[ ]→[x]` + commit ref;延後項標 🚧 + reason(per CLAUDE.md §10 sacred rule — 唔可以刪未勾選 item)。

## F0 — ADRs(H1/H2 gate — must land before F1/F2 implementation)

- [x] F0.1 NEW `docs/adr/0022-auth-transport-hardening.md`(httpOnly cookie + CSRF double-submit + `/auth/refresh` rotation;dual-path `get_current_user`;amends ADR-0014 transport layer)— Status `Accepted` — `6edd9ef`
- [x] F0.2 NEW `docs/adr/0023-kb-manager-persistent-backing.md`(Postgres via `psycopg`;reuse docker-compose postgres + dedicated `ekp` DB;`PostgresKBBackend` satisfies `KBStorageBackend` Protocol;in-memory fallback when `DATABASE_URL` empty)— Status `Accepted` — `6edd9ef`
- [x] F0.3 `docs/adr/README.md` index — add ADR-0022 + ADR-0023 rows;「Next NNNN」→ `0024` — `6edd9ef`

## F1 — Postgres persistent backing for KB Manager + users_repo(per ADR-0023)

> **F1 verdict = PARTIAL**(2026-05-10)— all code landed(KB backend `2453a50` D2;users-repo path + doc notes `5c5df92` D3),but `pip install psycopg[binary]` is **R8 corp-proxy-blocked**(5th cumulative R8 occurrence → **ADR-0017 landed `fb0253a`**;vendor-decision pivot point — **decision = Option A**:keep psycopg, defer local Postgres-path *verification* to W18+ / CO17). Code is shippable regardless(dep declared,lazy-imported,in-memory path unaffected;CI 595 passed / 11 skipped — Postgres-path tests skip cleanly). **CO18 → CLOSED**(persistent backing implemented;in-memory remains the no-`DATABASE_URL` fallback). Only the literal `pip install` + manual `docker compose up` smoke remains(🚧 F1.5b).

## F0b — ADR-0017(R8 mitigation pattern — formalized at the 5th occurrence + vendor-decision pivot trigger)

- [x] F0b.1 NEW `docs/adr/0017-r8-corp-proxy-mitigation-pattern.md`(dependency-add discipline:stdlib > managed-REST > lazy-imported optional dep + graceful fallback;declare-but-defer-install for binary-heavy assets;stop-and-ask when unavoidable)— Status `Accepted` — `fb0253a`
- [x] F0b.2 `docs/adr/README.md` — 0017 row added;ADR-0017 reservation cleared(only 0013 AF3 remains);next NNNN → 0024 — `fb0253a`

- [x] F1.1 `backend/pyproject.toml` — added `psycopg[binary]>=3.2` to deps(H2 — ADR-0023 covers);no `uv.lock` in repo so `pyproject.toml` declaration suffices;**local `pip install` R8-blocked** — `2453a50`
- [x] F1.2 `infrastructure/docker-compose.yml` — postgres service exposes `:5432` + mounts `./postgres-init/01-create-ekp-db.sql`(creates the dedicated `ekp` DB on a fresh volume;existing volume → `docker compose exec postgres createdb -U langfuse ekp`);`.env.example` + docker-compose comments updated(`docs/setup.md §4.2` table update folded into F1.8 / closeout)— `2453a50`
- [x] F1.3 `backend/storage/settings.py` — added `database_url: str = ""`;`.env.example`「Persistent storage」section with commented `DATABASE_URL=postgresql://langfuse:langfuse_local_dev_only@localhost:5432/ekp`;empty → in-memory fallback documented — `2453a50`
- [x] F1.4 NEW `backend/kb_management/postgres_backend.py` — `PostgresKBBackend` satisfying `KBStorageBackend` Protocol(create / list_all / get / delete / update_config / update_metadata);**connection-per-op** via psycopg async(deviation from「pool」— plan §7 changelog D2);`CREATE TABLE IF NOT EXISTS knowledge_bases (...)` on connect;raises `KBNotFoundError` / `KBAlreadyExistsError`;`KbStatus` ⇄ row mapping(JSONB for `config` + `failed_documents` via `model_dump(mode="json")`)— `2453a50`
- [x] F1.5 `backend/api/auth/users_repo.py` Postgres-backed path — done. `UsersStore` Protocol split(NEW `backend/api/auth/users_store.py` = `UserRecord`/`SessionRecord` models + `UsersStore` Protocol + `InMemoryUsersStore` + `make_users_store(settings)` factory;NEW `backend/api/auth/postgres_users_store.py` = `PostgresUsersStore` — **sync** `psycopg` connection-per-op, because `users_repo`'s public surface is sync(consumed by the sync `get_current_user` dependency)— differs from the async `PostgresKBBackend`;`CREATE TABLE IF NOT EXISTS users (... email UNIQUE ...)` + `sessions (... user_oid FK→users ON DELETE CASCADE ...)` on connect);`users_repo.py` → thin facade keeping the same 9 public functions + business logic(oid/code/scrypt-hash gen,expiry,`AuthenticatedUser` projection)+ re-exports `UserRecord`/`SessionRecord`/`SELF_REGISTER_TID`;`make_users_store` selects Postgres when `database_url` set,else in-memory;`psycopg` lazily imported(unset `DATABASE_URL` never touches it — R8-safe,same shape as the KB backend);`reset_repo()` preserved → `_store.reset()`(in-memory clear / Postgres `TRUNCATE`);4 test-only `users_repo._users/_sessions` direct writes in `test_auth_self_register.py` retargeted to `users_repo._store._users/._sessions` — `5c5df92`
- [ ] 🚧 F1.5b Postgres `users_repo` path local verification(CRUD + session)— **deferred W18+ / CO17**(R8 — `pip install psycopg[binary]` not installable under the proxy;same caveat as F1.4's CRUD tests). Test scaffold IS committed:NEW `backend/tests/test_users_store_factory.py`(in-memory selection always runs ✅;Postgres selection `importorskip("psycopg")`)+ NEW `backend/tests/test_auth_users_postgres_store.py`(`importorskip("psycopg")` module-level + `skipif(not DATABASE_URL)` — 8 CRUD/session/FK-cascade cases;skips cleanly in CI;manual `docker compose up` + `DATABASE_URL=...` smoke instructions in the docstring)— `5c5df92`. Also defers:`mypy postgres_users_store.py`(`-p api.auth` shows the 2 psycopg `import-not-found` errors — documented R8 PARTIAL-PASS per ADR-0017)
- [x] F1.6 Dependency wiring — NEW `backend/kb_management/factory.py` `make_kb_backend(settings)`(mirrors `make_reranker`)— Postgres when `database_url` set, else in-memory;`PostgresKBBackend` lazily imported(unset `DATABASE_URL` never touches psycopg — same graceful shape as ACS lazy import + keeps in-memory working under R8);`get_kb_service()` wired through it;`kb_management/__init__.py` exports `make_kb_backend`;no route / service call-site change(Protocol holds;dependency-override still works for tests)— **connection-per-op so no lifespan pool wiring needed**(deviation from plan §F1.6)— `2453a50`
- [x] F1.7 Tests — NEW `backend/tests/test_kb_factory.py`(in-memory selection — always runs;Postgres-selection `importorskip("psycopg")`)+ NEW `backend/tests/test_kb_postgres_backend.py`(full CRUD — `importorskip("psycopg")` + `skipif(not DATABASE_URL)` → skips otherwise per Tier 1 PARTIAL PASS;manual-smoke instructions in the docstring);existing `test_kb*` in-memory tests unchanged + pass;**backend pytest 594 passed / 9 skipped**(was 593/7)— `2453a50`
- [x] F1.8 `docs/architecture.md` §3.4(NEW「KB metadata 持久化」note — per ADR-0023,inline-tagged amendment;doc version not bumped — same convention as §3.7's ADR-0014 tag)+ `docs/02-architecture/COMPONENT_CATALOG.md` C02(table row + Tech / Phase plan / Risks / Status / Interface lines → Postgres-backed via `make_kb_backend`,in-memory fallback,R8 verification-deferral note)+ `docs/setup.md §4.2`(postgres service purpose row + NEW `ekp` DB note:`createdb` one-liner + `DATABASE_URL` env var + in-memory fallback)— **CO18 → CLOSED**;C11 catalog entry left as-is(stale「Beta+ scope」predates W7-W15 hybrid auth — separate housekeeping,not in F1.8 scope)— `5c5df92`

## F2 — Auth-transport hardening: httpOnly cookie + CSRF + /auth/refresh(per ADR-0022)

> **F2 verdict = PASS**(2026-05-10 D4)— landed `7cca23e`. **CO_F5_refresh + CO_F5_cookie → CLOSED**. One scope note:`/auth/verify-email`'s verified-transition now also sets the cookie + auto-logs-in(per ADR-0022 §1「the email-verify step ... set ekp_session」)→ `VerifyEmailResponse` gained `access_token`/`expires_in`(None on the idempotent already-verified branch);this fixes the prior register-flow gap(Step-3 → `/chat` was hitting `/chat` unauthenticated). `register/page.tsx` needed no edit(never wrote localStorage). `server.py` CORS gained `allow_credentials=True`(cookie-transport correctness — same local-dev-gap category as the `84d030e` CORS add,not H1).

- [x] F2.1 `backend/api/routes/auth.py` — `/auth/login` + `/auth/verify-email`(verified-transition)success → set `ekp_session`(httpOnly,SameSite=Lax,Secure when `environment != "local"`,Path=/,Max-Age=session TTL)+ `ekp_csrf`(readable double-submit)via NEW `backend/api/auth/cookies.py` `set_session_cookies()`;token still in the JSON body for API/CLI(Bearer transport parity)— `7cca23e`
- [x] F2.2 `POST /auth/refresh` — rotates the session token + both cookies(revokes the old token);401 via the `Depends(get_current_user)` gate if no valid session(no unauthenticated bootstrap);mock dev mode keeps returning the fixed dev-token(no cookie);real MSAL still 503 — **closes CO_F5_refresh** — `7cca23e`
- [x] F2.3 `POST /auth/logout` — revokes the session token(cookie token + any bearer)+ `clear_session_cookies()`(`ekp_session` + `ekp_csrf` Max-Age=0)— **closes CO_F5_cookie** — `7cca23e`
- [x] F2.4 `backend/api/auth/dependency.py` `get_current_user` — dual-path:`ekp_session` cookie(precedence)→ bearer session → mock → MSAL;a present-but-invalid cookie falls through to the bearer paths;CSRF double-submit(`X-CSRF-Token` header == `ekp_csrf` cookie,constant-time)enforced on cookie-authenticated state-changing requests(POST/PUT/PATCH/DELETE)→ 403 on missing/mismatch;GET + Bearer-authenticated requests CSRF-exempt;now takes `request: Request`(test callers updated)— `7cca23e`
- [x] F2.5 `frontend/lib/api-client.ts` — `credentials:'include'` on all requests + `X-CSRF-Token`(from the `ekp_csrf` cookie via `getCsrfHeaders()`, exported)on non-GET;raw-fetch callers `lib/api/query.ts` `streamQuery` + `lib/api/kb.ts` `uploadDoc` also send credentials + CSRF;`frontend/lib/auth/index.ts` — `SESSION_TOKEN_STORAGE_KEY` + `readSessionBearer` removed(cookie is the self-register credential;`getBearer()` = mock dev-token / MSAL JWT only);`lib/api/auth.ts` drops the re-export + adds `VerifyEmailResponse.access_token`/`expires_in` — `7cca23e`
- [x] F2.6 `frontend/app/login/page.tsx` — drops the `localStorage.setItem(SESSION_TOKEN_STORAGE_KEY, …)` write + the now-unused import(cookie set by the response);flow still `router.push('/chat')`. `frontend/app/register/page.tsx` — no edit needed(never wrote localStorage;verify-email auto-login cookie makes Step-3 → `/chat` work)— `7cca23e`
- [x] F2.7 Tests — NEW `backend/tests/test_auth_cookie_transport.py`(17 cases:Set-Cookie shape + httpOnly/SameSite/no-Secure-in-local / verify-email auto-login + idempotent / cookie GET no-CSRF / cookie POST 403 without header / 200 with matching header / 403 mismatch / Bearer + mock-Bearer CSRF-exempt / invalid-cookie fallthrough / refresh rotation + revoke-old + 401-no-creds + mock-mode-no-cookie / logout clear+revoke);`test_auth_self_register.py::test_logout_revokes_session_token` + `test_mock_msal.py` 2 dependency tests updated for the new `get_current_user(request, …)` signature;**backend pytest 609 passed / 11 skipped**(was 607 passed / 2 failed — the 2 were the mock_msal sig mismatch, now fixed;+17 new cookie-transport tests);`tsc --noEmit` + `next lint` clean — `7cca23e`
- [x] F2.8 `docs/architecture.md` §3.7「Auth transport」note(per ADR-0022,inline-tagged amendment)+ `docs/adr/0014-*.md` References cross-link to ADR-0022(+ADR-0016, ADR-0023)+ ADR-0022 「Implementation Deliverables」F0.1+F2.1-F2.8 ticked;no new OQ(transport detail);CO_F5_refresh + CO_F5_cookie → CLOSED — `7cca23e`

## F3 — RAGAs 4-metric full integration

> **F3 verdict = PASS(structural;live-verify deferred — same shape as F1)**(2026-05-10 D5)— landed `7f446fb`. The wiring is done: `/eval/run` + `/eval/shootout` now run the real RAGAs 4-metric judge when an Azure OpenAI judge key + `app.state.synthesizer` are both present, mapping `faithfulness` → `EvalReport.faithfulness` + `answer_relevancy` → `EvalReport.correctness` + per-query attention rows into `failed_queries`. Without an Azure key(CI / local — same as `_engine_or_503` already)it falls back to the Recall@5-only report with a documented note. **Live verification against Azure is deferred to a non-proxy env(same CO17 pattern as F1's Postgres path)** — CI mocks at the LLM-judge boundary. **CO_W15_F1_eval_set_v1 stays OPEN**(see F3.4 finding).

- [x] F3.1 `backend/eval/` — NEW `backend/eval/ragas_evaluator.py` `make_ragas_evaluator(settings) -> Callable | None`(extracted from `scripts/run_ragas_eval.py`'s `_make_real_evaluator` + `_patch_for_gpt5` — the GPT-5-reasoning-judge param shim;`scripts/run_ragas_eval.py` now re-exports both, `test_run_ragas_eval_patch.py` unchanged);`backend/eval/orchestrator.py` upgraded — `run_eval_pipeline` takes optional `synthesizer` + `ragas_evaluator`(+ `judge_deployment`);when both present → `build_ragas_samples`(retrieve → synthesize per main query, ADR-0018 `kb_id`)→ `RagasRunner.evaluate` → `EvalReport.faithfulness` ← faithfulness mean,`EvalReport.correctness` ← answer_relevancy mean(「Answer-Correctness」approximated by RAGAs answer-relevancy — real `answer_correctness` needs SME reference answers per Q14),per-query RAGAs errors + below-0.70-attention-threshold scorers → `failed_queries`;`image_association` / `crag_trigger_rate` / `avg_cost_per_query_usd` stay 0.0(custom image-text-correlation metric + CRAG-loop integration + cost-table — out of F3 scope,documented note in `failed_queries`)— `7f446fb`
- [x] F3.2 `backend/api/routes/eval.py` `POST /eval/run` — replaces the W16 F5.4 placeholder;`_ragas_wiring(request)` reads `app.state.synthesizer` + `make_ragas_evaluator(get_settings())` and passes them to `run_eval_pipeline`(synchronous,bounded by the existing `max_main_queries` cap — no job-id shape was introduced W16);502 on eval failure(unchanged);503 if no `RetrievalEngine`(unchanged)— `7f446fb`
- [x] F3.3 `POST /eval/shootout` — same `_ragas_wiring` path passed through to `run_eval_pipeline` per reranker entry(cohere-v4.0-pro / off);voyage / zeroentropy / azure still skipped → CLI driver(unchanged)— `7f446fb`
- [x] F3.4 `eval-set-v1` verify(CO_W15_F1_eval_set_v1)— **FINDING**:`docs/eval-set-v1.yaml`(final)does **NOT** exist;`docs/eval-set-v1-draft.yaml` is the WIP. Finalizing v1 needs Chris's SME reference-answer labels per Q14 — without those, `answer_correctness` proper can't be scored(hence the answer-relevancy approximation in F3.1). **CO_W15_F1_eval_set_v1 stays OPEN** pending the SME label cascade;no ground truth fabricated. The route exposes `eval-set-v0`(real validated subset)+ `eval-set-v1-draft`(WIP)as the available ids — `7f446fb`
- [x] F3.5 Tests — NEW `backend/tests/test_eval_ragas.py`(4 cases:`make_ragas_evaluator` → None without Azure key / `run_eval_pipeline` Recall@5-only fallback shape / `run_eval_pipeline` populates RAGAs metrics with a stub evaluator + fake engine + fake synthesizer / below-threshold queries surface in `failed_queries`);the stub `ragas_evaluator` IS the LLM-judge boundary — no live Azure in CI;existing `test_orchestrator.py` / `test_eval_endpoints.py` / `test_ragas_runner.py` / `test_run_ragas_eval_patch.py` / `test_eval_runner.py` unchanged + pass;**backend pytest 613 passed / 11 skipped**(was 609/11)— `7f446fb`
- [ ] 🚧 F3.5b RAGAs live-verify(`scripts/run_ragas_eval.py` or `POST /eval/run` against a populated Azure index + judge)— **deferred to a non-proxy env / personal Azure dev tier(CO17 pattern, same as F1.5b)**;the structural integration is complete + CI-tested,but the real judge run needs Azure OpenAI keys + a populated `ekp-kb-drive-v1` index
- [ ] F3.6 V5 Eval Console — verify in browser smoke(F5)that the 4-metric cards + Failed queries table render the real `EvalReport`(no frontend code change — already consumes the schema;the cards now show real faithfulness/correctness when the judge ran, or 0.0/None + the note when it didn't)

## F4 — Frontend hardening bundle — `9ee636c`

- [x] F4.1 `frontend/app/admin/kb/[id]/page.tsx` Documents tab — wired `GET /kb/{id}/documents`(real per W16 F5.1.1);NEW `frontend/lib/api/documents.ts`(`DocumentSummary` + `documentsApi.list`);renders a doc table(title + tags / format / chunks / last-indexed / doc_id)via `useQuery`;loading skeleton + error banner + empty-index Upload prompt;**dropped the stale「Backend status: GET /kb/{id}/documents — W2 listing implementation (501 stub)」copy**(`BackendStubNote` component itself preserved — still used by ChunksTab);top-of-file docstring F3.2 line updated to reflect W17 wiring — `9ee636c`
- [x] F4.2 `frontend/app/admin/page.tsx` — removed unused `CardTitle` import;`npm run lint` now green globally(it was the only ESLint error)— `9ee636c`
- [x] F4.3 `NEXT_PUBLIC_LANGFUSE_URL` — **already present** in `.env.example`(line ~101,added W16 F5.x.2 `1dbcdf3`);`frontend/app/debug/[traceId]/page.tsx` reads it via `LANGFUSE_FALLBACK_BASE`(ADR-0020 Session 2);effectively closed — no change needed;CO_W15_F2_langfuse_url → CLOSED
- [x] F4.4 Cohere reranker naming canonicalization — canonical model identifier = `Cohere-rerank-v4.0-pro`(Azure Marketplace deployment name,matches the live `.env`);aligned `backend/storage/settings.py` `cohere_rerank_model` default + `backend/retrieval/reranker/cohere.py` default + docstrings + `.env.example` `COHERE_RERANK_MODEL`;human display label `"Cohere v4.0-pro"` kept for UI;grep-verified no stray code mismatch(`cohere-rerank-v4-pro` / unqualified `rerank-v4.0-pro` — empty);ADR / audit doc historical references left per anti-pattern「keep historical narrative」— `9ee636c`

## F5 — a11y + dark-mode verification pass

> **F5 verdict = PASS**(2026-05-10 D6)— `[oklch(...)]` arbitrary-value form = **0 globally**(milestone preserved);dark-mode mechanism confirmed(next-themes `attribute="class"` → `globals.css` `.dark` vars → `tailwind.config.ts` `oklch(var(--token))` → utility classes;`tokens.ts` `colorsDark` = the documented inverted-button pattern);**V8 Login + V7 Landing browser-verified in dark**(`body bg === oklch(0.18 0.005 285)` == `colorsDark.background`;inverted-button pattern rendering;error-boundary fallback incidentally verified dark)。V1-V6/V9 inherit(token-class-only — the `[oklch`=0 grep proves no view can escape the `.dark` swap)→ the per-view interactive dark-mode walkthrough stays the user's pre-Beta smoke(W12-W15 caveat shape, scope reduced to the interactive layer). One W17-own-mess fix:`DocumentsTable`(F4.1)`<th>` → `<th scope="col">` ×5(WCAG 1.3.1). `tsc`+`next lint` clean. Playwright screenshots temp-only(reviewed, deleted, not committed).

- [x] F5.1 Dark-mode visual verify — `Grep '\[oklch'` across `frontend/` = **0**(milestone preserved — bare `oklch(...)` hits all in `tokens.ts` source-of-truth + `tailwind.config.ts` wrappers + docstrings, zero hardcoded arbitrary values in components/pages);mechanism confirmed via code review(`layout.tsx` ThemeProvider `attribute="class"` → `globals.css` `.dark` CSS vars → tailwind `oklch(var(--token))` → utility classes);browser smoke `next dev -p 3001` + Playwright `classList.add('dark')` — V8 Login(light+dark screenshots;dark `body bg oklch(0.18 0.005 285)` + `color oklch(0.95 0 0)`;BrandPanel + button inversion working)+ V7 Landing(dark full-page;inverted buttons + coral accents + token borders)+ error-boundary fallback(incidental, dark OK);V1-V6 Chat/Admin/KB/Eval/Debug + V9 Register inherit(same token utility classes — `[oklch`=0 grep proves it)→ interactive 9-view walkthrough = user pre-Beta smoke;**CO_W15_F3_dark_mode_visual_verify → partially closed**(mechanism + grep + V7/V8 verified;interactive admin/chat-view pass remains — F7 decides full closure)— `(this commit)`
- [x] F5.2 ARIA spot-check — register 6-box code input(`aria-label="Digit N"` + `inputMode="numeric"` + `autoComplete="one-time-code"` + keyboard nav + paste-distribute)/ login dual-path(email/password `<Input id>` + `<Label htmlFor>`;SSO button text-labelled)/ new Documents tab(`<th scope="col">` ×5 added — F5.3)/ RetrievalTab ADR-0021(Sliders `aria-label`;Selects/Switches/textarea `id`+`<Label htmlFor>`)/ `ThemeToggle` `aria-label="Toggle theme"` — all interactive elements labelled;**pre-existing gap noted not fixed**(not W17's mess):register `Stepper` `<li>` active step lacks `aria-current="step"`(W13) → defer to CO_W15_F3_aria_full_audit;full NVDA/JAWS/VoiceOver audit stays Tier 2 — `(this commit)`
- [x] F5.3 W17-own-mess a11y fix — `app/admin/kb/[id]/page.tsx` `DocumentsTable`(added W17 D1 / F4.1)`<th>` → `<th scope="col">` ×5(WCAG 1.3.1 header association);surgical, only this;`tsc --noEmit` + `next lint` clean after the edit;pre-existing gaps(Stepper `aria-current`)mentioned + deferred — `(this commit)`

## F6 — Vitest + React Testing Library infrastructure scaffold

> **F6 verdict = PASS**(2026-05-10 D7)— landed `(this commit)`. Dev deps via `pnpm add -D`(H2-exempt §5.2): `vitest ^4.1.5` + `@vitejs/plugin-react ^6.0.1` + `@testing-library/react ^16.3.2` + `@testing-library/jest-dom ^6.9.1` + `@testing-library/user-event ^14.6.1` + `jsdom ^29.1.1`(`pnpm install` OK — R8 doesn't block the npm registry, only binary CDNs);`vitest.config.ts`(jsdom + react plugin + `@`→`.` alias + `setupFiles` + `tests/unit/**` include + `tests/e2e/**` excluded);`tests/unit/{setup.ts(jest-dom matchers + cleanup),button.test.tsx(3 cases — render + token-class variant + onClick interaction),README.md(3-layer boundary)}`;`package.json` `test:unit`/`test:unit:watch` scripts. `pnpm test:unit` → 1 file / 3 tests passed;`tsc`+`next lint` clean. **CO_W15_F4_vitest_baseline_gap → CLOSED**(harness exists + sample passes + boundaries documented;coverage expansion stays Tier 2).

- [x] F6.1 `frontend/package.json` devDependencies — added `vitest ^4.1.5` + `@vitejs/plugin-react ^6.0.1` + `@testing-library/react ^16.3.2` + `@testing-library/jest-dom ^6.9.1` + `@testing-library/user-event ^14.6.1` + `jsdom ^29.1.1`(dev deps — H2 exception per §5.2);`pnpm add -D` / `pnpm install` succeeded(`pnpm-lock.yaml` +101/-35 packages — R8 corp proxy does NOT block the npm registry, only binary-CDN downloads per the W15 precedent)— `(this commit)`
- [x] F6.2 NEW `frontend/vitest.config.ts` — `defineConfig` from `vitest/config`;`plugins: [react()]`;`resolve.alias` `'@' → resolve(__dirname, '.')`(mirrors `tsconfig.json` `paths`);`test.environment: 'jsdom'` + `globals: true` + `setupFiles: ['./tests/unit/setup.ts']` + `include: ['tests/unit/**/*.test.{ts,tsx}']` + `exclude: ['node_modules', '.next', 'tests/e2e/**']`(vitest never picks up Playwright specs)— `(this commit)`
- [x] F6.3 NEW `frontend/tests/unit/` — `setup.ts`(`import '@testing-library/jest-dom/vitest'` + `afterEach(cleanup)`)+ `button.test.tsx`(sample component test on `@/components/ui/button`:renders children + default `bg-primary`/`text-primary-foreground` token-class variant / `variant="destructive"` swaps to `bg-destructive` / `onClick` fires on `userEvent.click` — 3 cases, render + interaction smoke proving jsdom + RTL + jest-dom + cva + user-event wire up)— `(this commit)`
- [x] F6.4 `frontend/package.json` — `test:unit` (`vitest run`) + `test:unit:watch` (`vitest`) scripts added between `type-check` and `test:e2e`;no conflict with `test:e2e*`;`pnpm test:unit` → **Test Files 1 passed (1) / Tests 3 passed (3)** — `(this commit)`
- [x] F6.5 NEW `frontend/tests/unit/README.md` — the 3 test layers(unit/component = Vitest+RTL here / E2E golden-path = Playwright `tests/e2e/` / backend = pytest)+ run commands + config pointer;Tier 2 = expand component/hook/MSW-data-fetch coverage;interactive multi-page flows stay in the Playwright layer(`CO_W15_F4_interactive_flow_E2E`);**CO_W15_F4_vitest_baseline_gap → CLOSED** — `(this commit)`

## F7 — Phase closeout + W18+ rolling-JIT trigger

> **F7 verdict = PASS**(2026-05-10 D8)— landed `(this commit)`. **W17 phase Gate = PASS**(all 7 plan §3 criteria substantively met;sub-verdicts:F1 PARTIAL — 🚧 F1.5b R8-blocked Postgres-path smoke deferred / F3 PASS-structural — 🚧 F3.5b Azure-key-bound live-verify deferred / F2,F4,F5,F6 PASS;no FAIL condition;backend pytest grew 593/7 → 613/11). Full Gate rationale + 7-section retro in `progress.md`.

- [x] F7.1 W17 phase Gate verdict = **PASS**(plan §3 7-criterion eval — see `progress.md` Day-8 "W17 phase Gate verdict" + Retro);sub-verdicts F1 PARTIAL(🚧 F1.5b)/ F3 PASS-structural(🚧 F3.5b)/ F2,F4,F5,F6 PASS;no FAIL condition(no ADR Tier-2 creep;in-memory fallback intact + tested;mock-auth dev path verified working post-cookie-change via the F5 next-dev smoke;pytest grew, no regression)— `(this commit)`
- [x] F7.2 W17 `progress.md` Retro — 7 sections(What worked / What didn't & friction / Surprises / Decisions / Carry-overs to W18+ / Time tracking / Spec ref alignment)— `(this commit)`
- [x] F7.3 ADR-0017 + ADR-0022 + ADR-0023 status = **`Accepted`**(verified — all three created `Accepted` this phase:ADR-0022/0023 in `6edd9ef`, ADR-0017 in `fb0253a`;`docs/adr/README.md` index has all three rows + "next available 0024", only ADR-0013 AF3 reserved)— `(this commit)`(verify-no-op)
- [x] F7.4 W17 `plan.md` + `checklist.md` + `progress.md` frontmatter `status: active` → `status: closed`(this commit cycle)— `(this commit)`
- [x] F7.5 W18+ phase folder **NOT pre-created**(rolling-JIT per CLAUDE.md §10 R1)— kickoff candidates noted in `progress.md` Retro carry-overs:W16 F1-F4 if Track A IT cred lands / Tier 2 prep governance(Q12)/ the user's local-dev-setup task(needs `scripts/seed_dev_kb.py` or one-liner — in-memory KB store starts empty)— `(this commit)`
- [x] F7.6 Hygiene catch-up — `session-start.md`:§2 "v5 frozen"→v6 + Cohere v3.5→v4.0-pro;§3 C02 status(in-memory→Postgres-backed per ADR-0023, CO18 CLOSED, 🚧 F1.5b)+ C06 status(RAGAs integrated W17 F3, CO_W15_F1_eval_set_v1 OPEN);§4 authority-order header v5→v6;§11 ADR count 15→22 landed + next-NNNN 0017→0024 + carry-overs CLOSED(CO18 / CO_F5_refresh / CO_F5_cookie / CO_W15_F2_langfuse_url / CO_W15_F4_vitest_baseline_gap)+ CO_W15_F3_dark_mode_visual_verify partially closed + W17 milestones row + W17 closed;§10 W16 notes(F5 backend stub cascade done — F1-F4 still Track-A-blocked);Last-Updated + Update-history row. `COMPONENT_CATALOG.md` §11 stale "C13 Workflow Engine" note — left as standalone housekeeping(already flagged in session-start.md §3 Note + W17 D3 changelog Deviation 4;Karpathy §1.3 — not expanded here)— `(this commit)`
- [x] F7.7 No new OQ surfaced — `decision-form.md` unchanged(Q8 4-metric-replacement note holds: F3 delivered the *current* 4 metrics, not a replacement set;Q14 SME labels remain the eval-set-v1 blocker — that's the existing Q14 status, not a new OQ)— `(this commit)`

---

## Cross-Cutting

- [x] Each commit references `progress.md` Day-N entry(R2)— `docs(planning):` housekeeping commits exempt — held(F4 `9ee636c`↔Day-1 / F1 `2453a50`↔Day-2 + `5c5df92`↔Day-3 / F2 `7cca23e`↔Day-4 / F3 `7f446fb`↔Day-5 / F5 `414a21e`↔Day-6 / F6 `2d71b1e`↔Day-7 / F7 this↔Day-8;F0 ADR `6edd9ef` + F0b `fb0253a` = `docs(adr):` housekeeping-exempt)
- [x] Component tag in commit message per CC-1 — held(F1 = C02+C11+C12 / F2 = C08+C11 / F3 = C06+C08 / F4 = C09+C07 / F5 = cross-cutting C09+C10+C11 / F6 = test harness governance — reflected in commit scopes `feat(infra,api)` / `feat(api)` / `feat(api,frontend)` / `feat(eval,api)` / `feat(frontend,api)` / `feat(frontend)` / `chore(frontend)`)
- [x] OQ status sync to `decision-form.md`(R4)— **no W17 critical OQ surfaced**;`decision-form.md` unchanged(Q8 4-metric-replacement note holds: F3 delivers the *current* 4 metrics, not a replacement set — Q8 stays deferred Tier 2;Q14 SME labels = the existing eval-set-v1 blocker, not a new OQ)
- [x] Risk register update — R8 corp proxy hit a **5th cumulative occurrence**(`pip install psycopg[binary]` blocked, D2)→ **ADR-0017 landed `fb0253a`**(R8 mitigation pattern formalized — now the authoritative record);converse confirmed:`pnpm add -D` of the F6 Vitest deps went through fine(R8 blocks binary-CDN downloads, not the npm registry). `RISK_REGISTER.md` R8-row + R12-row(Azurite mismatch — permanent fix = cloud Azure Blob, Track A)bump is opportunistic living-doc maintenance — ADR-0017 + session-start.md §11 carry the substantive R8 state;not separately re-touched here(Karpathy §1.3)
- [x] CLAUDE.md §5.1 H1 check — F1 storage-layout change covered by **ADR-0023**;F2 transport change covered by **ADR-0022**;both authorized via the 2026-05-10 AskUserQuestion("approved + write ADR");no other architectural change(F3 RAGAs = within §5.6 spec / F4 = UI wiring + naming canon / F5 = a11y verify / F6 = dev-dep test harness — none touch §3/§4 components)
- [x] CLAUDE.md §5.2 H2 check — `psycopg[binary]`(F1)covered by ADR-0023;Vitest/RTL/jsdom/`@vitejs/plugin-react`/`@testing-library/*`(F6)= **dev-dependency exception per §5.2**;`ragas` was already installed(no new vendor for F3);no other new dependency
- [x] CLAUDE.md §3.1/§3.2 conventions — `ruff check` clean on all new/changed backend files;`mypy -p api.auth` + `-p eval` clean on the new modules except the documented R8 `psycopg` `import-not-found` ×2 in `postgres_users_store.py`(ADR-0017 PARTIAL-PASS)+ the pre-existing `import yaml` untyped + azure/jose stubs;`tsc --noEmit` + `next lint` clean(frontend);no `any`
- [x] CLAUDE.md §5.5 H5 — no secret committed;`DATABASE_URL` only in `.env`(gitignored — `.env.example` carries a commented template);no separate cookie-signing key(the session token IS the credential — opaque random `secrets.token_urlsafe`, validated server-side via `resolve_session`);`Secure` cookie attr gated on `environment != "local"`;no PII / no plaintext-prompt logging changed

---

**Lifecycle reminder**:呢份 checklist 衍生自 `plan.md` deliverables。新加 deliverable 必須先入 plan + §7 changelog,然後再加 checklist item。延後 item 標 🚧 + reason,**唔可以刪**未勾選 `[ ]`。
