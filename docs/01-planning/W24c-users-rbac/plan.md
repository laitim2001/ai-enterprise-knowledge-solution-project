---
phase: W24c-users-rbac
name: "/users Tier 1.5 RBAC — full RBAC per ADR-0027 Option A (Members + Roles + Groups + Audit log + per-KB ACL)"
sprint_week: W24c
start_date: 2026-05-21              # real-calendar — Chris directive 2026-05-21 「Kickoff W24c — /users RBAC」(AskUserQuestion pick)
end_date: 2026-05-30                # ~10-14 plan-day window;~20 backend days compressed by W19-W24 real-calendar collapse (~1.5-12× pattern)
status: active
spec_refs:
  - ADR-0027                        # Option A full RBAC — Accepted W19 F6 Chris pick
  - ADR-0025                        # /kb/[id] Access tab — hard dep on RBAC infra (disabled affordance → active at W24c)
  - ADR-0026                        # Settings Identity & Auth role mapping reads the same role/permission matrix
  - ADR-0023                        # Postgres persistent backing — users table + PostgresUsersStore already exist
  - ADR-0014                        # hybrid auth — Entra ID group → role mapping concept
  - ADR-0017                        # R8 corp-proxy mitigation — Entra Graph SDK new dep (H2) install discipline
  - CLAUDE.md §5.1 H1               # RBAC Tier 2 hook → Tier 1.5 = architectural change (ADR-0027 covers it)
  - CLAUDE.md §5.2 H2               # Entra Graph SDK NEW dependency — pre-cleared via ADR-0027 acceptance
  - CLAUDE.md §5.4 H4               # Tier boundary — Power User role + custom roles stay Tier 2 (disabled affordance)
  - CLAUDE.md §10 R1-R6             # rolling JIT + plan-before-code + R6 pre-active-flip recursive grep audit
prior_phase: W24b-frontend-wave-c2-settings-depth   # closed 2026-05-20 PASS WITH SMOKE-USER-DEFERRED CAVEAT
related_artifacts:
  - docs/adr/0027-users-tier-1-5-rbac.md            # primary ADR — Option A full RBAC
  - docs/adr/0025-kb-detail-8-tab.md                # Access tab dep
  - references/design-mockups/ekp-page-users.jsx    # canonical visual spec — PageUsers 4 tabs + TabKbAccess
  - backend/api/auth/users_store.py                 # existing users store — role column ADD
  - backend/api/auth/postgres_users_store.py        # PostgresUsersStore per ADR-0023
  - backend/api/middleware/                         # audit_log.py + rate_limit.py exist — ACL middleware NEW
  - backend/storage/audit_log_storage.py            # audit_log table ALREADY EXISTS (W24-c1 F4) — W24c EXTENDS it
---

# Phase W24c — /users Tier 1.5 RBAC Plan

> **Authorization**:Chris directive 2026-05-21「**Kickoff W24c — /users RBAC**」(AskUserQuestion pick post-W24b closeout)+ ADR-0027 **Accepted Option A full RBAC**(W19 F6 Chris pick — Option A over the W19 F2 §6 Option B minimal recommendation)。
>
> **Wave lineage**:W19 F4 §3.6 SPLIT — Wave C(ADR-0026 + ADR-0027 combined ~42 backend days)split into sub-phases。Wave C1 = ADR-0026 Settings 6-tab backend + read-mostly frontend(W24)。Wave C2 = ADR-0026 Settings depth inline-edit(W24b)。**Wave C3 = ADR-0027 `/users` Tier 1.5 RBAC = W24c**(this phase)— the remaining Wave C work。
>
> **Scope weight**:per ADR-0027 Option A ~**20 backend days** — the largest single phase in the W-series。5 NEW Postgres tables(`audit_log` 已存在 W24-c1 — 見 R6 finding §7 Day 0)+ ACL middleware on every protected endpoint + Entra Graph SDK new dependency + NEW `/users` 4-tab route + `/kb/[id]` Access tab activation per ADR-0025 + NEW C16 Users Service component(or C11 expansion — F1 decision)。

---

## §0 Phase identity

**Trigger**:W24b-wave-c2 closeout 2026-05-20 + Chris W24c kickoff directive 2026-05-21 + ADR-0027 Accepted Option A(W19 F6)。

**Decision authority**:Chris W19 F6 ADR-0027 Accepted **Option A full RBAC**(explicitly over Option B minimal recommendation)+ W24c kickoff pick 2026-05-21。

**Scope**(ADR-0027 Option A):
- F0 Kickoff cascade(plan + checklist + progress)
- F1 `architecture.md v6 §3.7` amendment(RBAC Tier 2 hook → Tier 1.5 minimum)+ NEW §3.8 `/users` reference + Entra Graph SDK install(H2 pre-cleared / ADR-0017 Plan B)+ C16 Users Service vs C11 expansion decision
- F2 RBAC schema layer — 5 NEW Postgres tables(`roles` + `role_permissions` + `groups` + `group_members` + `kb_acl`)+ storage Protocol/InMemory/Postgres + role/permission seed(3 active roles + Power User Tier 2 + PERMISSIONS_MATRIX 5 areas × 24 permissions)
- F3 ACL middleware + auth-time role claim(role carried in session/cookie)+ mock-auth `role:'admin'` + real-MSAL Entra group → role extraction
- F4 `/users` Members tab backend(list + filter + invite + suspend + role-change endpoints)
- F5 `/users` Roles tab backend(role cards + permissions matrix endpoints)
- F6 `/users` Groups tab backend + `POST /groups/sync-from-entra`(Entra Graph SDK)
- F7 Audit log expansion — new action types(role.changed / user.invited / kb.access.granted / kb.config.changed / user.suspended)+ writes on protected endpoints + 90d retention
- F8 per-KB ACL — `kb_acl` table CRUD + Manage/Edit/Query role override + add member + add Entra group(TabKbAccess backend)
- F9 frontend `/users` 4-tab page(Members / Roles / Groups / Audit log)+ `useRole()` hook + role-gated view rendering
- F10 frontend `/kb/[id]` Access tab activation(disabled affordance → active)+ `<TabKbAccess>` per ADR-0025
- F11 Tests(backend pytest ≥80% on ACL/RBAC modules per H6 + Vitest + Playwright)
- F12 Closeout cascade(retro + Gate + session-start sync + ADR-0027 + ADR-0025 Implementation Status)

**Out of scope**(Tier 2 per ADR-0027 §Consequences + H4):
- Custom role creation(PERMISSIONS_MATRIX stays hard-coded static admin policy)
- Power User role activation(4th matrix column visible but disabled per H4)
- Multi-tenancy / tenant-scoped RBAC(Tier 2)
- Real-MSAL feature flag operational verification(W16 Track A IT cred parallel track,Q11 operational early June 2026 — mock-auth default continues per user 岔口 2)

**Authorities**:
- **ADR-0027** Status `Accepted (Option A full RBAC)` → W24c closeout amends to `Accepted + implemented`
- **ADR-0025** Access tab disabled affordance → activated at W24c F10
- **CLAUDE.md §5.1 H1** — RBAC promotion Tier 2 hook → Tier 1.5 is architectural;ADR-0027 covers it;F1 amends `architecture.md v6 §3.7`
- **CLAUDE.md §5.2 H2** — Entra Graph SDK NEW dep — pre-cleared via ADR-0027 acceptance(W19 F6 noted「Entra Graph SDK new dependency H2 trigger」);F1 install per ADR-0017 Plan B sequencing
- **CLAUDE.md §5.4 H4** — Power User + custom roles stay Tier 2(disabled affordance,not implemented)
- **CLAUDE.md §10 R1-R6** — rolling JIT;R6 pre-active-flip 5-step recursive grep audit(plan-text + code)per F-deliverable

---

## §1 Authorization + spec refs

| F-deliverable | Authorization | Spec ref |
|---|---|---|
| F0 Kickoff | this plan + Chris kickoff directive 2026-05-21 | CLAUDE.md §10 R1 + R5 + ADR-0027 §References |
| F1 Spec amendment + Entra Graph SDK | ADR-0027 §Decision + CLAUDE.md §5.1 H1 + §5.2 H2 + ADR-0017 | `architecture.md v6 §3.7`+NEW§3.8 inline-tag + `backend/pyproject.toml` Entra Graph SDK |
| F2 RBAC schema | ADR-0027 §Decision Option A 6-table(`audit_log` 已存在 → 5 NEW)| `backend/storage/` NEW rbac storage 3-file split per ADR-0023 pattern |
| F3 ACL middleware | ADR-0027 §Decision Option A ACL + ADR-0014 role claim | `backend/api/middleware/` NEW `acl.py` |
| F4 Members backend | ADR-0027 §Context Members tab | `backend/api/routes/users/` NEW router |
| F5 Roles backend | ADR-0027 §Context Roles tab + PERMISSIONS_MATRIX | `backend/api/routes/users/` roles endpoints |
| F6 Groups backend | ADR-0027 §Context Groups tab + Entra Graph SDK | `POST /groups/sync-from-entra` |
| F7 Audit log expansion | ADR-0027 §Context Audit log tab 6 action types | `backend/storage/audit_log_storage.py` extend(W24c F6 precedent — additive)|
| F8 per-KB ACL | ADR-0027 §Decision `kb_acl` + ADR-0025 Access tab | `kb_acl` table + per-KB ACL CRUD |
| F9 frontend /users | ADR-0027 §Context 4 tabs + mockup `ekp-page-users.jsx` | `frontend/app/(app)/users/` NEW route |
| F10 frontend Access tab | ADR-0025 Access tab + ADR-0027 TabKbAccess | `frontend/` KB detail Access tab activate |
| F11 Tests | CLAUDE.md §5.6 H6(ACL/RBAC ≥80%)+ W23-W24b test pattern | pytest + Vitest + Playwright |
| F12 Closeout | CLAUDE.md §10 R3 + W19-W24b closeout pattern | retro + ADR-0027 + ADR-0025 Implementation Status |

---

## §2 F0-F12 deliverables

**Rolling JIT discipline**:F0 + F1 detailed at kickoff;F2-F12 sketched — detail refines per-deliverable at active-flip per W12-W24b pattern。Per CLAUDE.md §10 R6 — pre-active-flip 5-step grep audit applied **recursively** to plan-text **and** code-at-active-flip-time。**F-deliverables may sub-split at active-flip** per actual scope(~20 backend days = large;e.g. F4 Members / F5 Roles may each sub-split)— logged in §7 changelog per R3。

### F0 — Kickoff cascade(landed at phase open — `(this commit)`)

- **Component(s)**:governance(no Cn touched at F0)
- **Spec ref**:CLAUDE.md §10 R1 + this plan §0
- **OQ deps**:Q11(Entra ID tenant — Resolved decision-level;operational early June 2026 — non-blocking,mock-auth default per user 岔口 2)
- **Acceptance criteria**:
  - F0.1 W24c folder `plan.md`(this file)+ `checklist.md` + `progress.md` created `status: active` 2026-05-21
  - F0.2 NO `frontend/` or `backend/` code change at kickoff(F0 governance only — per W19-W24b F0 precedent)
  - F0.3 `architecture.md v6 §3.7` amendment deferred to F1(not F0 — F0 is doc-folder governance only)
  - F0.4 Pre-active-flip 5-step grep audit recursive(per R6)completed at kickoff prep(documented in `progress.md` Day 0 + plan §7 changelog):
    - `references/design-mockups/ekp-page-users.jsx` confirmed exists(`PageUsers` 4 tabs + `TabKbAccess`)
    - `backend/api/auth/{users_store,postgres_users_store,users_repo}.py` confirmed — `users` table per ADR-0023 exists,`role` column ADD needed
    - `backend/api/middleware/` has `audit_log.py` + `rate_limit.py` — NEW `acl.py` needed
    - **`audit_log` Postgres table ALREADY EXISTS**(W24-c1 F4 + W24b F6 filter)→ ADR-0027「6 NEW tables」實際 = **5 NEW**(`roles` + `role_permissions` + `groups` + `group_members` + `kb_acl`);`audit_log` 係 EXTEND not CREATE
    - `frontend/app/(app)/users/` does NOT exist — `/users` route is NET NEW per ADR-0027
  - F0.5 W24c kickoff cascade committed `(this commit)`
- **Effort estimate**:0.25 day(this commit)

### F1 — Spec amendment + Entra Graph SDK install + C16/C11 decision

- **Component(s)**:**C11** Identity & Access(or NEW **C16** Users Service — F1 decision)+ **C12** DevOps & Infra(Entra Graph SDK dep)
- **Spec ref**:
  - ADR-0027 §Decision「Amend `architecture.md v6 §3.7` to promote RBAC from Tier 2 hook to Tier 1.5 minimum + add §3.8 /users page reference」
  - CLAUDE.md §5.2 H2 — Entra Graph SDK NEW dep(`msgraph-sdk` or `azure-identity` Graph scope)— **pre-cleared via ADR-0027 acceptance**(W19 F6);install per ADR-0017 §Decision-rule #5 Plan B sequencing
  - ADR-0027 §Decision Option A「New Cn:C16 Users Service(or fold into C11 Identity & Access expansion)」— F1 strategic decision
- **OQ deps**:none
- **Acceptance criteria**:
  - F1.1 `architecture.md v6 §3.7` inline-tagged amendment — RBAC「Tier 2 hook」→「Tier 1.5 minimum」per ADR-0027(doc version held — same convention as ADR-0024/0023/0022/0026 inline-tag)
  - F1.2 NEW `architecture.md v6 §3.8 /users page` reference paragraph(4 tabs + per-KB ACL)inline-tagged
  - F1.3 **C16 vs C11 decision** — Option A ~20 days + 5 tables + ACL middleware + Entra Graph SDK weight evaluated;decision logged plan §7 + `COMPONENT_CATALOG.md` updated(NEW C16 Users Service card OR C11 scope-expansion note)
  - F1.4 Entra Graph SDK install — Plan B (a) `pip install` attempt;若 R8 fail → Plan B (c) mobile hotspot(precedent:W24-c1 Key Vault SDK + Langfuse)+ ADR-0017 amendment occurrence #9
  - F1.5 `backend/pyproject.toml` + lock confirm SDK landed;`mypy --strict` clean on the import site;lazy-import per ADR-0023(unset Entra config never touches SDK — Graph SDK only loaded on `sync-from-entra`)
- **Effort estimate**:1 day(0.5 spec amendment + 0.25 SDK install + 0.25 C16/C11 decision)

### F2 — RBAC schema layer(5 NEW Postgres tables + storage)

- **Component(s)**:C16/C11 + **C08** API Gateway(schema)
- **Spec ref**:ADR-0027 §Decision Option A — `roles` + `role_permissions` + `groups` + `group_members` + `kb_acl`(5 NEW;`audit_log` already exists)+ ADR-0023 storage Protocol/InMemory/Postgres 3-file split pattern
- **OQ deps**:none
- **Acceptance criteria(refined at active-flip Day 2)**:
  - F2.1 — 5 NEW Postgres tables idempotent `CREATE TABLE IF NOT EXISTS`(`roles` + `role_permissions` read+seeded F2;`groups` + `group_members` + `kb_acl` declared-ahead — Protocol methods land F6/F8 per R6 Day 2 finding #3)
  - F2.2 — `users.role` column ADD(default `'user'`)+ `UserRecord.role` field + `PostgresUsersStore` `_USER_COLS`/`_row_to_user`/`add_user`/`replace_user` 同步(R6 Day 2 finding #2 — column-only ≠ usable)
  - F2.3 — `RbacBackend` Protocol(async)+ `InMemoryRbacBackend` + `PostgresRbacBackend` + `make_rbac_backend` factory(lazy-import per ADR-0023)
  - F2.4 — Seed:3 active roles(Admin / Editor / End User)+ Power User(Tier 2 `active=False`)+ PERMISSIONS_MATRIX(5 areas × **23** permissions per mockup `ekp-page-users.jsx` lines 26-60 — R6 Day 2 finding #1:plan「24」→ actual 23)
- **Effort estimate**:~4 days

### F3 — ACL middleware + auth-time role claim

- **Component(s)**:**C08** API Gateway + **C11** Identity & Access
- **Spec ref**:ADR-0027 §Decision Option A「ACL middleware(every protected endpoint checks role + KB ACL)+ auth-time role claim」+ ADR-0014 role-from-Entra-group + §Consequences「mock provider returns role:'admin';real MSAL extracts role from group membership」
- **OQ deps**:Q11(non-blocking — mock-auth default)
- **Acceptance criteria(refined at active-flip Day 3)**:
  - F3.0 — role key vocabulary 統一(R6 #5;Chris AskUserQuestion 2026-05-21「short form」)— W24-c1 `admin_identity` long-form(`workspace_admin` etc.,Option-B-era)→ RBAC-core short(`admin`/`editor`/`user`/`power`),9 files;`settings-identity.tsx` role 顯示 `.replace('_',' ')` → NEW `EKP_ROLE_LABELS` map 修 H7 drift(mockup line 664「Workspace Admin」title-case)
  - F3.1 — `acl.py` `require_role(*roles)` FastAPI dependency factory(R6 #2 — `Depends()` 非 Python decorator);`require_kb_acl` defer F8(R6 #3 — 連 `kb_acl` storage method,no stub per Karpathy §1.2)
  - F3.2 — `AuthenticatedUser.role` field + 三路徑 server-side resolve(R6 #1 — self-register `UserRecord.role` / mock `Settings.auth_mock_role` / MSAL app-role claim;role 非真存 cookie)
  - F3.3 — mock `auth_mock_role` default `admin` + real-MSAL `_role_from_claims`(Entra app-role claim;Tier-1-grantable `{admin,editor,user}`,`power` downgrade per H4)
  - F3.4 — `require_role` 403 contract test-verified(R6 #4 — per-endpoint apply 由 F4-F10 inline per plan §4 R-W24c-3「opt-in」,F3 = mechanism)
- **Effort estimate**:~3 days

### F4 — `/users` Members tab backend

- **Component(s)**:C16/C11 + C08
- **Spec ref**:ADR-0027 §Context Members tab(11 mock users + filter seg + 10-col table + invite/suspend/role-change)
- **Acceptance criteria(refined at active-flip Day 4)**:
  - F4.1 — `GET /users` 返回全部 members(`UserListResponse{users,total}`,newest-first);filter seg = client-side per mockup(R6 #1 — backend 返回 subset,mockup analytics 欄位 queries_7d/kbs_owned/last_login/source/group 非 Tier 1 backend state,§13 backend wins on field shape)
  - F4.2 — `POST /users/invite`(建 `status="invited"` record;**invite email + accept flow 🚧 defer** — C13 territory)+ `POST /users/{oid}/suspend` + `PATCH /users/{oid}/role`(`power` reject 422 per H4)
  - F4.3 — audit_log writes(`user.invited`/`user.suspended`/`role.changed` — F4 加 3 個 `AuditAction`,F7 加 kb.* + retention)
  - 全部 endpoint router-level `require_role("admin")`(F3.4「per-endpoint apply F4-F10」首次兌現)
- **Effort estimate**:~3 days

### F5 — `/users` Roles tab backend

- **Component(s)**:**C16** Users Service + **C08** API Gateway
- **Spec ref**:ADR-0027 §Context Roles tab(4 role cards + permissions matrix)+ mockup `ekp-page-users.jsx` lines 19-286(`ROLES` + `PERMISSIONS_MATRIX` + `RolesTab`)
- **OQ deps**:none
- **Acceptance criteria(refined at active-flip Day 5)**:
  - F5.1 — `GET /roles` 返回 `RoleListResponse{roles,total}`(4 roles in `_ROLE_ORDER`:Admin / Editor / End User active + Power User Tier 2 `active=False`);role member count = client-side per mockup(R6 #3 — mockup `RolesTab` count = `MOCK_USERS.filter`;F2 `Role` schema 無 `member_count`;§13 backend wins on field shape — mirrors F4 D4.6)
  - F5.2 — `GET /roles/permissions` 返回 `PermissionMatrixResponse{permissions,total}`(flat `list[RolePermission]` 92-row;R6 #4 — F2 canonical per-cell shape,F9 frontend pivot by area + role;custom roles Tier 2 per H4 — read-only,無 write path)
  - NEW `backend/api/routes/roles.py`(R6 #2 — `prefix="/roles"` 獨立 module,非 `routes/users.py` sub-path;plan §1「`routes/users/` package」係 W22 D9 plan-text contamination,F4 已 deviate single module)+ router-level `require_role("admin")`(R6 #6 — F3.4 per-endpoint apply 持續)
  - `app.state.rbac_backend` lifespan-wired + `seed_defaults()`(R6 #1 — F2 建咗 `make_rbac_backend` factory 但 `server.py` lifespan 未 wire;InMemory restart-wipe → startup seed mandatory)
- **Effort estimate**:~2 days

### F6 — `/users` Groups tab backend + sync-from-entra

- **Component(s)**:**C16** Users Service + **C08** API Gateway(`entra_graph.py` transport co-located 喺 `api/auth/` C11 identity-infra)
- **Spec ref**:ADR-0027 §Context Groups tab + §Decision `POST /groups/sync-from-entra` + mockup `ekp-page-users.jsx` lines 288-322(`GroupsTab`)+ F1 D1 managed-REST decision
- **OQ deps**:Q11(Entra ID tenant — non-blocking,mock-auth default → `sync-from-entra` graceful-degraded)
- **Acceptance criteria(refined at active-flip Day 6)**:
  - F6.1 — `GET /groups` 返回 `GroupListResponse{groups,total}`(`Group{group_key,name,description,source,entra_object_id,synced_at,member_count}`,ordered by name);`member_count` backend-computed from `group_members`(R6 #4 — 直屬 child table LEFT JOIN,非 cross-domain;F6 值 0 — member sync defer per F6 R6 #6);`EKP role` column = F9 client-side join from `admin_identity.RoleMappingConfig`(R6 #3 — group→role mapping separate concern,§13 backend-subset)
  - F6.2 — `POST /groups/sync-from-entra` 返回 `GroupSyncResult{status,synced_count,detail}`;managed-REST(R6 #5/#7 — F1 D1 decision,NOT `msgraph-sdk`):NEW `api/auth/entra_graph.py` 用 `azure-identity` `DefaultAzureCredential`(mirror `storage/azure_key_vault.py`)acquire app token + `httpx` `GET graph.microsoft.com/v1.0/groups`(`@odata.nextLink` pagination);`azure_tenant_id` unset → graceful `status="skipped"`(non-500);Graph failure → 502
  - `groups` table 加 `synced_at TIMESTAMPTZ` — additive `ALTER TABLE … ADD COLUMN IF NOT EXISTS`(R6 #2 — F4 `users.status` precedent;W24c/ADR-0027 RBAC scope 內,R3-changelog deviation 非 H1;F2 docstring「never a migration」stale claim 更新)
  - NEW `routes/groups.py`(`prefix="/groups"`,router-level `require_role("admin")`)+ `RbacBackend` Protocol +2(`list_groups`/`upsert_entra_group`)+ InMemory + Postgres impl
  - **🚧 deferred**:group **member** sync from Entra(Graph `/groups/{id}/members` + Entra-oid↔EKP-user matching)→ W24d+/F8(R6 #6 — F6 sync group **list** only per Karpathy §1.2);`sync-from-entra` 不加 `AuditAction`(R6 #7 — mockup AuditTab 無 group event)
- **Effort estimate**:~2.5 days

### F7 — Audit log expansion

- **Component(s)**:**C08** API Gateway + **C02** KB Manager(`kb.config.changed` write site)
- **Spec ref**:ADR-0027 §Context Audit log tab(action types + 90d retention)+ mockup `ekp-page-users.jsx` lines 324-377(`AuditTab`)— EXTENDS the existing `audit_log` table(W24-c1 F4 + W24b F6 filter/pagination)
- **OQ deps**:none
- **Acceptance criteria(refined at active-flip Day 7)**:
  - F7.1 — `AuditAction` Literal +2(`kb.access.granted` + `kb.config.changed`)— R6 #1:F4 D4.5 已加 `user.invited`/`user.suspended`/`role.changed`,F7 只加剩餘 `kb.*`(`audit_log.py` comment「F7 adds the kb.* actions」已預告)
  - F7.2 — `kb.config.changed` audit write wired on `PATCH /kb/{kb_id}/settings`(`update_kb_settings`)— R6 #3:endpoint 加 `request: Request`(拿 `app.state.audit_log_backend`),**不加 `Depends(get_current_user)`**(會 break pre-existing `test_kb_metadata_patch.py` minimal-app test;Karpathy §1.3 surgical)→ `actor=None`(對齊 F3 `_audit_identity_patch` + `audit_log.py` schema doc「actor extraction = middleware-level Wave C2+」);payload = `config.model_dump(mode="json")`(R6 #6 — new KbConfig snapshot,對齊 F2/F3/F4 audit payload pattern);audit best-effort skip-when-unwired(mirrors F4 `_audit`);只 wire `update_kb_settings`(KbConfig),**不** wire `update_kb_metadata`(R6 #4 — name/desc 非 `kb.config.changed` 語義範圍)
  - F7.3 — 90d retention:`AuditLogBackend` Protocol +`prune_expired(retention_days: int = 90) -> int` + InMemory + Postgres impl(`DELETE … WHERE created_at < NOW() - make_interval(days => %s)`)+ `server.py` lifespan startup call `prune_expired(90)`(R6 #5 — best-effort retention,Tier 1 無 scheduler → restart/deploy 時 prune;90d hard-code per plan/mockup,不加 settings field per Karpathy §1.2)
  - **🚧 deferred**:`kb.access.granted` audit **write** → F8(連 `kb_acl` CRUD endpoint;R6 #2 — F7.1 只加 literal,F8 wire write,per F4 D4.5 sequencing)
- **Effort estimate**:~2 days

### F8 — per-KB ACL(`kb_acl`)

- **Component(s)**:**C16** Users Service + **C08** API Gateway
- **Spec ref**:ADR-0027 §Decision `kb_acl` table + ADR-0025 Access tab + mockup `ekp-page-users.jsx` lines 389-519(`TabKbAccess`)
- **OQ deps**:none
- **Acceptance criteria(refined at active-flip Day 8)**:
  - F8.1 — `kb_acl` CRUD:NEW `routes/kb_acl.py`(R6 #6 — 獨立 module,不塞入已大 `kb.py`)4 endpoints `GET`/`POST`/`PATCH`/`DELETE` `/kb/{kb_id}/acl`;`POST` add member/group grant(`KbAclGrantRequest{principal_type,principal_id,access_role}`,upsert on `UNIQUE (kb_id,principal_type,principal_id)`)+ 寫 `kb.access.granted` audit(R6 #7 — actor = `current_user.preferred_username`,NEW route 可攞 `current_user`);`PATCH /{entry_id}` role override(`manage`/`edit`/`query`);`DELETE /{entry_id}` revoke;`GET` 返回 explicit `kb_acl` grants only(R6 #3 — synthetic system/inherited rows + workspace-role join = F10 frontend,§13 backend-subset);`kb_acl` 加 `granted_by` column(R6 #2 — additive ALTER,F6 `synced_at` precedent)
  - F8.2 — `acl.py` 加 `require_kb_acl(min_role)` dependency factory(R6 #5 — workspace `admin` always-pass per ADR-0027;else direct user `kb_acl` grant ≥ `min_role`,role-rank `manage>edit>query`);apply 到 `kb_acl` CRUD router(`require_kb_acl("manage")` — mockup「Manager can edit access list」);`RbacBackend` Protocol +`list_kb_acl`/`add_kb_acl`/`set_kb_acl_role`/`remove_kb_acl`/`get_kb_access` + InMemory + Postgres impl
  - **🚧 deferred**:KB **Visibility**(private/workspace/public — R6 #4,KB-level setting 非 `kb_acl`,C02 metadata → F10 surface / W24d+);**group-inherited** per-KB access resolution(R6 #5 — 需 F6 `group_members` member sync,F8 `get_kb_access` 只 direct user grant);其他 KB endpoint `require_kb_acl` retrofit(plan §4 R-W24c-3 opt-in,blast radius)
- **Effort estimate**:~2.5 days

### F9 — frontend `/users` 4-tab page + `useRole()` hook

- **Component(s)**:**C09** Admin Console UI + **C11** Identity & Access + **C16** Users Service(`GET /auth/me`)
- **Spec ref**:ADR-0027 §Context 4 tabs + mockup `ekp-page-users.jsx` PageUsers / UsersTab / RolesTab / GroupsTab / AuditTab;CLAUDE.md §5.7 H7 — 100% mockup fidelity
- **OQ deps**:none
- **Sub-split(R6 Day 9 — F9 ~3 plan days,NET NEW 4-tab route,sub-split per plan §2 + §7 R3)**:
  - **F9.1 — Foundation**:backend NEW `GET /auth/me`(current user + `role`,`auth.py`,`Depends(get_current_user)` non-admin — R6 #2/#6:frontend `AuthenticatedUser` 無 `role`,backend 無 current-user endpoint;`useRole()` data source)+ frontend NEW `lib/api/users.ts`(`/users` + `/roles` + `/groups` client methods + TS types mirror F4-F6 schemas — R6 #3)+ `useRole()` hook(fetch `/auth/me`,returns `EkpRoleKey`;`EkpRoleKey`+`EKP_ROLE_LABELS` 重用 `lib/api/admin.ts` per R6 #4)
  - **F9.2 — `/users` route shell + Members tab**:`app/(app)/users/page.tsx` NET NEW(`'use client'` + `<Suspense>` + 4-tab nav `.tabs`/`.tab` + `?tab=` deep-link per settings precedent R6 #5 + `<TabBoundary>` per tab)+ Members tab(`UsersTab` — stat-grid + filter seg + 10-col table + invite/suspend/role-change per mockup `ekp-page-users.jsx` lines 62-191)
  - **F9.3 — Roles tab + Groups tab**:`RolesTab`(banner + 4 role cards + permissions matrix per mockup lines 209-286)+ `GroupsTab`(Entra ID groups table + sync button per mockup lines 288-322)
  - **F9.4 — Audit tab + role-gating + verify**:`AuditTab`(workspace audit feed,重用 `adminApi.listAuditLog()` per mockup lines 324-377)+ `useRole()` role-gated view rendering wired + H7 per-tab fidelity 7-item self-verify(4 tabs)+ Vitest + Playwright;runtime browser smoke per plan §3 PARTIAL PASS(smoke-user-deferred pattern)
- **Acceptance criteria**:`/users` NET NEW route 4 tabs 100% mockup-faithful + `useRole()` + role-gated rendering;每 tab H7 7-item self-verify pass
- **Effort estimate**:~3 days(F9.1 ~0.5 + F9.2 ~1 + F9.3 ~1 + F9.4 ~0.5)

### F10 — frontend `/kb/[id]` Access tab activation

- **Component(s)**:C09 + C02
- **Spec ref**:ADR-0025 Access tab(disabled affordance Wave A → active W24c)+ ADR-0027 `TabKbAccess`(mockup lines 390-519)
- **Acceptance criteria(sketch)**:`/kb/[id]` 8th tab Access activated(disabled affordance removed)+ `<TabKbAccess>` per-KB ACL UI;H7 fidelity verify
- **Effort estimate**:~1.5 days

### F11 — Tests

- **Component(s)**:all touched
- **Spec ref**:CLAUDE.md §5.6 H6 — ACL/RBAC backend modules ≥80% coverage(`acl.py` middleware is protected-endpoint-critical)
- **Acceptance criteria(sketch)**:backend pytest(RBAC storage + ACL middleware + endpoints)+ Vitest(`/users` tabs + `useRole`)+ Playwright(`/users` render-smoke + Access tab)
- **Effort estimate**:~2 days

### F12 — Closeout cascade

- **Component(s)**:governance
- **Spec ref**:CLAUDE.md §10 R3 + W19-W24b closeout pattern
- **Acceptance criteria(sketch)**:Phase Gate verdict + 7-section retro + frontmatter `active→closed` + session-start.md 6 places + COMPONENT_CATALOG C16/C11/C08/C02 + PAGE_INVENTORY `/users` row 12 + `/kb/[id]` Access tab + ADR-0027 + ADR-0025 Implementation Status + W24d+ NOT pre-created
- **Effort estimate**:0.5 day

---

## §3 Success criteria + Gate criteria

Phase Gate **PASS** requires:

1. **All F0-F12 `[x]` complete**(~70+ atomic items — large phase)
2. **Entra Graph SDK landed**(Plan B (a) or (c)fallback per ADR-0017)
3. **5 NEW Postgres tables** + `users.role` column — idempotent migration,InMemory fallback
4. **ACL middleware working** — every protected endpoint role-checked;mock-auth `role:'admin'`;403 on unauthorized
5. **`/users` 4 tabs working** — Members(list+invite+suspend+role-change)+ Roles(matrix)+ Groups(sync)+ Audit log(feed)
6. **per-KB ACL working** — `kb_acl` CRUD + `/kb/[id]` Access tab activated per ADR-0025
7. **Audit log writes** — RBAC action types written on protected-endpoint mutations
8. **H7 fidelity preserved** — `/users` 4 tabs + Access tab 100% mockup-faithful per `ekp-page-users.jsx`
9. **H4 boundary** — Power User role + custom roles disabled affordance(NOT implemented)
10. **No regression** — backend pytest(W24b baseline 816)+ Vitest + verify gates(`tsc` / `lint` / `[oklch`=0 / `mypy --strict` on RBAC modules ≥80%)

**PARTIAL PASS allowance**:
- Entra Graph SDK R8 install fail through Plan B (c) → PARTIAL PASS + F6 `sync-from-entra` ship as graceful-degraded stub + F6 Entra-bound runtime smoke defer(CO17 umbrella precedent — W24-c1 Key Vault pattern)
- Real-MSAL role extraction runtime verify = user pre-Beta smoke(mock-auth default per user 岔口 2)— same smoke-user-deferred pattern as W18/W20/W24-c1/W24b
- F-deliverable scope explosion → sub-split per R3 changelog,not scope-cut

---

## §4 Risks

| Risk | Mitigation | Status |
|---|---|---|
| **R-W24c-1** Entra Graph SDK R8 install fail | Plan B (c) mobile hotspot per ADR-0017(3 realized precedents:Playwright / Azurite / Langfuse / Key Vault)+ amendment occurrence #9 | 🟡 active |
| **R-W24c-2** ~20 backend days = largest W-series phase,scope explosion | Rolling JIT F-deliverable sub-split at active-flip per R3;F0+F1 detailed,F2-F12 sketched | 🟡 active |
| **R-W24c-3** ACL middleware on every protected endpoint = broad blast radius | H6 ≥80% coverage on `acl.py`;additive `@requires_role` decorator(opt-in per endpoint,no silent global gate);mock-auth `role:'admin'` keeps dev unblocked | 🟡 active |
| **R-W24c-4** `audit_log` table double-ownership(W24-c1 ADR-0026 + W24c ADR-0027) | F7 EXTENDS the existing table additively(`AuditAction` Literal append)— no schema break;W24b F6 filter/pagination preserved | 🟢 mitigated |
| **R-W24c-5** H7 fidelity drift on NET NEW `/users` 4-tab surface | Per-tab H7 7-item self-verify + user-eye verify per F9;mockup `ekp-page-users.jsx` line-ref alignment | 🟡 active |
| **R-W24c-6** real-MSAL role extraction untestable without Track A IT cred | mock-auth `role:'admin'` default(per user 岔口 2);real-MSAL runtime verify = user pre-Beta smoke | 🟢 mitigated |
| **R8 corp-proxy** | Plan B (a) attempt + Plan B (c) hotspot fallback per ADR-0017 | 🟢 mitigated |

---

## §5 Component-Catalog references

| Cn | Touch | Scope |
|---|---|---|
| **C16** Users Service(NEW — or C11 expansion,F1 decision)| RBAC core | F2-F8 RBAC schema + ACL + Members/Roles/Groups/Audit/per-KB ACL |
| **C11** Identity & Access | role claim + ACL | F1 spec amendment + F3 auth-time role claim;C16 host decision F1.3 |
| **C08** API Gateway | NEW `/users/*` + `/groups/*` + `kb_acl` routes + ACL middleware | F3-F8 endpoints |
| **C09** Admin Console UI | NEW `/users` 4-tab route + Access tab | F9 + F10 |
| **C02** KB Manager | per-KB ACL consumer | F8 `kb_acl` + F10 Access tab |
| **C12** DevOps & Infra | Entra Graph SDK NEW dep | F1 install |
| **C01/C03-C07/C10/C13** | _no touch_ | preserved unchanged |

---

## §6 Carry-overs to W24d+ / rolling JIT

- **W24d+ candidates**(per W24c retro):
  - Tier 2 RBAC expansion(custom roles + Power User activation + multi-tenancy)— Q12 post-Beta governance
  - Connections deployment cap edit(Wave B+ Azure portal authoritative)
  - real-MSAL feature flag operational verification + W16 F1-F4 Track A IT cred(parallel track,Q11 early June 2026)
  - `PAGE_INVENTORY.md` row 6 `/doc-detail` staleness doc-sync(W24b F8 R6 finding carry-over)
- **W24d+ NOT pre-created** per CLAUDE.md §10 R1 rolling JIT
- **CO17 R8 umbrella** — F1.5b psycopg + F3.5b RAGAs live-verify + W24c Entra Graph SDK runtime smoke 仍 external-blocked

---

## §7 Changelog

| Day | Change | Reason |
|---|---|---|
| Day 0 2026-05-21 | Plan kickoff cascade — frontmatter / §0-§7 全部 landed `status: active` + Chris kickoff directive。**Pre-active-flip R6 audit finding**:ADR-0027 §Decision「6 NEW Postgres tables」中 `audit_log` **已存在**(W24-c1 F4 created + W24b F6 filter/pagination extended)→ W24c F2 實際 = **5 NEW tables**(`roles` + `role_permissions` + `groups` + `group_members` + `kb_acl`),`audit_log` 由 F7 **EXTEND**(additive `AuditAction` Literal append)非 create — plan §2 F2 + F7 已 reflect。**C16 vs C11 decision** deferred to F1.3(ADR-0027 leaves it open)。**Entra Graph SDK H2** pre-cleared via ADR-0027 acceptance(W19 F6)— F1 install per ADR-0017 Plan B,no fresh stop-and-ask。 | per CLAUDE.md §10 R1 + R6 + ADR-0027 §Decision Option A |
| Day 1 2026-05-21 | **F1 active-flip — pre-active-flip R6 audit surfaced 2 findings + 1 decision**(per CLAUDE.md §10 R6,讀 `architecture.md §3.7` + §5 + `pyproject.toml` + COMPONENT_CATALOG):**(1)** ADR-0027 §Decision「amend `architecture.md v6 §3.7` + add §3.8 /users」嘅 **§-pointer 錯** — `§3.7` 實為「C13 Email Verification Service」,`§3.8` 不存在(§3 之後即 §4);`/users` 屬 UI view → **Adjust** F1.1-F1.2 amendment 落 **§5**(UI Specifications),用 `> **Amendment**` inline block,對齊 ADR-0024(§5.0 line 752)/ ADR-0026(§5.0 line 754)/ ADR-0025(§5.5 line 887)3 個 §5-inline-amendment precedent — plan-text-contamination per W22 D9 anti-pattern,R6 auto-adjust(established convention 明確,不需 user escalate);**(2)** **Entra Graph SDK fork** — ADR-0027 寫 `sync-from-entra` 用 Entra Graph SDK(明標 new dep / H2 / R8 risk),但 R6 grep `pyproject.toml` 發現 `azure-identity>=1.20` + `httpx>=0.27` **W24-c1 已裝** → `sync-from-entra` 本質 = `GET graph.microsoft.com/v1.0/groups` REST call → **Chris AskUserQuestion 2026-05-21 pick managed-REST**(零新 dep / 零 H2 / 零 R8,對齊 ADR-0017 §Decision-rule「managed-REST > heavy SDK」)→ **Adjust** F1.4 install 變 **no-op**(deps 已在),F1.5 無 `pyproject.toml` change;**(3)** **C16 vs C11 decision**(F1.3)— ADR-0027 Option A ~20 backend days + 5 NEW tables + ACL middleware + distinct **authorization** concern(vs C11 **authentication**)→ pick **C16 NEW Users Service** component(over「fold into C11」)— COMPONENT_CATALOG C16 card landed。F1 ship:`architecture.md v6 §5.0` ADR-0027 inline-amendment block + COMPONENT_CATALOG C16 card;**no backend code change**(F1 = spec + component-registry only — `entra_graph.py` managed-REST module 落 F6)| Karpathy §1.1 think-before-coding + §1.2 avoid-speculative-dep + ADR-0017 managed-REST discipline;F1 zero new dependency |
| Day 2 2026-05-21 | **F2 active-flip — pre-active-flip R6 audit surfaced 3 findings**(讀 mockup `ekp-page-users.jsx` lines 26-60 + `storage/{audit_log_storage,audit_log_postgres,audit_log_factory}.py` + `api/auth/{users_store,postgres_users_store,users_repo}.py` + `kb_management/postgres_backend.py` ALTER pattern):**(1)** plan §2/checklist F2.4 寫「PERMISSIONS_MATRIX 5 areas × **24** permissions」,實際 mockup = **23**(KB 6 + Documents 3 + Chat 6 + Eval 3 + Platform config 5)— plan-text-contamination per W22 D9 anti-pattern class → **Adjust** plan §2 F2.4 + checklist F2.4「24→23」,R6 auto-adjust(pure count error,convention 明確不需 escalate);**(2)** F2.2「`users.role` column ADD」字面只述 schema migration,但只加 DB column 而不同步 `UserRecord.role` field + `PostgresUsersStore` 的 `_USER_COLS`/`_row_to_user`/`add_user`/`replace_user` = dead schema(永遠 default、讀不出)→ **Adjust** F2.2 acceptance 補完 4-處 surface;**(3)** F2 Protocol scope decision — `groups`/`group_members`/`kb_acl` 的 read/write Protocol method F2 未需要 → **Adjust**:F2.1 Postgres `_ensure_schema` 一次建 5 table(declared-ahead,F6/F8 不再 migrate),InMemory + Protocol F2 只暴露 `roles`+`role_permissions`(F5 backing),groups/members/acl method 留 F6/F8 per Karpathy §1.2 不寫 speculative。**F2 ship**:4 NEW files(`api/schemas/rbac.py` Role+RolePermission models + `storage/rbac_storage.py` RbacBackend Protocol+InMemory+`_PERMISSION_MATRIX` 23-perm seed constant + `storage/rbac_postgres.py` 5-table PostgresRbacBackend + `storage/rbac_factory.py`)+ 2 EDIT(`users_store.py` `UserRecord.role` + `postgres_users_store.py` `users` table `role` column + ALTER + 4-處同步)+ 1 NEW test(`tests/storage/test_rbac_storage.py` 12 cases)。`seed_defaults` idempotent → 4 roles + 92 role_permission rows。**Verify**:backend pytest **828 passed**(W24b 816 +12)+ 11 skipped + 0 failed;mypy `--strict` — rbac non-postgres 模組 0 error,`rbac_postgres.py` 唯一 error = `psycopg` import-not-found(CO17 R8 — psycopg 未裝,與 `audit_log_postgres.py`/`postgres_users_store.py` 同類豁免,非 F2 引入)。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + code)+ ADR-0027 §Decision Option A;Karpathy §1.2 no speculative surface |
| Day 3 2026-05-21 | **F3 active-flip — pre-active-flip R6 audit surfaced 5 findings**(讀 `api/auth/{dependency,models,mock_msal,msal_provider}.py` + `api/middleware/rate_limit.py` + `api/schemas/admin_identity.py` + `storage/admin_identity_storage.py` + `api/routes/admin/identity.py` + frontend `admin.ts`/`identity.ts`/`settings-identity.tsx`):**(1)** `AuthenticatedUser` 無 `role` field — F3.2「role carried in session/cookie」措辭 misleading,role 係 server-side resolve(非存 cookie)→ **Adjust** F3.2 = `AuthenticatedUser` 加 `role` + 三路徑 populate;**(2)** `@requires_role`/`@requires_kb_acl`「decorator」— FastAPI 慣例係 `Depends()` dependency factory(既有 `get_current_user` 全用 `Annotated[..., Depends]`)→ **Adjust** F3.1 交付 `require_role(*roles)` factory;**(3)** `@requires_kb_acl` backing(`kb_acl` storage method)F2 D2.2 已 defer F8 → **Adjust** F3 只交付 `require_role`,`require_kb_acl` 連 `kb_acl` method F8 一齊(Karpathy §1.2 no stub);**(4)** checklist F3.4「every protected endpoint role-checked」— F3 時點 `/users/*`(F4-F6)未建 → **Adjust** F3.4 = `require_role` mechanism + 403 contract test-verified,per-endpoint apply F4-F10 inline(plan §4 R-W24c-3「opt-in」);**(5)** **role key vocabulary 衝突** — W24-c1 `admin_identity.EkpRoleKey` long-form(`workspace_admin` etc.,Option-B-era)vs F2 RBAC-core + mockup short-form,17 files 散落 → **STOP + Chris AskUserQuestion 2026-05-21** pick「統一 short form(改 admin_identity)」。**F3 ship(2 commits)**:**C1 F3.0** vocabulary 統一 — 9 files long→short(`admin_identity` import `rbac.RoleKey`;`settings-identity.tsx` role 顯示 `.replace('_',' ')` → NEW `EKP_ROLE_LABELS` map 修 H7 drift — mockup line 664「Workspace Admin」title-case,既有 implementation 顯示 lowercase「workspace admin」)+ 2 frontend test mock 改 `importOriginal` partial-mock;**C2 F3.1-F3.5** — NEW `api/middleware/acl.py` `require_role(*roles)` dependency factory(chains `get_current_user`,403 on under-privileged)+ `AuthenticatedUser.role` field + 三路徑 populate(`resolve_session`→`UserRecord.role` / `authenticate_mock`→`Settings.auth_mock_role` NEW default `admin` / `authenticate_msal`→`_role_from_claims` Entra app-role claim,Tier-1-grantable `{admin,editor,user}` + `power` downgrade per H4)+ NEW `tests/api/test_acl_middleware.py` 11 cases。**Verify**:backend pytest **839 passed**(F2 828 +11)+ 11 skipped + 0 failed;mypy `--strict` F3 modules 0 error(`acl.py` clean;jose/psycopg pre-existing 豁免);ruff F3-introduced files clean(`admin_identity_storage.py:39` UP017 + `test_admin_identity.py:8` I001 = pre-existing W24-c1 lint,F3 未修 per Karpathy §1.3 surgical);frontend `tsc` exit 0 + `next lint` clean + Vitest 29 passed + `[oklch`=0。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + code)+ ADR-0027 §Decision Option A;Karpathy §1.1 think-before-coding + §1.2 no speculative |
| Day 4 2026-05-21 | **F4 active-flip — pre-active-flip R6 audit surfaced 6 findings**(讀 mockup `ekp-page-users.jsx` Members tab + `conversations.py` route pattern + `users_store`/`users_repo` + `server.py` route registration):**(1)** mockup `MOCK_USERS` 有 `queries_7d`/`kbs_owned`/`last_login`/`source`/`group` — Tier 1 backend 完全無 track(per-user query volume 需 query log Q6 open,KB ownership 需 ownership model)→ **Adjust** `GET /users` response = backend subset(`oid`/`email`/`display_name`/`role`/`status`/`created_at`),§13 backend wins on field shape;**(2)** mockup status 4 態(active/pending/invited/suspended)— `UserRecord` 只有 `verified: bool` → **Adjust** `UserRecord` 加 `status` field(active/invited/suspended),`pending` = `not verified` derive;**(3)** invite flow — `UserRecord` 無 invite 概念 → **Adjust** invite 建 `status="invited"` record(unusable random password),**invite email + accept flow 🚧 defer**(C13 territory,完整 accept flow 牽涉 `register` flow 改動);**(4)** `UsersStore` Protocol 無 `list_users` → **Adjust** 加 `list_users()` Protocol method + InMemory + Postgres;**(5)** `/users` route NET NEW → NEW `routes/users.py` + router-level `require_role("admin")`(F3.4「per-endpoint apply F4-F10」首次兌現);**(6)** F4.3 audit write 需 `AuditAction` 有 `user.*`/`role.changed` — plan F7 才 extend `AuditAction` → **Adjust** F4 加 3 個(`user.invited`/`user.suspended`/`role.changed`),F7 加剩餘 `kb.*` + retention。**F4 ship**:2 EDIT(`users_store.py` `UserRecord.status` + `UsersStore.list_users` Protocol/InMemory / `postgres_users_store.py` `status` column + ALTER + 4-處同步 + `list_users`)+ `users_repo.py` 4 NEW management functions(`list_users`/`invite_user`/`set_user_status`/`set_user_role`)+ `audit_log.py` `AuditAction` +3 + 2 NEW(`api/schemas/user.py` UserSummary/UserListResponse/InviteRequest/RoleChangeRequest;`api/routes/users.py` 4 endpoints)+ `server.py` route register + 1 NEW test(`tests/api/test_users_route.py` 15 cases)。**Verify**:backend pytest **854 passed**(F3 839 +15)+ 11 skipped + 0 failed;mypy `--strict` F4 modules 0 error(`routes/users.py`/`schemas/user.py`/`users_repo.py`/`users_store.py` clean;jose/psycopg pre-existing 豁免);ruff F4 NEW files all clean(`server.py` 28 E402 = pre-existing truststore-import baseline,`git show HEAD` 確認 F4 前已 28,per Karpathy §1.3 未順手修)。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + code)+ ADR-0027 §Context Members tab;§13 backend-wins;Karpathy §1.2 no speculative |
| Day 5 2026-05-21 | **F5 active-flip — pre-active-flip R6 audit surfaced 6 findings**(讀 mockup `ekp-page-users.jsx` lines 19-286 `ROLES`+`PERMISSIONS_MATRIX`+`RolesTab` + `api/schemas/rbac.py` + `storage/rbac_storage.py`+`rbac_factory.py` + `server.py` lifespan + `routes/users.py` F4 router):**(1)** `server.py` lifespan wire 咗 key_vault/admin_provider/admin_identity/audit_log backend,**無 `app.state.rbac_backend`** — F2 建咗 `make_rbac_backend` factory 但無 caller → **Adjust** F5 lifespan 加 `app.state.rbac_backend = make_rbac_backend(settings)` + `await …seed_defaults()`(`InMemoryRbacBackend` restart-wipe + `seed_defaults` async → startup seed mandatory,否則 `list_roles` 空);**(2)** plan §1 F5 spec-ref column 寫「`routes/users/` package」,plan §2 寫 literal path `GET /roles`(無 `/users` prefix);F4 actual = `routes/users.py` single module router `prefix="/users"`,加 `/roles` 入該 router → path 變 `/users/roles` 偏離 plan literal → **Adjust** F5 = NEW `routes/roles.py` `prefix="/roles"` 獨立 module(roles/permissions 係 RBAC resource 非 users sub-resource;mockup tab grouping ≠ backend path nesting;plan §1「package」係 W22 D9 plan-text contamination,F4 已 deviate single module,R6 auto-adjust);**(3)** mockup `RolesTab` line 225 role member count = `MOCK_USERS.filter(u=>u.role===key).length` 100% client-side,F2 `Role` schema 無 `member_count` → **Adjust** `GET /roles` 返回純 `Role` list,member count F9 frontend client-side(§13 backend wins on field shape + Karpathy §1.2 — backend join users count = speculative;mirrors F4 D4.6 filter-seg-client-side / D4.1 backend-subset);**(4)** mockup `PERMISSIONS_MATRIX` row shape = `{p,a,e,u,w}`(per-permission + 4 grants,area-grouped),F2 backend canonical = flat `RolePermission`(per-cell,92 row,有 `area` field)→ **Adjust** `GET /roles/permissions` 返回 flat `list[RolePermission]`(§13 backend wins on field shape — F2 `RolePermission` 已 lock canonical;F9 frontend pivot by `area` + per-permission 4-grant);**(5)** F5 response wrapper schema 放邊 → **Adjust** EDIT `api/schemas/rbac.py` 加 `RoleListResponse`+`PermissionMatrixResponse`(roles/permissions = RBAC schema natural home,非 F4 user-domain `user.py`);**(6)** F5 endpoint admin gate → router-level `require_role("admin")`(F3.4「per-endpoint apply F4-F10」持續兌現,一致 F4 router-level gate)。**F5 ship**:1 NEW(`api/routes/roles.py` 2 endpoints router-level `require_role("admin")`:`GET /roles` `RoleListResponse` + `GET /roles/permissions` `PermissionMatrixResponse`;`_get_rbac_backend` 503-on-unwired helper per `routes/admin/identity.py` precedent)+ 2 EDIT(`api/schemas/rbac.py` +2 response models;`server.py` lifespan `app.state.rbac_backend` wire+seed + `include_router(roles.router)`)+ 1 NEW test(`tests/api/test_roles_route.py`)。endpoint count 49 → **51**。全 6 findings = established convention / §13 backend-wins / Karpathy §1.2,R6 auto-adjust,無 STOP+ask escalate。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + code)+ ADR-0027 §Context Roles tab;§13 backend-wins;Karpathy §1.2 no speculative |
| Day 6 2026-05-21 | **F6 active-flip — pre-active-flip R6 audit surfaced 7 findings**(讀 mockup `ekp-page-users.jsx` lines 288-322 `GroupsTab` + `storage/rbac_postgres.py` `groups`/`group_members` schema + `storage/rbac_storage.py` Protocol + `api/schemas/admin_identity.py` `RoleMappingConfig` + `storage/settings.py` Entra config + `storage/azure_key_vault.py` `DefaultAzureCredential` pattern + `pyproject.toml` deps):**(1)** `Group` schema 0、`RbacBackend` group Protocol method 0 — F2 declared `groups`/`group_members`/`kb_acl` table 但 read/write surface 留 F6/F8 → F6 加 `Group` schema + `list_groups`/`upsert_entra_group` Protocol + InMemory + Postgres impl(F2 plan 已預告,non-deviation);**(2)** `groups` table **漏 `synced_at` column** — mockup `GroupsTab` 有 `Synced` column(per-group last-sync relative time),F2 `_CREATE_TABLES` `groups` schema(`group_key`/`name`/`description`/`source`/`entra_object_id`/`created_at`)無 `synced_at` → **Adjust** F6 加 `synced_at TIMESTAMPTZ` — additive `ALTER TABLE groups ADD COLUMN IF NOT EXISTS`(對齊 F4 `users.role`/`users.status` ALTER precedent;W24c/ADR-0027 RBAC schema scope 內,additive 非 breaking,R3-changelog deviation 非 H1 — F2 docstring「F6 add only Protocol methods, never a migration」stale claim 一併更新 per Karpathy §1.3 surgical);**(3)** mockup `GroupsTab` `EKP role` column — group→role mapping 喺 `admin_identity.RoleMappingConfig`(`RoleMapping{ekp_role,entra_group_name,entra_group_id}`,W24-c1 F3 已有 `GET /admin/identity` endpoint),`groups` table 無 `mapped_role` column → **Adjust** `GET /groups` 返回純 group(無 role mapping),F9 frontend client-side join from `GET /admin/identity`(mockup card-desc 明示「group → role mapping in Settings → Identity & Auth」= separate concern;§13 backend-subset;mirrors F4 D4.1 / F5 D5.3);**(4)** group `member_count` — `group_members` 係 `groups` 嘅**直屬 child table**(`PK (group_key, user_oid)`),count-by-group 係 group 自己 aggregate,非 cross-domain(對比 F5 D5.3 role member count 要 join `users` table → client-side)→ **Adjust** `member_count` backend-computed(Postgres `LEFT JOIN group_members … GROUP BY` / InMemory dict),F6 值 = 0(member sync defer per #6),欄位忠實 ship(`group_members` 係 real declared table,LEFT JOIN 係 real query,非 speculative);**(5)** `sync-from-entra` — backend 係 **resource-server-only**(`settings.py` 只有 `azure_tenant_id`+`azure_client_id`,**無 `client_secret`**)→ **Adjust** F1 D1 managed-REST decision 落實 = NEW `api/auth/entra_graph.py` 用 `azure-identity` `DefaultAzureCredential`(mirror `storage/azure_key_vault.py` lazy-import pattern;`pyproject.toml` 確認 `azure-identity>=1.20` + `httpx>=0.27` 已裝 — 零新 dep per F1 D1)acquire app token for `https://graph.microsoft.com/.default` + `httpx` `GET graph.microsoft.com/v1.0/groups`(`@odata.nextLink` pagination);`azure_tenant_id` unset(mock-auth dev)→ caller short-circuit graceful `status="skipped"`(non-500,never touches `azure-identity`);Graph failure → 502;config-set live path = deferred pre-Beta smoke(R-W24c-6 / CO17 umbrella);**(6)** group **member** sync — Graph `/groups/{id}/members` per-group enumeration + Entra-oid↔EKP-user(`users.oid`)matching 係 separate larger concern → **🚧 defer W24d+/F8**;F6 sync group **list** only(per Karpathy §1.2 no speculative);**(7)** plan §2 F6 字面「Entra Graph **SDK**」+ Component「C12」係 pre-F1 sketch contamination(W22 D9 anti-pattern class)— F1 D1 已 decide managed-REST(零新 dep,零 C12 install)→ **Adjust** F6 refine 用 managed-REST 字眼,Component = C16+C08;mockup `AuditTab`(lines 324-377)action 列表無 group event → `sync-from-entra` 不加 `AuditAction`、不寫 audit row(per Karpathy §1.2)。**F6 ship**:2 NEW(`api/auth/entra_graph.py` managed-REST Graph client + `EntraGroup` dataclass + `fetch_entra_groups` nextLink-paginated;`api/routes/groups.py` 2 endpoints router-level `require_role("admin")`)+ 4 EDIT(`api/schemas/rbac.py` +`Group`+`GroupListResponse`+`GroupSyncResult`;`storage/rbac_storage.py` Protocol +`list_groups`+`upsert_entra_group` + InMemory impl;`storage/rbac_postgres.py` `synced_at` column + ALTER + `list_groups` LEFT JOIN + `upsert_entra_group` ON CONFLICT + docstring;`server.py` import `groups` + `include_router`)+ 1 NEW test(`tests/api/test_groups_route.py`,mock `entra_graph.fetch_entra_groups`)。endpoint count 51 → **53**。全 7 findings = F2-predicted surface / additive-ALTER-F4-precedent / §13 backend-subset / Karpathy §1.2 no-speculative / plan-text-contamination,R6 auto-adjust,無 STOP+ask escalate。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + code)+ ADR-0027 §Context Groups tab + F1 D1 managed-REST;§13 backend-wins;Karpathy §1.2 no speculative |
| Day 7 2026-05-21 | **F7 active-flip — pre-active-flip R6 audit surfaced 6 findings**(讀 `api/schemas/audit_log.py` `AuditAction` Literal + `storage/audit_log_storage.py`/`audit_log_postgres.py` + `api/routes/kb.py` `update_kb_settings` + `api/routes/admin/audit_log.py` + mockup `ekp-page-users.jsx` lines 324-377 `AuditTab` + `tests/storage/test_audit_log.py`/`tests/test_kb_metadata_patch.py`):**(1)** F7.1 plan literal 列 5 個 action(`role.changed`/`user.invited`/`kb.access.granted`/`kb.config.changed`/`user.suspended`),其中 3 個(`user.invited`/`user.suspended`/`role.changed`)F4 D4.5 已加 → F7 實際只加 **2 個**(`kb.access.granted` + `kb.config.changed`)— non-deviation,`audit_log.py` comment「W24c F7 adds the kb.* actions」已預告;**(2)** `kb.access.granted` 嘅 write site = F8 `kb_acl` CRUD endpoint(per-KB ACL grant),F7 時點未建 → **Adjust** F7.1 加 `kb.access.granted` Literal,但 audit **write** 🚧 defer F8(連 `kb_acl` CRUD 一齊 — per F4 D4.5「F7/F8 sequencing」+ Karpathy §1.2 no write-without-endpoint);**(3)** `kb.config.changed` write site = `PATCH /kb/{kb_id}/settings`(`update_kb_settings`),現 signature `(kb_id, config, service)` 無 `Request`/`current_user` → **Adjust** F7.2:`update_kb_settings` 只加 `request: Request`(拿 `app.state.audit_log_backend`),**不加 `Depends(get_current_user)`** — 加 auth dependency 會令 pre-existing `test_kb_metadata_patch.py:151` minimal-app test(無 auth setup)收 401 regression → Karpathy §1.3 surgical 避免擴大 blast radius → audit `actor=None`,對齊 `routes/admin/identity.py` `_audit_identity_patch(actor=None)` 既有 pattern + `audit_log.py` schema doc 明示「actor ... Wave C2 promotes when ADR-0027 wires actor extraction at middleware level」;audit best-effort(`getattr(app.state, "audit_log_backend", None)` skip-when-unwired,mirrors F4 `_audit`);**(4)** `kb.config.changed` 只 wire `update_kb_settings`(`KbConfig` change)— **不** wire `update_kb_metadata`(`PATCH /kb/{kb_id}`,name/description)→ mockup `AuditTab` `kb.config.changed` event 係 `KbConfig` field(「default_top_k 50 → 30」),`kb.config.changed` 語義 = `KbConfig` change;name/desc 係 metadata 非 config → 不 over-extend(per Karpathy §1.2 + W22 D6 over-extending anti-pattern);**(5)** **90d retention 機制完全不存在** — `audit_log` table 無 prune column / 無 prune logic,Tier 1 無 background scheduler/cron → **Adjust** F7.3:`AuditLogBackend` Protocol 加 `prune_expired(retention_days: int = 90) -> int`(刪 `created_at < now - retention_days`,返回 deleted count)+ InMemory(list comprehension filter)+ Postgres(`DELETE … WHERE created_at < NOW() - make_interval(days => %s)`,`cur.rowcount`)impl;`server.py` lifespan startup call `await audit_log_backend.prune_expired(90)`(best-effort retention — Tier 1 無 cron,restart/deploy 時 prune,~90d window 足夠;90d hard-code per plan/mockup「90d retention」,不加 `settings` field per Karpathy §1.2 no speculative config);**(6)** `kb.config.changed` audit payload — mockup `AuditTab` 顯示「default_top_k 50 → 30」before→after diff,但既有 F2/F3/F4 audit payload pattern = 記 mutation content(非 diff;F4 `user.invited` payload = `{email,role}`,F3 `identity_patch` payload = sanitized PATCH 內容)→ **Adjust** F7.2 payload = `config.model_dump(mode="json")`(new `KbConfig` snapshot — `KbConfig` 無 secret;before/after diff 渲染係 frontend concern,§13 backend ships mutation payload)。**F7 ship**:5 EDIT(`api/schemas/audit_log.py` `AuditAction` +2;`storage/audit_log_storage.py` Protocol +`prune_expired` + InMemory impl;`storage/audit_log_postgres.py` `prune_expired` Postgres impl;`api/routes/kb.py` `update_kb_settings` +`request` + audit write;`api/server.py` lifespan startup `prune_expired(90)`)+ 2 test EDIT(`tests/storage/test_audit_log.py` +`prune_expired` cases;`tests/test_kb_metadata_patch.py` +`kb.config.changed` audit write test)。endpoint count 不變 53(F7 無新 endpoint)。全 6 findings = F4-sequenced / additive Literal / Karpathy §1.3 surgical(避 regression)/ §1.2 no-over-extend+no-speculative-config,R6 auto-adjust,無 STOP+ask escalate。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + code)+ ADR-0027 §Context Audit log tab;Karpathy §1.2 no speculative + §1.3 surgical |
| Day 8 2026-05-21 | **F8 active-flip — pre-active-flip R6 audit surfaced 8 findings**(讀 mockup `ekp-page-users.jsx` lines 389-519 `TabKbAccess` + `api/middleware/acl.py`(F3 `require_role`,docstring「`require_kb_acl` lands with F8」)+ `storage/rbac_storage.py`/`rbac_postgres.py`(`kb_acl` F2-declared schema)+ `api/auth/models.py`(`AuthenticatedUser.oid`)):**(1)** `kb_acl` Protocol method 0、`KbAclEntry` schema 0 — F2 declared `kb_acl` table 但 read/write surface 留 F8 → F8 加 schema + Protocol + InMemory + Postgres impl(F2 plan 已預告,non-deviation);**(2)** `kb_acl` table 漏 `granted_by` column — mockup `TabKbAccess` table 有「Granted by」column(`system`/`chris.lai`/`(group)`),F2 `kb_acl` schema(`id`/`kb_id`/`principal_type`/`principal_id`/`access_role`/`created_at`)無 → **Adjust** F8 加 `granted_by TEXT` — additive `ALTER TABLE kb_acl ADD COLUMN IF NOT EXISTS granted_by`(對齊 F6 `synced_at` ALTER precedent;W24c/ADR-0027 RBAC schema scope 內);**(3)** mockup `TabKbAccess` table row 唔全部係真 `kb_acl` row — `system`/auto-locked row(workspace admin 永遠 manage)+ `inherited` row(`granted_by:"(group)"`,user 透過 group membership 得 access)係 synthetic/derived,加上 `Workspace role` column 係 join → **Adjust** `GET /kb/{kb_id}/acl` 返回 `kb_acl` table 嘅 explicit grants only(`KbAclEntry` list);system/inherited synthetic rows + workspace-role join 喺 F10 frontend 渲染(§13 backend-subset;mirrors F4 D4.1 / F6 D6.2);**(4)** mockup `TabKbAccess` 有 **Visibility card**(private/workspace/public-Tier2-disabled)— 係 KB-level setting(邊個睇得到 KB),**唔係 `kb_acl`**(per-principal grant);plan F8 scope = `kb_acl` CRUD(per ADR-0027 §Decision `kb_acl` table)→ **Adjust** F8 = `kb_acl` only,KB **Visibility**(需 `KbStatus`/`KbConfig` enum field + endpoint)🚧 defer(C02 KB Manager metadata,非 F8 `kb_acl` scope;mockup Visibility card 喺 F10 frontend 渲染時 surface,或 W24d+ — per Karpathy §1.2 no over-extend);**(5)** `require_kb_acl` — F3 deferred 到 F8(`acl.py` docstring 明寫「lands with F8」);group-based per-KB access(user 透過 group membership 得 access)需要 `group_members` table membership data,但 F6 D6.5 已 defer group member sync → **Adjust** F8.2:`acl.py` 加 `require_kb_acl(min_role)` dependency factory(workspace `admin` always-pass per ADR-0027「Workspace Admins always have full access」;else direct user `kb_acl` grant,role-rank `manage>edit>query`);**group-inherited access resolution 🚧 defer**(連 F6 member sync — F8 `get_kb_access` 只 check direct `principal_type='user'` grant);`require_kb_acl` apply 到 `kb_acl` CRUD router 本身(`require_kb_acl("manage")` — mockup「Manager can edit access list itself」),其他既有 KB endpoint 大規模 retrofit `require_kb_acl` 🚧 defer(plan §4 R-W24c-3 opt-in,blast radius 大,mock-auth dev 全 admin → 不阻礙);**(6)** `kb_acl` route 放邊 — `/kb/{kb_id}/acl` 係 KB sub-resource,但 `kb.py`(C02 KB Manager)已大 + `kb_acl` 係 C16 per-KB ACL concern → **Adjust** NEW `routes/kb_acl.py`(獨立 module,router no-prefix paths `/kb/{kb_id}/acl`);NEW route 可攞 `Depends(get_current_user)`(無 pre-existing test regression — 對比 F7 `update_kb_settings` 係 pre-existing endpoint 不能加 auth dep → `actor=None`;F8 `kb_acl` route NEW → `kb.access.granted` 可寫真 actor `current_user.preferred_username`);**(7)** `kb.access.granted` audit write(F7 D7.2 deferred 到 F8)— `POST /kb/{kb_id}/acl`(add grant)寫 `kb.access.granted`(`AuditAction` literal F7.1 已加);`PATCH`(role override)/`DELETE`(revoke)無對應 mockup `AuditTab` action / 無 `AuditAction` literal → 不寫 audit(per Karpathy §1.2 — 唔加 mockup/plan 都冇嘅 `AuditAction`);**(8)** `access_role` valid values = `manage`/`edit`/`query`(mockup `<select>` 3 option)→ `KbAclRole` Literal + `KbPrincipalType` Literal(`user`/`group`);`require_kb_acl` 需要 role-rank ordering(`_KB_ACL_RANK = {query:1, edit:2, manage:3}`)。**F8 ship**:1 NEW(`api/routes/kb_acl.py` 4 CRUD endpoints router-level `require_kb_acl("manage")`)+ 5 EDIT(`api/schemas/rbac.py` +`KbAclRole`+`KbPrincipalType`+`KbAclEntry`+`KbAclListResponse`+`KbAclGrantRequest`+`KbAclRoleChangeRequest`;`storage/rbac_storage.py` Protocol +5 kb_acl methods + InMemory impl;`storage/rbac_postgres.py` `kb_acl` `granted_by` ALTER + 5 method Postgres impl + `_row_to_kb_acl`;`api/middleware/acl.py` +`require_kb_acl` + docstring 更新「lands with F8」stale claim;`api/server.py` import `kb_acl` + `include_router`)+ 1 NEW test(`tests/api/test_kb_acl_route.py`)+ 2 test EDIT(`tests/storage/test_rbac_storage.py` +kb_acl storage cases;`tests/api/test_acl_middleware.py` +`require_kb_acl` cases)。endpoint count 53 → **57**。全 8 findings = F2-predicted surface / additive-ALTER-F6-precedent / §13 backend-subset / Karpathy §1.2 no-over-extend+no-speculative / §1.3 surgical / F4-F7 sequencing,R6 auto-adjust,無 STOP+ask escalate。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + code)+ ADR-0027 §Decision `kb_acl` + ADR-0025 Access tab;§13 backend-subset;Karpathy §1.2 no speculative + §1.3 surgical |
| Day 13 2026-05-21 | **F10 active-flip — pre-active-flip R6 audit surfaced 10 findings**(讀 mockup `ekp-page-users.jsx` lines 390-519 `TabKbAccess` + `backend/api/routes/kb_acl.py`(F8 4 endpoints)+ `backend/api/schemas/rbac.py` `KbAcl*` schemas + `frontend/app/(app)/kb/[id]/page.tsx`(VALID_TABS / TAB_DEFS / Access disabled affordance / tab body switch)+ `lib/api/kb.ts` + `tests/unit/kb-detail-tabs.test.tsx` + `tests/unit/kb-detail.test.tsx`):**(1)** `/kb/[id]/page.tsx` Access tab 目前 = `<DisabledAffordance>` wrapped disabled `<button>` OUTSIDE `VALID_TABS`(W20 F5 / ADR-0027 Wave C1 — RBAC pending)。F8 `kb_acl` backend 已 land → F10 activate:`'access'` 加入 `VALID_TABS` + `TAB_DEFS`(icon `Shield`),移除 disabled affordance block,render `<TabKbAccess>` body;**(2)** 移除 disabled affordance 後 `Lock` import(只此一處用)+ `DisabledAffordance` import(file 內唯一 usage)變 orphan → 一併移除 + 更新 docstring 2 處 comment(Karpathy §1.3 清自己 mess);**(3)** `kb_acl` client — `/kb/{id}/acl` 係 `/kb/*` path → `kbApi.listAcl` 加入 `lib/api/kb.ts`(URL-prefix convention,1 method 唔開新 file);F10 ship **read-only `listAcl`** only — mockup `TabKbAccess` CRUD affordance(Add member / Add Entra group button、per-row KB-role `<select>` `defaultValue` 無 onChange、per-row More)**全部 presentational**(無 onClick/onChange)→ write client(POST/PATCH/DELETE)**不建**(Karpathy §1.2 no speculative — 無 caller;kb_acl CRUD UI 建嗰陣先加 write client);**(4)** `<TabKbAccess>` → NEW separate file `components/kb/tab-kb-access.tsx`(`/kb/[id]/page.tsx` ~2000 行;`TabKbAccess` mockup home 係 `ekp-page-users.jsx` 非 KB-detail mockup decomposition;隔離 component + kb_acl concern;import `RoleBadge` from `components/users/`);**(5)** stat-grid 4 card — 「Members with access」real(`entries.length` + by-role sub);「Visibility」/「Pending invites」/「Default new member access」= `—` placeholder(無 backend — F8 D8.4 KB Visibility deferred;W22 B-i);**(6)** Visibility card(3 radio row private/workspace/public)— F8 D8.4 deferred KB Visibility(KB-level setting 非 `kb_acl`,D8.4「mockup Visibility card 喺 F10 frontend 渲染時 surface」)→ F10 render card per mockup,radio **全部 `disabled`**(read-only — Tier 1 無 Visibility setting,「Workspace」係 de-facto state;§13 keep-element + W22 B-i);**(7)** Members table row — backend `GET /kb/{id}/acl` = explicit `kb_acl` grant only(F8 D8.3 — synthetic system/inherited row + workspace-role join = F10 frontend)→ F10 render 1 synthetic「Workspace Admins (auto)」system row(truthful — 反映 `require_kb_acl` admin-always-pass rule)+ explicit grants;**inherited row 🚧 skip**(需 group-member data,F6 D6.5 group member sync deferred);**(8)** Members table join — `KbAclEntry.principal_id`(user oid / group key)→ display name via `usersApi.listUsers()` / `listGroups()` join(graceful — fallback raw `principal_id` if join data unavailable);「Workspace role」column user row 經 `/users` join `RoleBadge`,group row `—`(group→workspace-role mapping 需 `/admin/identity` join — defer,§13 fallback);只 block on `kb_acl` query,users/groups best-effort;**(9)** CRUD affordance render inert per mockup(F9.2 D10.2 / F9.3 D11.4 precedent);footer「Manage all workspace members → /users」有 `onClick={onNavigate}` → wired(mockup 唯一 functional element)via `useRouter().push('/users')`;**(10)** `tests/unit/kb-detail-tabs.test.tsx` 斷言「7 active + Access disabled(`aria-disabled='true'`)」— F10 activate Access → test break → F10 **更新該 test**(「8 active tab triggers incl. Access」,移除 disabled 斷言;Karpathy §1.4 tests pass before+after,非 deferred to F11);`kb-detail.test.tsx` 無 Access 斷言(unaffected);Playwright `/kb/[id]` render-smoke(unseeded KB → error-banner path)不斷言 Access — unaffected。**F10 ship**:1 EDIT `lib/api/kb.ts`(+`KbAclRole`/`KbPrincipalType`/`KbAclEntry`/`KbAclListResponse` types + `kbApi.listAcl`)+ 1 NEW `components/kb/tab-kb-access.tsx`(`<TabKbAccess kbId>` — banner + 4 stat + Visibility card + Members table + footer nav)+ 1 EDIT `app/(app)/kb/[id]/page.tsx`(VALID_TABS + TAB_DEFS + 移除 disabled affordance + 移除 `Lock`/`DisabledAffordance` import + render `<TabKbAccess>` + docstring)+ 1 EDIT `tests/unit/kb-detail-tabs.test.tsx`(7→8 active tabs)。endpoint count 不變 58(F10 純 frontend,consume F8 既有 endpoint)。全 10 findings = F8-anticipated frontend surface(D8.3/D8.4)/ §13 visual-fidelity / W22 B-i placeholder / Karpathy §1.2 no-speculative + §1.3 surgical + §1.4 tests-pass / established precedent,R6 auto-adjust,無 STOP+ask escalate。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + frontend + backend route/schema)+ ADR-0025 Access tab + ADR-0027 §Decision `kb_acl` + CLAUDE.md §5.7 H7;§13 visual-fidelity;Karpathy §1.2 no speculative + §1.3 surgical |
| Day 12 2026-05-21 | **F9.4 active-flip — pre-active-flip R6 audit surfaced 10 findings**(讀 mockup `ekp-page-users.jsx` lines 324-377 `AuditTab` + `backend/api/schemas/audit_log.py` `AuditAction`/`AuditLogEntry`/`AuditLogPage` + `lib/api/admin.ts` `AuditAction`/`adminApi.listAuditLog` + `components/settings/settings-audit-log.tsx` render pattern + `lib/hooks/use-role.ts` + `tests/unit/settings-6tab.test.tsx` + `tests/e2e/app-shell-path.spec.ts` + `styles-mockup.css` `banner-warning`/`.row` confirm):**(1)** frontend `admin.ts` `AuditAction` Literal **stale** — 只有 5 個 W24-c1 action(`connection_*`/`identity_patch`/`api_keys_*`),backend `api/schemas/audit_log.py` `AuditAction` 已係 **10 個**(W24c F4 加 `user.invited`/`user.suspended`/`role.changed` + F7 加 `kb.access.granted`/`kb.config.changed`)→ **Adjust** F9.4 EDIT `admin.ts` extend `AuditAction` mirror backend 10 值(additive union,`SettingsAuditLog` 既有 `ACTION_OPTIONS` 6-element array 不 break — non-architectural correctness fix);**(2)** mockup `AuditTab` event shape `{at,actor,action,target,note}` vs backend `AuditLogEntry{id,actor,action,resource,payload,created_at}` — `target`→`resource`、`at`→`created_at`(`formatRelative`)、`note` **無 backend field** → per §13 v1.9 keep visual element,`payloadNote()` synthesize note from `payload` scalar entries(`k: v · k: v`,skip nested/null;backend 保證 payload 無 secret per `AuditLogEntry.payload` doc)→ note 空時 omit 第 3 行(per-entry fallback,row 結構不變);**(3)** mockup `actionIcon` 5-prefix branch(`role`→Shield / `user`→Users / `kb.access`→Link / `provider`→Cpu / `kb.config`→Pencil / else→Activity)— backend 無 `provider.*` action(W24-c1 用 `connection_*`),`connection_*`/`identity_patch`/`api_keys_*` 全 fall to `Activity` default → reproduce mockup `actionIcon` **verbatim**(含 dead `provider` branch — H7 mockup-faithful,Karpathy §1.2 唔 over-extend icon map);**(4)** mockup `AuditTab` 無 pagination / 無「Load more」(static feed)— backend `listAuditLog()` 返回 `AuditLogPage{entries,next_cursor}` default `limit=10` → F9.4 render `entries`,`next_cursor` 不用(無「Load more」per mockup;對比 `SettingsAuditLog` W24b 有 cursor pagination 係 *無 mockup* 嘅 functional surface);**(5)** mockup「Filter」/「Export」button presentational(無 onClick)→ render inert per mockup + F9.2 D10.2 / F9.3 D11.4 precedent;**(6)** `useRole()` role-gating — `/users` 係 Workspace-Admin-only(ADR-0027 permissions matrix `cfg.manage_users` = admin-only)→ whole-page gate:`UsersPageInner` 加 `useRole()`,`role===null`(loading)→ minimal loading state、`role!=='admin'`(editor/user/power)→ access-denied `banner banner-warning`、`role==='admin'`→ full page;shell `useQuery(['users','list'])` 加 `enabled: role==='admin'`(非 admin 唔 fetch admin data,避 403);access-denied state mockup 無 spec(prototype assume admin)→ minimal honest gate per Karpathy §1.2(同 loading/error state 一樣係 real-page 必需);**(7)** hooks ordering — `useRole`/`useSearchParams`/`useState`/`useQuery`/`useMemo`/`useCallback` 全部 unconditional 喺 early-return 之前 call,role-based early-return 喺所有 hook 之後(React hooks rule);**(8)** Vitest `users-page.test.tsx` — mock `next/navigation` + `@/lib/api/users`(`usersApi` 含 `getMe` for `useRole`)+ `@/lib/api/admin`(`adminApi.getIdentity`/`listAuditLog`)per `settings-6tab.test.tsx` established pattern;mutable `mockRole` var 測 admin(page render)vs non-admin(access-denied);**(9)** Playwright — 加 `/users` render-smoke 入 `app-shell-path.spec.ts`(per W24-c1 settings test precedent,非 new spec);heading「Users & access」+ 4 tab visible;runtime execution = smoke-user-deferred per plan §3;**(10)** `AuditTab` inline 喺 `page.tsx` + self-fetch `['admin','audit-log']`(consistent F9.2/F9.3 inline + self-fetch);F9.2 `audit` `<TabPlaceholder>` replaced — `TabPlaceholder` helper 全部 tab 用完 → 移除(F9.4 後無 caller,Karpathy §1.3 清自己 mess)。**F9.4 ship**:1 EDIT `admin.ts`(`AuditAction` 5→10)+ 1 EDIT `page.tsx`(imports + `audit` tab body swap + NEW `AuditTab` + `auditActionIcon`/`payloadNote` helpers + `useRole()` role-gating + remove `TabPlaceholder`)+ 1 NEW `tests/unit/users-page.test.tsx` + 1 EDIT `tests/e2e/app-shell-path.spec.ts`(+`/users` render-smoke)。endpoint count 不變 58(F9.4 純 frontend)。全 10 findings = stale-type correctness-fix / §13 visual-fidelity-synthesize / Karpathy §1.2 no-over-extend / established test pattern / hooks rule,R6 auto-adjust,無 STOP+ask escalate。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + frontend + backend schema)+ ADR-0027 §Context Audit log tab + CLAUDE.md §5.7 H7;§13 visual-fidelity;Karpathy §1.2 no speculative + §1.3 surgical |
| Day 11 2026-05-21 | **F9.3 active-flip — pre-active-flip R6 audit surfaced 10 findings**(讀 mockup `ekp-page-users.jsx` lines 209-322 `RolesTab`+`GroupsTab` + `backend/storage/rbac_storage.py` `_PERMISSION_MATRIX`/`_DEFAULT_ROLES` seed + `lib/api/admin.ts` `adminApi.getIdentity`/`IdentityConfig`/`RoleMapping` + `styles-mockup.css` `card-body-tight`/`banner-info`/`.col` confirm):**(1)** RolesTab role-card member count — backend `Role` schema 無 `member_count`(F5 D5.3 client-side);mockup count = `MOCK_USERS.filter(u=>u.role===key)` → RolesTab `useQuery(['users','list'])`(shared cache with shell — TanStack dedupes)+ client-side count;backend `_DEFAULT_ROLES` `description`/`label` 經 grep 確認 **verbatim-match** mockup `ROLES[].desc`/`EKP_ROLE_LABELS` → render `role.description` + `<RoleBadge>`,無 hardcoded role metadata;**(2)** permissions matrix pivot — F5 D5.4:`GET /roles/permissions` 返回 **flat** `list[RolePermission]`(92 row,`role_key`/`permission_key`/`area`/`label`/`granted`),backend `permission_matrix_rows()` order = area → permission → role(`_ROLE_ORDER` admin/editor/user/power),且 `_PERMISSION_MATRIX` seed verbatim-mirror mockup `PERMISSIONS_MATRIX` lines 26-60 → frontend `pivotMatrix()` first-seen accumulation = mockup area+perm order,**無需** explicit order constant;**(3)** RolesTab `isTier2` — mockup `key==='power'`;backend `Role.tier`(1/2)+`active` → `isTier2 = role.tier >= 2`(power tier=2 active=false → TIER 2 badge + opacity 0.6);**(4)** GroupsTab `EKP role` column — F6 D6.3:`GET /groups` 返回 pure `Group`(無 `mapped_role`),group→role mapping 喺 `admin_identity.RoleMappingConfig` → GroupsTab client-side join:`usersApi.listGroups()` + `adminApi.getIdentity()` → match `RoleMapping.entra_group_id === Group.entra_object_id` → `ekp_role` 或「Not mapped」;`getIdentity()` 係 `require_role("admin")`-gated — fine(`/users` page admin-scoped,F9.4 加 `useRole()` gating);**(5)** GroupsTab `Synced` column — backend `Group.synced_at: string|null`,mockup static「2m ago」→ local `formatRelative` helper(kb/dashboard/kb-id 每 page 自定 precedent),`null` → `—`;**(6)** GroupsTab `Object ID` — backend `Group.entra_object_id: string|null` full GUID,mockup truncate「a7f4…b1c4」(first4…last4,column-width fidelity)→ `truncateOid` helper,`null`(local group)→ `—`;**(7)** GroupsTab `Sync from Entra` button **inert** — mockup button presentational(無 onClick);`POST /groups/sync-from-entra` 返回 `GroupSyncResult{status,detail}` 其中 `status='skipped'` 係 mock-auth dev **default**(無 Entra tenant)→ 一個 wired button 需要 `synced/skipped/502` result-feedback surface 而 mockup 冇 → render inert per mockup + per F9.2 D10.2 precedent(mockup presentational action button → inert;needs surface not in mockup → 不 build per W22 D6);matrix-card「Export」button 同理 inert;`usersApi.syncGroupsFromEntra()` 保持 F9.1-shipped client surface;**(8)** RolesTab/GroupsTab inline 喺 `page.tsx` — consistent with F9.2 `UsersTab` inline + `kb/[id]/page.tsx` 8-tab-inline precedent;各自 self-fetch domain endpoint(RolesTab:`/roles`+`/roles/permissions`+`/users`;GroupsTab:`/groups`+`/admin/identity`),shell 不 pass 額外 prop;F9.2 `<TabPlaceholder>` for roles/groups replaced(`audit` 仍用 placeholder 至 F9.4);**(9)** `React.Fragment` with key — mockup matrix `<React.Fragment key={area}>` → TSX `import { Fragment }` + `<Fragment key>`(bare `<>` 唔可以帶 key);**(10)** loading/error state — RolesTab 3 query(roles/permissions/users)+ GroupsTab 2 query(groups/identity),mockup static → aggregate loading(任一 loading)early-return + error(任一 error)`banner banner-destructive`,per F9.2 UsersTab pattern;GroupsTab 加 empty state(mock-auth dev 常 0 group)。**F9.3 ship**:1 EDIT `app/(app)/users/page.tsx` — imports(+`Check`/`RefreshCw`/`Fragment`/`adminApi`/`IdentityConfig`/`EkpRoleKey` + `Role*`/`Group*` types)+ 2 tab body swap(`<TabPlaceholder>` → `<RolesTab/>`/`<GroupsTab/>`)+ NEW inline `RolesTab`(banner + 4 role cards + pivoted 92-cell permissions matrix)+ `GroupsTab`(Entra groups table + client-side role-mapping join)+ helpers(`pivotMatrix`/`MatrixArea`/`MATRIX_ROLES`/`formatRelative`/`truncateOid`)。endpoint count 不變 58(F9.3 純 frontend)。全 10 findings = established convention(F9.2/settings/kb)/ F5-F6-anticipated client-side surface(D5.3/D5.4/D6.3)/ §13 visual-fidelity / Karpathy §1.2 no-speculative,R6 auto-adjust,無 STOP+ask escalate。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + frontend code + backend seed)+ ADR-0027 §Context Roles/Groups tab + CLAUDE.md §5.7 H7;§13 visual-fidelity;Karpathy §1.2 no speculative |
| Day 10 2026-05-21 | **F9.2 active-flip — pre-active-flip R6 audit surfaced 10 findings**(讀 mockup `ekp-page-users.jsx` lines 62-207 `PageUsers`+`UsersTab`+`RoleBadge` + `frontend/app/(app)/settings/page.tsx` tab pattern + `kb/page.tsx` data-fetch/placeholder pattern + `frontend/components/settings/tab-error-state.tsx` + `lib/api/admin.ts` `EkpRoleKey`/`EKP_ROLE_LABELS` + `styles-mockup.css` `.tab .count`/`.kbd`/`.input-search-wrap`/`.avatar-sm`):**(1)** `RoleBadge` 唔存在 — mockup inline 定義 + `window.RoleBadge` export,用於 Members(F9.2)+ Roles + Groups(F9.3)3 tab → **NEW shared `components/users/role-badge.tsx`**(genuine 3-consumer primitive,mockup 自己已 factor 出);mockup runtime `r.color.replace(")", " / 0.12)")` → precomputed literal oklch token strings(identical output,no string-hack);**(2)** `GET /users` backend subset vs mockup 10-col table — F4 D4.1 已定 `UserSummary`=`oid/email/display_name/role/status/created_at`,mockup table 有 `source`/`group`/`queries_7d`/`kbs_owned`/`last_login` 5 個無 backend field 嘅 column → **keep 全 10 column**(visual fidelity per §13 v1.9 — column structure 100% 保留,**NOT** visual element removal),5 個 missing-data cell 用 `—` placeholder per W22 B-i policy(`kb/page.tsx` `R@5 —%`/`Owner —` precedent);非 H7 deviation,非 STOP+ask;**(3)** stat-grid 2-of-4 card backend-unavailable — 「Total members」+「Pending invites」= real(`/users` data derive);「Active sessions」+「Avg queries / user」需 session tracking / query log(Q6 open)→ value+sub `—` placeholder per W22 B-i;全 4 card keep(§13 visual fidelity);**(4)** mockup invite/suspend/role-change 係 presentational — mockup `UsersTab`+`PageUsers` lines 62-191「Export CSV」/「Invite member」/ per-row「More」button 無 onClick / 無 modal / 無 menu(click-through prototype affordance);plan §2 F9.2 text「invite/suspend/role-change」= 呢啲 mockup affordance → F9.2 reproduces them **inert** per mockup(對齊 `kb/page.tsx` inert More-button precedent);functional invite-modal / role-dropdown UI **唔喺 mockup** → 不 build(Karpathy §1.2 no speculative + W22 D6 over-extending anti-pattern);`usersApi.inviteUser/suspendUser/changeUserRole` 保持 F9.1-shipped client surface;mockup 清晰,非 STOP+ask;**(5)** mockup search input inert — mockup line 121 search `<input>` 無 value/onChange(prototype),seg filter 有 wire → **Adjust** F9.2 wire search client-side(name+email filter)consistent with seg filter(both client-side)+ `kb/page.tsx` search precedent(令既有 rendered control functional ≠ 加 visual element);placeholder text verbatim 重現 per H7(「…group…」保留雖然 group 非 backend-searchable);**(6)** tab nav `<div onClick>` → `<button role="tab">` — mockup 用 `<div className="tab" onClick>`,`settings/page.tsx` 已 establish `<button role="tab" aria-selected>` for a11y(已 H7-passed)→ F9.2 跟 settings convention,非 H7 deviation(a11y upgrade,established);**(7)** `?tab=` deep-link — D9.5 / §7 Day 9 #5 已定:F9.2 跟 settings `?tab=` `useSearchParams`+`<Suspense>` convention,mockup plain `useState` 係 prototype URL-state 簡化 → 非 H7 deviation;**(8)** roles/groups/audit tab body 未 build — sub-split:F9.3 build Roles+Groups,F9.4 build Audit → F9.2 render 4-tab nav(shell fidelity)+ transient neutral `<TabPlaceholder>` for 3 個未建 tab(同 sprint F9.3/F9.4 replace);F9.2 H7 self-verify scoped 至 shell + Members tab;**(9)** `TabBoundary` local helper — `settings/page.tsx` 將 `TabBoundary` 定義 locally(非 shared),wrap `ErrorBoundary`(`components/error/`)+ `TabErrorState`(`components/settings/`)→ F9.2 自定 local `TabBoundary` mirror settings(extract 去 shared = 改 settings,非 surgical per Karpathy §1.3);`TabErrorState` cross-dir import from `components/settings/`(48-line generic presentational,reuse 勝 duplicate);**(10)** `Stat` component — mockup inline `Stat`(`.stat`/`.stat-label`/`.stat-value`/`.stat-meta`)→ F9.2 inline `StatCard` helper per mockup(F10 `/kb/[id]` Access tab 另一 page,不在 F9.2 scope)。**F9.2 ship**:2 NEW(`components/users/role-badge.tsx` shared RoleBadge;`app/(app)/users/page.tsx` route shell — `<Suspense>` + page-header + stat-grid 4-card + 4-tab nav `?tab=` deep-link + local `TabBoundary` + inline `UsersTab` Members tab + `StatCard`/`TabPlaceholder` helpers + client-side search/seg filter)。`/users` route NET NEW。全 10 findings = established convention(settings/kb)/ §13 visual-fidelity / W22 B-i placeholder / Karpathy §1.2 no-speculative + §1.3 surgical / sub-split discipline,R6 auto-adjust,無 STOP+ask escalate。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + frontend code)+ ADR-0027 §Context Members tab + CLAUDE.md §5.7 H7;§13 visual-fidelity-wins;Karpathy §1.2 no speculative + §1.3 surgical |
| Day 9 2026-05-21 | **F9 active-flip — pre-active-flip R6 audit surfaced 6 findings**(Explore agent 調研 frontend structure + 讀 `references/design-mockups/PAGE_INVENTORY.md` + `frontend/lib/providers/auth-provider.tsx` + `backend/api/routes/auth.py`):**(1)** F9 規模大 — NET NEW `/users` 4-tab route(Members/Roles/Groups/Audit log)+ `useRole()` + role-gated rendering + H7 全程約束,~3 plan days → **sub-split** per plan §2 + §7 R3:F9.1 foundation / F9.2 route shell + Members tab / F9.3 Roles + Groups tabs / F9.4 Audit tab + role-gating + H7 verify + tests;**(2)** **`useRole()` data source gap** — frontend `AuthenticatedUser`(`lib/auth/types.ts`)只有 `oid`/`tid`/`preferredUsername`/`isMock`,**無 `role`**;`backend/api/routes/auth.py` 無 current-user endpoint(`/refresh`+`/login`+`/logout`+`/register`+`/verify-email`+`/resend-verification` only)。F3 D3.2 加咗 `role` 落 **backend** `AuthenticatedUser`(3-path server-resolved),但 frontend 完全睇唔到 → **Adjust** F9.1 加 backend NEW `GET /auth/me`(`auth.py`,`Depends(get_current_user)` non-admin — every user reads own role,`require_role("admin")` gate 不適用),返回 current user + `role`;`useRole()` fetch 佢。NOT H1(read endpoint,W24c C16/C11 scope,同 F4-F8 13 NEW endpoints 同類非架構);R6 auto-adjust;**(3)** `adminApi`(`frontend/lib/api/admin.ts`)只有 `/admin/*` method(connections/identity/api-keys + `listAuditLog`),**無** `users`/`roles`/`groups` method;`/users`+`/roles`+`/groups` 係 top-level path(非 `/admin/*`)→ **Adjust** F9.1 NEW `frontend/lib/api/users.ts`(client methods + TS types mirror F4 `UserSummary`/`UserListResponse` + F5 `Role`/`RoleListResponse`/`PermissionMatrixResponse` + F6 `Group`/`GroupListResponse`/`GroupSyncResult`);`adminApi.listAuditLog()`+`AuditLogPage` 已存在 → F9.4 Audit tab 重用;**(4)** `EkpRoleKey`(`'admin'|'editor'|'user'|'power'`)+ `EKP_ROLE_LABELS` F3.0 已 land 喺 `lib/api/admin.ts` → F9 重用,不重定義;**(5)** tab pattern — `settings` 6-tab page 係 canonical(`.tabs`/`.tab` mockup CSS + `useState<TabId>` + `useSearchParams` `?tab=` deep-link + `<TabBoundary>` per-tab ErrorBoundary),但 mockup `PageUsers` 用 plain `useState("members")`(無 URL state)→ **Adjust** F9.2 跟 `settings` `?tab=` established convention — visual + interaction 與 mockup 100% 一致(同 `.tabs`/`.tab`,tab click 行為相同),`?tab=` 係 invisible deep-link enhancement(mockup plain `useState` 係 prototype URL-state 簡化)→ 非 H7 deviation;**(6)** `GET /auth/me` placement — `routes/users.py` 係 router-level `require_role("admin")`,`/auth/me` 不可 admin-gate(任何 authenticated user 要讀自己 role)→ `GET /auth/me` 放 `auth.py`(C11,in-route `Depends(get_current_user)`,無 admin gate)。**F9 ship**(sub-split — 本 changelog row 記 F9.1):F9.1 = 1 backend EDIT(`auth.py` +`GET /auth/me` + `MeResponse` schema)+ 1 backend test EDIT + 2 frontend NEW(`lib/api/users.ts` client + `lib/hooks/use-role.ts` 或同等 `useRole()`)。F9.2-F9.4 後續 sub-split。全 6 findings = sub-split discipline / non-architectural read-endpoint enabler / §13-style backend-mirror types / established-convention reuse,R6 auto-adjust,無 STOP+ask escalate。 | per CLAUDE.md §10 R6 recursive(plan-text + mockup + frontend code)+ ADR-0027 §Context 4 tabs + CLAUDE.md §5.7 H7;Karpathy §1.1 think-before-coding |

---

**End of W24c-users-rbac plan(version 1.0 kickoff)**
