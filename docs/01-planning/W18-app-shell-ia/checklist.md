---
phase: W18-app-shell-ia
plan_ref: ./plan.md
status: active
last_updated: 2026-05-11
---

# Phase W18 — Checklist

> Atomic checkbox(每 item ≤ 0.5–2 hour effort)。Status:`active` from kickoff 2026-05-10(Chris directive + ADR-0024 Accepted = the authorization)。
> 每 item done 後 `[ ]→[x]` + commit ref;延後項標 🚧 + reason(per CLAUDE.md §10 sacred rule — 唔可以刪未勾選 item)。
> Deliverable ↔ ADR-0024 mapping:F1=D1 / F2=D2+D7 / F3=D3 / F4=D4 / F5=D5 / F6=D6 / F7=D8 / F8=D9+D10-residual / F9=closeout。

## F0 — Kickoff cascade(ADR-0024 acceptance + spec amendment — landed at kickoff)

- [x] F0.1 `docs/adr/0024-unified-application-shell-ia.md` Status **Proposed → Accepted**(Q1-Q6 resolved;Chris directed the post-acceptance cascade;`## Decision` header「proposed」→「accepted」;Implementation-Deliverables note「folder not pre-created until Accepted」→「created on acceptance 2026-05-10」)— `(this commit)`
- [x] F0.2 `docs/adr/README.md` — ADR-0024 row status「**Proposed**」→「Accepted」+ context cell updated(post-acceptance cascade);footnote「Proposed 2026-05-10」→「Accepted 2026-05-10」;「Next NNNN」block 0024 line updated — `(this commit)`
- [x] F0.3 `docs/adr/0015-ui-tier-1-expansion-dify-leaning.md` — Status line gets「**amended by ADR-0024 2026-05-10**」note(3 ways:V7 Landing removed / per-view layout-regime split → single `<AppShell>` / V2「Admin Dashboard」→ real `/dashboard`;preserves V8/V9 auth pages + shadcn/ui foundation + EKP visual identity + W12-W15 impl)+ References section gets an ADR-0024 amended-by entry — `(this commit)`
- [x] F0.4 `docs/architecture.md` — top-block §5 amendment note added(after the v5.1→v6「註」block;inline-tagged, doc version held per the §3.4/§3.7 precedent);**NEW §5.0 Application Shell** section inserted(before §5.1 — statement of the unified shell:top bar + collapsible left sidebar + main content;5 sidebar modules;top-bar contents incl. disabled language toggle [i18n Tier 2 §11];login-gate;flattened routing `app/(app)/...`);§5.2 Chat header `/`→`/chat` + in-shell note;**§5.3「Admin Dashboard」→「Dashboard」** `/admin`→`/dashboard` + body rewritten as a real overview(KB summary / recent queries / latest eval / system health / quick actions);§5.4 KB List `/admin/kb`→`/kb`;§5.5 KB Detail `/admin/kb/[id]`→`/kb/[id]`;§5.6 Eval Console in-shell note(route unchanged);**§5.7「Debug View」→「Traces」** `/debug/[traceId]`→`/traces/[traceId]`;**§5.9 V7 Landing → REMOVED tombstone**(EKP internal-only;`/`→redirect);§5.10/§5.11 Login/Register redirect target `/chat`→`/dashboard` + "stays outside `<AppShell>`" note — `(this commit)`
- [x] F0.5 `docs/01-planning/W18-app-shell-ia/{plan,checklist,progress}.md` created — `status: active`(per Chris directive, not the usual draft→active flip — the directive + ADR-0024 Accepted IS the authorization;same pattern as W17 D0). Plan §2 deliverables F0-F9 = ADR-0024 D1-D10 mapped — `(this commit)`

## F1 — `<AppShell>` component(generalize `<AdminShell>`)— ADR-0024 D1

> **F1 verdict = PASS**(2026-05-10 D1)— landed `(this commit)`. NEW `frontend/components/nav/app-shell.tsx` — top bar(app name → `/dashboard` + global-search trigger + Cmd/Ctrl+K listener[no-op stub → F6 fills it]+ disabled language toggle + `<ThemeToggle>` + `<UserMenu>`)+ collapsible left sidebar(5 flat modules,`aria-current="page"` highlight,focus-mode toggle persisted to `localStorage['ekp-sidebar-collapsed']`)+ main content slot + responsive(desktop `<aside>` ↔ mobile off-canvas `<Sheet>`). 100% token classes;`[oklch`=0;`tsc --noEmit` + `next lint` clean. **Not yet wired into a layout — F2 does that.** Minor deviations(plan §7 changelog D1 + progress Day-1):`children`-only props(no nav-items prop — single-shell app);`<UserMenu>` "Settings → /settings" item is F5.2(not F1);native `disabled` + `title` on the language toggle(not `aria-disabled`);no top-bar breadcrumb(not in the §5.0 spec);`Ctrl K` hint(Windows-primary team).

- [x] F1.1 NEW `frontend/components/nav/app-shell.tsx` — `<AppShell>` = `<header sticky top-0 z-30 h-14>` top bar + `<aside sticky top-14 h-[calc(100dvh-3.5rem)] w-56 md:block>` collapsible left sidebar + `<main min-w-0 flex-1 p-4 md:p-8>{children}</main>` content slot;generalized from `admin-shell.tsx`(reuses the hamburger-collapse pattern + token-class layout);`children`-only props(deviation — `usePathname()` for active route, `NAV_ITEMS` module const);file header docstring per §3.2 — `(this commit)`
- [x] F1.2 Left sidebar — 5 flat module items via `NavLinks` sub-component(`/dashboard` LayoutDashboard / `/chat` MessageSquare / `/kb` Database / `/eval` FlaskConical / `/traces` Activity)+ active highlight `isActiveRoute(pathname, href)` → `aria-current="page"` + muted-bg;`<nav aria-label="Primary">`;**focus-mode toggle**(`PanelLeftClose`/`PanelLeftOpen` in the top bar, `aria-pressed`)collapses the desktop sidebar(not rendered when `collapsed`)— persisted to `localStorage['ekp-sidebar-collapsed']`(SSR-stable default `false` + mount-time `useEffect` read)— `(this commit)`
- [x] F1.3 Top bar — app name "EKP" → `/dashboard`(no tagline)+ global-search trigger(centred search-box `<button aria-label="Search (Ctrl+K)">` with a `Ctrl K` `kbd` hint;`onClick` + a `window` Cmd/Ctrl+K keydown listener both call `handleOpenSearch()` — a `// TODO(W18 F6)` no-op stub;F6 mounts `<GlobalSearch>` + fills the handler)+ **disabled language toggle**(`<Languages>` icon, native `disabled` + `title="Multi-language (JP / ZH) — coming in a later tier"` — i18n Tier 2 per §11 / CLAUDE.md §5.4 H4)+ `<ThemeToggle>`(reused)+ `<UserMenu>`(reused — its "Settings → /settings" item is F5.2)— `(this commit)`
- [x] F1.4 Responsive — desktop `<aside md:block>`;< `md` = controlled shadcn `<Sheet side="left">`(opened by the top-bar hamburger `aria-expanded={mobileNavOpen}`;each mobile `NavLinks` item gets `onNavigate` → closes the sheet);`<SheetHeader><SheetTitle>EKP</SheetTitle>` for Radix-Dialog a11y;top bar stays at all widths — `(this commit)`
- [x] F1.5 Tokens — 100% token classes(`border-border` / `bg-background` / `bg-muted/40` / `bg-muted` / `text-muted-foreground` / `text-foreground`);`Grep '\[oklch'` across `frontend/` = **0**(milestone preserved — one accidental F1-docstring occurrence reworded before commit);`pnpm exec tsc --noEmit` → exit 0;`pnpm exec next lint` → "No ESLint warnings or errors" — `(this commit)`

## F2 — `(app)/` route group + consolidated providers + login-gate — ADR-0024 D2 + D7

> **F2 verdict = PASS(F2.3 deferred into F3)**(2026-05-11 D2)— landed `(this commit)`. NEW `frontend/app/(app)/layout.tsx`(`<AuthProvider><QueryProvider><LoginGate><AppShell>{children}</AppShell></LoginGate></QueryProvider></AuthProvider>`)+ NEW `frontend/components/auth/login-gate.tsx`(mock-auth dev → pass-through, never gates;real MSAL → splash while idle/loading + a `/login` link on error/idle, **no auto-redirect** matching the existing AuthProvider anti-infinite-loop design + a `// TODO(W16)` to tighten once Q11 cred wiring is live). Root `app/layout.tsx` **already** only has `<ThemeProvider>` + `<Toaster>`(F2.4 = verify-no-op). `tsc --noEmit` + `next lint` clean;`[oklch`=0;dev server up(`/` HTTP 200). **The `(app)/` layout is inert until F3 moves the pages in** — and **F2.3(removing `app/admin/layout.tsx` / `eval/layout.tsx` / `debug/[traceId]/layout.tsx` + `app/admin/page.tsx`)is deferred into F3** because that removal is physically inseparable from the F3.1 page move(doing it now would strand `app/admin/kb/*` / `app/eval/page.tsx` / `app/debug/[traceId]/page.tsx` without their `AuthProvider`+`QueryProvider`+shell). See plan §7 changelog D2 + progress Day-2.

- [x] F2.1 NEW `frontend/app/(app)/layout.tsx` — server component;`<AuthProvider><QueryProvider><LoginGate><AppShell>{children}</AppShell></LoginGate></QueryProvider></AuthProvider>`;file header docstring per §3.2;inert until F3 moves the page tree into `app/(app)/` — `(this commit)`
- [x] F2.2 Login-gate — NEW `frontend/components/auth/login-gate.tsx`(`'use client'`);reads `useAuthStatus()` + `authMode` from `auth-provider`;**mock-auth dev mode = no-op pass-through**(AuthProvider auto-signs-in;the visible "未登入→/login" only appears in real MSAL/prod, per ADR-0024);**real MSAL**:`status==='authenticated'` → children;else → a minimal splash(`Loader2` spinner, or the error text on `status==='error'`)+ a `<Link href="/login">Sign in to continue</Link>`;**no auto-redirect**(matches the existing AuthProvider design that avoids an infinite loop if cred wiring is broken — `/login` is outside `(app)/`)+ a `// TODO(W16)` to tighten to `router.replace('/login')` once Q11 Track A cred is live — `(this commit)`
- [x] F2.3 Remove `frontend/app/admin/layout.tsx` + `frontend/app/eval/layout.tsx` + `frontend/app/debug/layout.tsx`(folded into `app/(app)/layout.tsx`)+ `frontend/app/admin/page.tsx`(old "Admin Dashboard" placeholder — role taken by `/dashboard`)+ `frontend/components/nav/admin-shell.tsx`(orphaned — the only consumers were those three layouts;my-own-mess cleanup per Karpathy §1.3)— **landed atomically with the F3.1 page move**(2026-05-11 D3;`debug/layout.tsx` was at `app/debug/layout.tsx`, not `app/debug/[traceId]/layout.tsx` as the plan text said — minor path correction)— `(this commit)`
- [x] F2.4 Root `frontend/app/layout.tsx` — verified **already** only `<ThemeProvider>`(next-themes, `attribute="class"` `defaultTheme="system"`)+ `<Toaster>` + `<html>`/`<body>` + metadata + `globals.css`;no `AuthProvider`/`QueryProvider` leak to root — no change needed(F2.4 = verify-no-op)— `(this commit)`(verified, no edit)
- [x] F2.5 Auth pages(`/login` `app/login/page.tsx` / `/register` `app/register/page.tsx`;`/verify` is currently a step inside the register wizard, not a standalone route)stay OUTSIDE `app/(app)/` — verified(no auth page created inside `(app)/`);they keep the root layout(no app chrome pre-auth)— `(this commit)`(verified)
- [x] F2.6 `pnpm -C frontend exec tsc --noEmit` → exit 0;`pnpm -C frontend exec next lint` → "No ESLint warnings or errors";dev server on `:3001`(prior session)up — `/` HTTP 200(mock-auth env;the `(app)/` layout being inert doesn't affect existing routes);Playwright `webServer` mock-auth smoke = booted-and-serving(the full E2E run stays the user's pre-Beta smoke per the R8 `npx playwright install chromium` block / CO_W15_F4_browser_binaries)— `(this commit)`
- [x] F2.7 File header docstrings — `app/(app)/layout.tsx` + `components/auth/login-gate.tsx` both have full docstrings per §3.2 / session-start §13 #8(the `app/admin/layout.tsx` / `eval/layout.tsx` / `debug/layout.tsx` docstring updates land with their removal in F3)— `(this commit)`

## F3 — Move + re-route pages;flatten URLs;update all links + Playwright — ADR-0024 D3

> **F3 verdict = PASS**(2026-05-11 D3)— landed `(this commit)`. **IA flip is live** — the `<AppShell>` is now browser-visible on `/dashboard` / `/chat` / `/kb` / `/eval` / `/traces`. 8 page files moved into `app/(app)/`(git tracks them as renames)+ `app/admin/error.tsx` → `app/(app)/error.tsx`(scoped error boundary, relocated)+ 4 layouts/pages + `admin-shell.tsx` deleted(F2.3 landed here)+ 2 new pages(`(app)/dashboard/page.tsx` F3 placeholder → F4 fills it;`(app)/traces/page.tsx` thin trace-ID index)+ all internal route literals updated(`/admin/kb/*` → `/kb/*`,`/debug/[traceId]` → `/traces/[traceId]`)+ Playwright `admin-path.spec.ts` → `app-shell-path.spec.ts`(renamed + route refs updated)+ `visual-baseline.spec.ts` + `tests/e2e/README.md` updated + **`.gitignore` `traces/` → `/traces/`**(the bare pattern was about to shadow the new `app/(app)/traces/` route folder — caught via `git check-ignore`). `tsc --noEmit` exit 0(after clearing the stale `.next/types/` cache from the old route tree)+ `next lint` clean + `[oklch`=0;curl smoke on `:3001`:`/` `/dashboard` `/chat` `/kb` `/eval` `/traces` `/traces/<dummy>` `/kb/new` all HTTP 200,`/admin` `/admin/kb` HTTP 404(✓ removed). Deviations(plan §7 changelog D3 + progress Day-3):`chat/page.tsx`'s own `<main>`+`min-h-screen` → `<div>`+`h-full`(can't nest `<main>` inside the AppShell's `<main>`)+ title row slimmed(`<h1>EKP — Knowledge Chat</h1>` → `<h1>Chat</h1>` — "EKP" is in the top bar now);`app/admin/error.tsx` moved → `app/(app)/error.tsx`(not in plan F2.3's list — but it'd die with `app/admin/`; relocated so `(app)/` keeps a scoped error boundary);2 new placeholder/index pages added(not pure "moves" but needed so the AppShell links don't 404);`debug/layout.tsx` was at `app/debug/layout.tsx`(not `app/debug/[traceId]/layout.tsx`);`admin-path.spec.ts` renamed(F3.3 expected);`lib/api/debug.ts` + `debugApi` kept their names(the backend endpoint stays `GET /debug/trace/{id}` — only the frontend route changed).

- [x] F3.1 Moved pages into `app/(app)/`(bash `mv` then `git add -A` → git records renames):`chat/page.tsx` ← `app/chat/` / `kb/page.tsx` ← `app/admin/kb/` / `kb/[id]/page.tsx` ← `app/admin/kb/[id]/` / `kb/new/page.tsx` ← `app/admin/kb/new/` / `kb/[id]/upload/page.tsx` ← `app/admin/kb/[id]/upload/` / `eval/page.tsx` ← `app/eval/` / `traces/[traceId]/page.tsx` ← `app/debug/[traceId]/`(renamed Debug→Traces)/ `error.tsx` ← `app/admin/error.tsx`(scoped error boundary;`AdminError`→`AppError`,`scope="Admin"`→`scope="App"`,docstring updated). Old dirs `app/{admin,chat,eval,debug}/` removed entirely. NEW `app/(app)/dashboard/page.tsx`(F3 placeholder — heading + module links;F4 builds the real overview cards)+ NEW `app/(app)/traces/page.tsx`(thin Traces index — trace-ID input → `/traces/<id>`). `chat/page.tsx`:`<main className="...min-h-screen...">` → `<div className="...h-full...">` + closing tag,title row slimmed(no nested `<main>` inside the AppShell's `<main>`);no `/admin`/`/debug` route literals in it(it had none)— `(this commit)`
- [x] F3.2 Updated **all** internal route refs — `replace_all` `/admin/kb` → `/kb` in `kb/page.tsx`(3 refs:docstring + Create-CTA ×2 + per-card link)、`kb/[id]/page.tsx`(5 refs:docstring + searchParams `router.push` + back link + upload link ×2)、`kb/new/page.tsx`(3 refs:docstring + post-create `router.push` + back link)、`kb/[id]/upload/page.tsx`(3 refs:docstring + post-upload `router.push` + back link);`eval/page.tsx` failed-query inspect link `/debug/${q.query_id}` → `/traces/${q.query_id}`;`traces/[traceId]/page.tsx` docstring header `V6 Debug View (\`/debug/[traceId]\`)` → `V6 Traces (\`/traces/[traceId]\`)`. **grep-verified**:`grep '/admin|/debug'` across `frontend/*.{ts,tsx}` → the only hits left are deliberate(`lib/api/debug.ts` = backend endpoint `GET /debug/trace/{id}`,unchanged;docstrings in the moved files / `(app)/layout.tsx` referencing the old paths historically;`debugApi` import name)— no stray route literals. No in-page logo link in `chat/page.tsx` to remove(it had none — the plan F3.2 anticipated one) — `(this commit)`
- [x] F3.3 Updated `frontend/tests/e2e/` — `admin-path.spec.ts` → renamed `app-shell-path.spec.ts`(content rewritten:`/admin` → `/dashboard`[F3-placeholder smoke + a `// TODO(W18 F4)` for the real cards]、`/admin/kb` → `/kb`、`/debug/${id}` → `/traces/${id}`、heading checks loosened where W16/W17 changed the view、stub-note assertion dropped[backend `/debug/trace` wired W16 F5.5]、AppShell-sidebar-nav test updated to `/kb`+`/eval`+`/chat`)；`visual-baseline.spec.ts`(`page.goto('/admin')` → `/dashboard`,heading `/overview|admin/i` → `/dashboard/i`,screenshot `v2-admin-dashboard.png` → `dashboard.png`,docstring updated — re-baseline once F4 lands)；`tests/e2e/README.md`(coverage table row + the snapshot-tree example + the "intentional UI change" example + a W18-F3 note). `golden-path.spec.ts` needs no change(`/` `/login` `/register` `/chat` URLs unchanged;it only checks for a `textarea` on `/chat`,still present). Full E2E run = R8-blocked(`npx playwright install chromium` / CO_W15_F4_browser_binaries / ADR-0017)— this delivers the updated specs + `tsc` compile-check; the run is the pre-Beta smoke — `(this commit)`
- [x] F3.4 `frontend/next.config.mjs` — verified:`reactStrictMode` + `output: 'standalone'` + a comment(the backend proxy moved to `app/api/backend/[...path]/route.ts`, which is path-agnostic and outside `(app)/`)— no route-specific config; no change needed — `(this commit)`(verified)
- [x] F3.5 `pnpm exec tsc --noEmit` → exit 0(after `rm -rf .next/types` — the stale generated type files referenced the old `app/admin/...` / `app/chat/...` routes;the dev server regenerates them);`pnpm exec next lint` → "No ESLint warnings or errors";`Grep '\[oklch'` across `frontend/` = **0**(milestone preserved through the restructure);curl smoke on the running `:3001` dev server — `/` `/dashboard` `/chat` `/kb` `/eval` `/traces` `/traces/<dummy>` `/kb/new` all HTTP 200(✓ AppShell renders;the traces detail page handles a not-found trace gracefully),`/admin` `/admin/kb` HTTP 404(✓ removed). `pnpm test:unit` — unaffected by the route moves(the W17 F6 button test is component-level)— `(this commit)`

## F4 — `/dashboard` — the real post-login overview — ADR-0024 D4

> **F4 verdict = PASS**(2026-05-11 D4)— landed `(this commit)`. `frontend/app/(app)/dashboard/page.tsx` rewritten from the F3 placeholder into the real overview:5 cards in a responsive grid(`sm:grid-cols-2 lg:grid-cols-3`)— **Knowledge bases**(`useQuery` off `kbApi.list()` → `GET /kb` → count + Σdocs/Σchunks/ΣMB + link → `/kb`;skeleton + error states)、**Recent queries**(no backend source — Q6 open → "ask" CTA → `/chat`)、**Latest evaluation**(no cached run source → "run eval" CTA → `/eval`)、**System health**(`useQuery` off `GET /health` → green "Backend operational" / red "Backend unreachable" dot;per-component connectivity needs a richer `/health` — noted as out of scope)、**Quick actions**(`<QuickAction>` ×4:New KB → `/kb/new` / Upload doc → `/kb` / Run eval → `/eval` / Open chat → `/chat`,`Button asChild variant="outline"` + `<Link>` + lucide icon). No new backend(`/health` is `{status:"ok"}` liveness;`/kb` is `kbApi.list`). `'use client'`(uses `useQuery`). `tsc`+`lint` clean,`[oklch`=0;`GET /dashboard` HTTP 200 on `:3001`. Deviations(plan §7 D4):F4.1 was a rewrite not a NEW(F3 created the placeholder);F4.2(d) limited to backend liveness(`/health` has no per-component statuses — a later-tier endpoint);F4.2(b)(c) are CTAs not data(no recent-query / no eval-cache backend);used `apiClient.get('/health')` directly instead of a new `lib/api/health.ts`(single trivial call);no Vitest dashboard test yet(→ F8).

- [x] F4.1 `frontend/app/(app)/dashboard/page.tsx` — **rewritten** from the F3 placeholder into the real overview(`'use client'`;cards grid;NOT a router-to-chat);file header docstring per §3.2 — `(this commit)`
- [x] F4.2 Cards — (a) **Knowledge bases**:`useQuery(['kb','list'], () => kbApi.list())` → `kbs.length` + `Σtotal_documents` + `Σtotal_chunks` + `Σstorage_size_mb` + link → `/kb`;(b) **Recent queries**:Q6 real-query collection is Open → no source → "Query history isn't collected yet (Q6)" + CTA → `/chat`;(c) **Latest evaluation**:no cached `/eval/run` result endpoint → "No eval run cached…" + CTA → `/eval`;(d) **System health**:`useQuery(['health'], () => apiClient.get<{status:string}>('/health'), {retry:1})` → green dot "Backend operational" / red dot "Backend unreachable" + a note that per-component connectivity(Azure Search / OpenAI / Cohere / Langfuse)needs a richer `/health` endpoint(later tier);(e) **Quick actions**:`<QuickAction>` grid `grid-cols-2 sm:grid-cols-4` — New KB `/kb/new`(`Plus`)/ Upload doc `/kb`(`Upload`)/ Run eval `/eval`(`FlaskConical`)/ Open chat `/chat`(`MessageSquare`)— `(this commit)`
- [x] F4.3 No new backend — `GET /kb`(via `kbApi.list`)+ `GET /health` are existing(W1/W2);there's no cached-eval-run endpoint → the eval card is a CTA(no backend caching added in W18);`apiClient.get('/health')` used directly(not a new `lib/api/health.ts` — Karpathy §1.2,one trivial call)— `(this commit)`
- [x] F4.4 Loading + error/empty states — KB card:`<Skeleton>` while `isPending`,`text-destructive` line on `isError`;health card:`<Skeleton>` while pending,red/green dot otherwise;the recent-queries + latest-eval cards are first-class empty states(CTAs)— a brand-new install shows: KB count(may be 0 or the seeded count)、both CTAs、backend dot — `(this commit)`
- [x] F4.5 100% token classes(`text-muted-foreground` / `text-destructive` / `bg-success` / `bg-destructive` / `border-border`);`pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` clean;`Grep '\[oklch'` across `frontend/` = **0**;`GET /dashboard` HTTP 200 on `:3001`. Vitest dashboard render-smoke deferred → **F8**(F8.4 adds `<AppShell>`+`<GlobalSearch>` tests;a dashboard render test can join then)— `(this commit)`

## F5 — `/settings` — small profile/preferences view — ADR-0024 D5

> **F5 verdict = PASS**(2026-05-11 D5)— landed `(this commit)`. NEW `frontend/app/(app)/settings/page.tsx`(`'use client'`)— 3 cards:**Profile**(the `AuthenticatedUser` claims — `preferredUsername` ≈ email / `oid` / `tid`,each mono;a `<Badge variant="outline">mock auth — dev mode</Badge>` when `user.isMock`;"Signing in…" while `user` is null)、**Preferences**(label "Theme" + the existing `<ThemeToggle>` — Light/Dark/System via next-themes)、**Session**(`<Button variant="outline">` Sign out → `useAuthStore.signOut` — same path the `<UserMenu>` uses). `<UserMenu>` got a "Settings" item(`<DropdownMenuItem asChild><Link href="/settings"><Settings/>Settings</Link></DropdownMenuItem>` above the existing Sign-out item + a separator;the menu docstring updated "admin shell" → "app-shell" + the W18 F5 note). `tsc`+`lint` clean,`[oklch`=0;`GET /settings` HTTP 200 on `:3001`. Deviations(plan §7 D5):`AuthenticatedUser` has no separate "display name"/"email" field — `preferredUsername`(≈ email)+ `oid` + `tid` shown(the ADR said "display name / email / oid");theme pref = embedded `<ThemeToggle>`(reuse — not a new radio group;Karpathy §1.2);`<UserMenu>` Settings item implemented as `asChild`+`<Link>`(the Radix nav-menu-item pattern — same effect as `router.push`, no `useRouter` needed);no separate "Profile" menu item(the ADR listed Profile/Settings/Sign out — the `<UserMenu>` label IS the profile glance,and `/settings` shows the full profile,so a separate "Profile" item would be redundant).

- [x] F5.1 NEW `frontend/app/(app)/settings/page.tsx` — Profile card(`<ProfileRow>` ×3 + mock `<Badge>`;reads `useCurrentUser()`)+ Preferences card("Theme" + `<ThemeToggle>`)+ Session card("Sign out" `<Button>` → `useAuthStore((s)=>s.signOut)`);`'use client'`;file header docstring per §3.2 — `(this commit)`
- [x] F5.2 `<UserMenu>` "Settings" item → `/settings` — implemented as `<DropdownMenuItem asChild><Link href="/settings"><Settings className="mr-2 h-4 w-4"/>Settings</Link></DropdownMenuItem>`(Radix nav-item pattern;above the Sign-out item, with a `<DropdownMenuSeparator>`;`Settings` icon + `Link` imported)+ menu docstring updated — `(this commit)`
- [x] F5.3 100% token classes(`text-muted-foreground` / `border-border`;`<Badge variant="outline">`)— no hardcoded colours;`pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` clean;`Grep '\[oklch'` = **0** — `(this commit)`
- [x] F5.4 Tier 1 scope guard — `/settings` v1 = exactly profile display(3 claim rows + mock badge)+ theme preference(`<ThemeToggle>`)+ sign-out;**no** notification prefs / API tokens / org settings — those stay later-tier — `(this commit)`

## F6 — Global search command palette(`<GlobalSearch>` — Cmd/Ctrl+K)— ADR-0024 D6

> **F6 verdict = PASS**(2026-05-11 D5)— landed `(this commit)`. NEW `frontend/components/nav/global-search.tsx` — a shadcn-`Dialog`-based command palette(**zero new dep** — the `cmdk` stop-and-ask path wasn't needed;Radix Dialog gives `role="dialog"`+`aria-modal`+focus-trap+Escape for free, arrow-key result nav + `aria-activedescendant` wired on the input combobox/listbox pattern)+ wired into `<AppShell>`(`searchOpen` state;the F1 `handleOpenSearch` no-op stub now does `setSearchOpen(true)`;the top-bar trigger + the Cmd/Ctrl+K listener both open it)+ a small `chat/page.tsx` `?q=` read-on-mount tweak(pre-fill the input + focus it). `tsc --noEmit` exit 0,`next lint` clean,`[oklch`=0;`/`+5 module routes+`/settings` all HTTP 200 on `:3001`(the palette's interactive nav = user pre-Beta smoke + the F8 Vitest test). Deviations(plan §7 changelog D5 + progress Day-5):**result types** = Pages(the 5 sidebar modules + Settings)+ Knowledge bases(off the cached `GET /kb`)+ always-present「Ask in chat: …」— **recent-docs / recent-traces omitted**(no cheap backend source — same CTA-not-data pattern as F4;Pages is the Tier-1 quick-jump substitute, arguably more useful);**`?q=` = pre-fill only**(not auto-send — "chat reads it on mount" per the plan's fallback scoping;don't redesign the chat input);**Vitest test deferred → F8.4**(per the plan "full pass = F8").

- [x] F6.1 NEW `frontend/components/nav/global-search.tsx` — command-palette dialog built on the shadcn `Dialog` primitive(**no new dep** — `cmdk` not needed)triggered by Cmd/Ctrl+K(the `<AppShell>` `window` keydown listener → `setSearchOpen(true)`)+ the top-bar search trigger;controlled by `<AppShell>`(`open`/`onOpenChange`);file header docstring per §3.2 — `(this commit)`
- [x] F6.2 Tier 1 quick-jump scope — filters:(a) **Pages**(the 5 sidebar modules + Settings — static `PAGE_RESULTS`, filtered by label + keywords)→ the route;(b) **Knowledge bases**(off the cached `GET /kb` list via `useQuery(['kb','list'])` — shares the dashboard's cache,`enabled: open`)→ `/kb/[kb_id]`;(c) always-present「Ask in chat: "<query>"」→ `/chat?q=<encoded>`. **recent docs / recent traces dropped** — no cheap backend source(would be N requests across all KBs;Q6 has no query log)— the Pages results cover the quick-jump need. NOT in scope:semantic search-as-you-type across chunks(Tier 2 candidate)— `(this commit)`
- [x] F6.3 a11y — Radix `Dialog` provides `role="dialog"` + `aria-modal` + focus trap + Escape-to-close;the search input is `role="combobox"` `aria-expanded` `aria-controls` `aria-activedescendant`;the results `<ul role="listbox">` with `<li role="option" aria-selected>`;ArrowUp/ArrowDown move the active index, Enter selects, `onMouseMove` syncs hover→active;`autoFocus` on the input(eslint-disabled with a one-line rationale — a command palette focuses its input on open by design)— `(this commit)`
- [x] F6.4 100% token classes(`border-border` / `bg-muted` / `text-foreground` / `text-muted-foreground` — no hardcoded colours);`pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` "No ESLint warnings or errors";`Grep '\[oklch'` across `frontend/` = **0**. Vitest test for the filter logic + key binding deferred → **F8.4**(joins the `<AppShell>` test pass)— `(this commit)`
- [x] F6.5 `?q=` chat pre-fill — `frontend/app/(app)/chat/page.tsx` got a mount-time `useEffect` that reads `new URLSearchParams(window.location.search).get('q')` → `setInput(q)` + `textareaRef.current?.focus()`(added a `textareaRef`);**pre-fill only**(the user hits Enter to send — the SSE / streaming logic is untouched, per "navigate with the query in the URL; chat reads it on mount; don't redesign the chat input");docstring updated — `(this commit)`

## F7 — login/register redirect → `/dashboard`;delete V7 Landing;`/` → redirect — ADR-0024 D8

> **F7 verdict = PASS**(2026-05-11 D5)— landed `(this commit)`. `app/login/page.tsx`(both `handleSelfSubmit` + `handleSsoClick`)+ `app/register/page.tsx`(Step 3 CTA `handleStartAsking` → `handleGoToDashboard`)now route to `/dashboard`(was `/chat`);`app/page.tsx` rewritten — the V7 Landing markup(hero + 3 feature cards + how-it-works + footer + site header/footer)**deleted** → a thin server-component `redirect('/login')`. `brand-panel.tsx` kept(it's the auth-page brand splash on `/login` / `/register`, not a marketing page);no Landing-only component existed(all inline functions in `page.tsx`)so nothing orphaned. Playwright:`golden-path.spec.ts` first test rewritten to a `/`→`/login` redirect assertion(was "V7 Landing renders");`visual-baseline.spec.ts` V7 Landing baseline removed(Landing gone);`tests/e2e/README.md` coverage table + snapshot tree updated. `tsc`+`lint` clean,`[oklch`=0;`/` HTTP 307 → `/login` HTTP 200 on `:3001`. Deviations(plan §7 changelog D5 + progress Day-5):**F7.3 = `redirect('/login')` always**(no "if-authenticated → /dashboard" branch — mock-auth has no server-readable session, and the `(app)/` login-gate + MSAL handle re-entry once you reach a `/app` route;the bare `/→/login` is the honest Tier 1 behaviour, documented in the `page.tsx` docstring);Step 3 CTA label "Start asking →" → "Go to your dashboard →"(honest given it goes to `/dashboard` now);`visual-baseline.spec.ts` edit not in the plan F7.5 literal but a necessary consequence of deleting the Landing.

- [x] F7.1 `frontend/app/login/page.tsx` — successful sign-in → `router.push('/dashboard')`(was `/chat`)in both `handleSelfSubmit`(self-register path)and `handleSsoClick`(MSAL path);docstring W18 F7 note added — `(this commit)`
- [x] F7.2 `frontend/app/register/page.tsx` — Step 3 CTA → `/dashboard`(was `/chat`;handler `handleStartAsking` → `handleGoToDashboard`,`Step3` prop `onStartAsking` → `onContinue`,button label "Start asking →" → "Go to your dashboard →",blurb updated);verify-email auto-login(ADR-0022)means Step 3 lands authenticated → `/dashboard` resolves inside `<AppShell>`;docstring updated — `(this commit)`
- [x] F7.3 `frontend/app/page.tsx` — V7 Landing markup **deleted**(`SiteHeader` / `Hero` / `FeatureHighlights` / `FeatureCard` / `HowItWorks` / `SiteFooter` — all inline,gone)→ thin server component `import { redirect } from 'next/navigation'; export default function RootPage() { redirect('/login'); }`;new docstring explains the "always → /login"(no server-readable mock session;the `(app)/` login-gate + MSAL re-route authenticated users)— `(this commit)`
- [x] F7.4 `frontend/components/auth/brand-panel.tsx` kept(auth-page brand splash on `/login`+`/register` — verified still imported by both);grep-verified the deleted Landing markup was all inline in `page.tsx`(no `landing-*` component file)— nothing orphaned;the shared `Button`/`Card` shadcn imports the Landing used stay(used everywhere)— `(this commit)`
- [x] F7.5 `pnpm exec tsc --noEmit` exit 0;`pnpm exec next lint` clean;Playwright — `golden-path.spec.ts` first test = `page.goto('/')` → `expect(page).toHaveURL(/\/login$/)` + login heading visible(replaces "V7 Landing renders");`visual-baseline.spec.ts` — V7 Landing screenshot test + docstring line removed;`tests/e2e/README.md` — coverage table row + the `…-snapshots/` tree + the "intentional UI change" example updated;`Grep '\[oklch'` across `frontend/` = **0**;`/` → 307 → `/login` 200 on the running `:3001` dev server — `(this commit)`

## F8 — responsive + a11y pass;Vitest + Playwright updates;dark-mode re-check;`COMPONENT_CATALOG.md` note — ADR-0024 D9 + D10-residual

> **F8 verdict = PASS**(2026-05-11 D6)— landed `(this commit)`. Vitest:**3 NEW test files**(`tests/unit/app-shell.test.tsx` + `global-search.test.tsx` + `dashboard.test.tsx`)— `pnpm test:unit` → **4 files / 13 tests pass**(was 1 file / 3 tests at the W17 F6 baseline). a11y:`<AppShell>` already had `aria-current="page"` / `aria-expanded` / `aria-pressed` / `aria-label`s(verified);`<GlobalSearch>` gets `role="dialog"`+`aria-modal`+focus-trap+Escape from Radix `Dialog`,plus `role="combobox"`+`aria-activedescendant`+`aria-controls` on the input and `role="listbox"`/`role="option"`+`aria-selected` on the results(verified);**added `role="heading" aria-level={2}` to the dashboard(6)+ settings(3) `CardTitle`s** so each card title is a real heading under the page `<h1>`;the disabled language toggle keeps native `disabled` + `aria-label` + `title`(the strongest "unavailable" signal — multi-language genuinely doesn't exist Tier 1). Responsive:the AppShell's `md:` breakpoint logic(desktop `<aside>` ↔ mobile off-canvas `<Sheet>`), the palette's `w-full max-w-xl`(full-width <576px), and the `/dashboard` grid's `grid sm:grid-cols-2 lg:grid-cols-3`(1-col on mobile)were all built responsively in F1/F4/F6 — verified by reading;the multi-viewport browser smoke stays the user's pre-Beta walkthrough(R8 — no Playwright browser binaries). Dark-mode:`Grep '\[oklch'` across `frontend/` = **0**(milestone preserved through F1-F8);the W17 F5 mechanism note(next-themes `attribute="class"` → `.dark` vars → tailwind `oklch(var(--token))`)is unchanged. Playwright:`tests/e2e/app-shell-path.spec.ts` — the `/dashboard` test rewritten for the real F4 cards(was the F3 placeholder + a `// TODO(W18 F4)`)+ a NEW "AppShell chrome present on `/dashboard` `/chat` `/kb` `/eval` `/traces/...` / absent on `/login` `/register` / `/` redirects to `/login`" test + docstring;`visual-baseline.spec.ts` `/dashboard` test comment(F3 placeholder → F4 cards);`tsc --noEmit` exit 0(compile-checks the specs);the full run = user pre-Beta smoke per the R8 `npx playwright install chromium` block(CO_W15_F4_browser_binaries / ADR-0017). Docs:`COMPONENT_CATALOG.md` C09 + C10 `Status` rows + `tests/unit/README.md` + `tests/e2e/README.md` updated. `tsc`+`lint` clean. Deviations(plan §7 changelog D6 + progress Day-6):F8.1 multi-viewport browser smoke deferred → user pre-Beta(R8 / CO_W15_F4_browser_binaries — the responsive *classes* are in place);F8.2 language toggle keeps native `disabled`(not `aria-disabled`);`/settings` has no form inputs so no `<Label htmlFor>`(ProfileRows are text, ThemeToggle + Sign-out are labelled buttons);F8.4 also added `dashboard.test.tsx`(per the F4.5 note "a dashboard render test can join then").

- [x] F8.1 Responsive pass — verified the `<AppShell>` is built responsive at the `md` breakpoint:desktop `<aside md:block>` ↔ mobile off-canvas shadcn `<Sheet side="left">`(opened by the top-bar hamburger `aria-expanded`);the top bar stays at all widths;`<GlobalSearch>`'s `DialogContent` is `w-full max-w-xl`(full-width on mobile <576px);`/dashboard`'s card grid is `grid gap-4 sm:grid-cols-2 lg:grid-cols-3`(1-column on mobile);the AppShell `<main className="p-4 md:p-8">` has tighter mobile padding. Browser smoke at 2-3 viewports = the user's pre-Beta walkthrough(R8 — no Playwright browser binaries / CO_W15_F4_browser_binaries;the responsive classes are the deliverable, the visual check is the smoke)— `(this commit)`(verified by reading)
- [x] F8.2 a11y on the new surfaces — `<AppShell>`:sidebar nav `aria-current="page"` on the active item(verified);hamburger `aria-expanded={mobileNavOpen}`,focus-mode toggle `aria-pressed={collapsed}` + a Collapse/Expand `aria-label`(verified);`<nav aria-label="Primary">`(verified). `<GlobalSearch>`:Radix `Dialog` gives `role="dialog"` + `aria-modal` + focus trap + Escape-to-close;the input is `role="combobox"` `aria-expanded` `aria-controls="global-search-results"` `aria-activedescendant`;the results `<ul role="listbox" aria-label="Search results">` with `<li role="option" aria-selected>`(verified). `/dashboard` + `/settings`:**added `role="heading" aria-level={2}` to the `CardTitle`s**(6 on the dashboard, 3 on settings)— each card title is now a level-2 heading under the page `<h1>`;the dashboard's links have text accessible names("View knowledge bases →" / "Ask a question →" / "Open Eval Console →" / the quick-action labels). `/settings` has no form inputs(the ProfileRows are text spans, `<ThemeToggle>` is a labelled button "Toggle theme", Sign-out is a labelled button)— no `<Label htmlFor>` needed. Disabled language toggle:keeps native `disabled` + `aria-label="Language (multi-language coming soon)"` + `title="Multi-language (JP / ZH) — coming in a later tier"`(native `disabled` is the strongest "unavailable" signal — i18n genuinely doesn't exist Tier 1;a visible-but-AT-announced "Tier 2" affordance is a polish detail per the F1 deviation note)— `(this commit)`
- [x] F8.3 Dark-mode re-check — `Grep '\[oklch'` across `frontend/` = **0**(W15 milestone preserved through F1-F8 — `<AppShell>` / `<GlobalSearch>` / `/dashboard` / `/settings` all use token classes only);the W17 F5 dark-mode mechanism(next-themes `ThemeProvider attribute="class" defaultTheme="system"` in the root layout → `.dark` CSS vars in `globals.css` → tailwind `oklch(var(--token))` utility classes)is unchanged by the restructure. Browser dark-mode smoke on `/dashboard` + `/settings` + the shell chrome = the user's pre-Beta walkthrough(same R8 caveat shape)— `(this commit)`
- [x] F8.4 Vitest unit tests — **3 NEW files** under `tests/unit/`:`app-shell.test.tsx`(4 tests:the 5 sidebar nav modules render under `<nav aria-label="Primary">` / the active route gets `aria-current="page"` / the focus-mode toggle hides the desktop sidebar + flips its label / the top-bar global-search trigger is present;`next/navigation` + `next/link` + `@/lib/api/kb` mocked,`<UserMenu>` "Signing in…" path exercised),`global-search.test.tsx`(4 tests:opening renders the 6 static Page results / typing filters them + appends the "Ask in chat: …" action / selecting it `router.push('/chat?q=…')` + closes / ArrowDown+Enter selects the next result;KB results mocked to `[]`),`dashboard.test.tsx`(2 tests:the `<h1>` + the 5 overview card headings + the 4 quick-action links with the right hrefs;both `useQuery` calls mocked). `pnpm test:unit` → **4 files / 13 tests pass**(was 1 file / 3 tests at the W17 F6 baseline);`tests/unit/README.md` "What's here now" updated — `(this commit)`
- [x] F8.5 Playwright E2E — `tests/e2e/app-shell-path.spec.ts`:the `/dashboard` test rewritten for the **real F4 overview cards**(`<h1>Dashboard</h1>` + the "Knowledge bases" / "System health" / "Quick actions" `role="heading" aria-level=2` titles + the "New KB" / "Open chat" quick-action links — replaces the F3-placeholder + the `// TODO(W18 F4)`)+ a **NEW test** "AppShell chrome is present on app routes and absent on the auth pages"(the `<nav aria-label="Primary">` + the "Search (Ctrl+K)" trigger visible on `/dashboard` `/chat` `/kb` `/eval` `/traces/<id>`,`toHaveCount(0)` on `/login` `/register`,and `/` → `/login` redirect with no chrome)+ docstring updated. `tests/e2e/visual-baseline.spec.ts`:the `/dashboard` test renamed/re-commented for the F4 cards. `tests/e2e/README.md` coverage table row updated. `pnpm exec tsc --noEmit` exit 0(compile-checks the specs);the full E2E run stays the user's pre-Beta smoke per the R8 `npx playwright install chromium` block(CO_W15_F4_browser_binaries / ADR-0017)— `(this commit)`
- [x] F8.6 `docs/02-architecture/COMPONENT_CATALOG.md` — C09(Admin Console UI)`Status` row appended with the W18 amendment(all authenticated views re-parented under a single `<AppShell>`;URLs flattened `/admin/*` → `/kb/*`,`/debug/[traceId]` → `/traces/[traceId]`,`/admin` → real `/dashboard` overview;`/settings` added;V7 Landing removed → `/` redirect;login/register → `/dashboard`;`<GlobalSearch>` Cmd/Ctrl+K palette;`[oklch`=0 preserved;`architecture.md v6 §5` carries the inline-tagged amendment, the catalog mirrors it)+ C10(Chat Interface UI)`Status` row appended(the chat view renders inside `<AppShell>` now — its `<main>`+`min-h-screen` became `<div>`+`h-full`,the in-page header slimmed;reads `?q=` on mount;route `/chat` + SSE logic unchanged)— `(this commit)`

## F9 — Phase closeout + W19+ rolling-JIT trigger

- [ ] F9.1 W18 phase Gate verdict(PASS / PARTIAL PASS / FAIL with explicit rationale per the W12-W15-W17 pattern)— in `progress.md`
- [ ] F9.2 W18 `progress.md` Retro — 7 sections(What worked / What didn't & friction / Surprises / Decisions / Carry-overs to W19+ / Time tracking / Spec ref alignment)
- [ ] F9.3 ADR-0024 status verified `Accepted`(landed at kickoff;verify-no-op)+ ADR-0024 "Implementation Deliverables" D1-D10 checkboxes ticked against the F1-F9 outcomes
- [ ] F9.4 W18 `plan.md` + `checklist.md` + `progress.md` frontmatter `status: active` → `closed`
- [ ] F9.5 W19+ phase folder **NOT pre-created**(rolling-JIT per CLAUDE.md §10 R1)— kickoff candidates noted in `progress.md` Retro(W16 F1-F4 if Track A IT cred lands / Tier 2 prep governance Q12 / Beta-launch readiness pass / the user's local-dev seed-KB convenience task)
- [ ] F9.6 `session-start.md` hygiene catch-up — §3 C09/C10 status(in `<AppShell>`;URL flatten;`/dashboard`;`/settings`;V7 Landing removed)+ §10 W18 row(closed, Gate verdict)+ W19+ not-pre-created + §11 carry-overs + §12 milestones row + Last-Updated + Update-history;`COMPONENT_CATALOG.md` already touched in F8.6
- [ ] F9.7 No new OQ expected(the multi-language affordance is a known §11 Tier 2 item, not a new OQ);if one surfaces → sync `decision-form.md` per R4

---

## Cross-Cutting

- [ ] Each commit references `progress.md` Day-N entry(R2)— `docs(planning):` / `docs(adr):` housekeeping commits exempt(the F0 kickoff cascade = a `docs:` housekeeping-class commit)
- [ ] Component tag in commit message per CC-1 — F1 = C09+C10+C11 / F2 = C09+C10+C11 / F3 = C09+C10 / F4 = C09(+C07/C02/C06 consumers) / F5 = C11+C09 / F6 = C09 / F7 = C09+C11 / F8 = cross-cutting + test harness + docs / F9 = governance
- [ ] OQ status sync to `decision-form.md`(R4)— no new W18 OQ expected(the language toggle is a known §11 Tier 2 disabled affordance)
- [ ] Risk register — no new W18 risk expected;the R8 `npx playwright install chromium` block(CO_W15_F4_browser_binaries / ADR-0017)is pre-existing and only affects F8.5's *run*, not the spec update
- [ ] CLAUDE.md §5.1 H1 check — the IA restructure is **authorized by ADR-0024**(Accepted 2026-05-10);no other architectural change(F1-F8 implement what the ADR + the amended §5.0-§5.11 describe — re-layout + re-route + 2 view changes + Landing removal;NO §3/§4 component change — backend untouched)
- [ ] CLAUDE.md §5.2 H2 check — frontend-only;no new dependency expected(F6 `<GlobalSearch>` — if `cmdk` is genuinely needed → stop-and-ask first;PARTIAL-PASS fallback = shadcn-`Dialog`-based, zero new dep);shadcn/ui + `tokens.ts` unchanged
- [ ] CLAUDE.md §3.2 conventions — `tsc --noEmit` + `next lint` clean on all new/changed files;no `any` / no `@ts-ignore` without a comment;shadcn/ui only;design tokens via `tokens.ts`(no hardcoded colours — `[oklch`=0);App Router only;Server Components default(Client Components have `"use client"` + a comment)
- [ ] CLAUDE.md §5.5 H5 — no secret committed;no hard-coded tenant/subscription/resource;the login-gate uses the existing auth context(no new credential store);no PII / no plaintext-prompt logging changed
- [ ] CLAUDE.md §5.4 H4 — Tier 2 boundary held:the language toggle is a **disabled affordance only**(no i18n machinery);global search is **quick-jump only**(no semantic search-as-you-type);no GraphRAG / multi-agent / multi-tenancy / multi-modal / auto-sync / fine-tune leak
- [ ] Karpathy §1.3 surgical — re-parent + re-route the W12-W15 views;do NOT rebuild their content;do NOT "順手" refactor adjacent code;clean up only W18's own orphans(deleted-Landing components, broken imports from the moves)

---

**Lifecycle reminder**:呢份 checklist 衍生自 `plan.md` deliverables。新加 deliverable 必須先入 plan + §7 changelog,然後再加 checklist item。延後 item 標 🚧 + reason,**唔可以刪**未勾選 `[ ]`。
