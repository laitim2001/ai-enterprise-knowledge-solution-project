# EKP Architecture Decision Records (ADRs)

> Format per `CLAUDE.md` §6。Filename pattern:`NNNN-short-kebab-title.md`(NNNN = 4-digit zero-padded sequential)。

## Index

| ADR | Title | Status | Source |
|---|---|---|---|
| [0001](./0001-build-vs-buy-self-build-ekp.md) | Build vs Buy — Self-build EKP based on D1/D2/D3 differentiators | Accepted | `architecture.md` §13.0 |
| [0002](./0002-project-rename-to-ekp.md) | Project rename to EKP(Enterprise Knowledge Platform) | Accepted | `architecture.md` §13.1 |
| [0003](./0003-multi-format-ingestion.md) | Multi-format ingestion(Word + PDF + PPT) | Accepted | `architecture.md` §13.2 |
| [0004](./0004-layout-aware-chunking.md) | Layout-aware chunking — not character-based | Accepted | `architecture.md` §13.3 |
| [0005](./0005-multi-kb-architecture-day1.md) | Multi-KB architecture from Day 1 | Accepted | `architecture.md` §13.4 |
| [0006](./0006-nextjs-shadcn-day1.md) | Next.js 14 + shadcn/ui from Day 1(replace Streamlit) | Accepted | `architecture.md` §13.5 |
| [0007](./0007-l2-crag-not-l0-l4.md) | L2 CRAG(not L0 / L4+)for Tier 1 | Accepted | `architecture.md` §13.6 |
| [0008](./0008-graphrag-trigger-matrix.md) | GraphRAG via Trigger Matrix(not "Plan B added on") | Accepted | `architecture.md` §13.7 |
| [0009](./0009-frontend-backend-separated.md) | Frontend / Backend separated(not Next.js full-stack) | Accepted | `architecture.md` §13.8 |
| [0010](./0010-dify-read-only-reference.md) | Dify as read-only reference(no fork, no copy) | Accepted | `architecture.md` §13.9 |
| [0011](./0011-custom-crag-vs-langgraph.md) | Custom CRAG vs LangGraph framework — Tier 1 path | Accepted | `architecture.md` §13.11 |
| [0012](./0012-cohere-v4-pro-upgrade-and-gate2-partial-pass.md) | Cohere Rerank v3.5 → v4.0-pro same-vendor model upgrade + Gate 2 PARTIAL PASS verdict landed | Accepted | W6 D5 stakeholder approval cycle 2026-05-05(`architecture.md` v5 → v5.1 amendment) |
| [0014](./0014-hybrid-auth-sso-plus-self-register.md) | Hybrid auth model — Microsoft Entra ID SSO + self-service email register | Accepted | W11 D2 cont stakeholder approval cycle 2026-06-10(`architecture.md` v5.1 → v6 amendment;sister ADR with 0015) |
| [0015](./0015-ui-tier-1-expansion-dify-leaning.md) | UI Tier 1 expansion — 6 views → 9 views + shadcn/ui foundation commit + Dify-leaning aesthetic | Accepted | W11 D2 cont stakeholder approval cycle 2026-06-10(`architecture.md` v5.1 → v6 amendment;sister ADR with 0014) |
| [0016](./0016-password-hash-scrypt-stdlib.md) | Password hashing — argon2-cffi → hashlib.scrypt(Python stdlib)| Accepted | W13 D5 F5 implementation 2026-06-10(R8 corp proxy `pip install argon2-cffi` blocker;user instruction option 1 stdlib path;H2 vendor decision change ADR per strict reading)|
| [0017](./0017-r8-corp-proxy-mitigation-pattern.md) | R8 — Ricoh corp-proxy mitigation pattern(dependency-add discipline:prefer stdlib > managed REST > lazy-imported optional dep;declare-but-defer-install for binary-heavy assets;stop-and-ask when unavoidable)| Accepted | W17 F1 2026-05-10 — 5th cumulative R8 occurrence(`pip install psycopg[binary]` blocked)+ vendor-decision pivot point both met → formalization trigger;coding-discipline ADR(no §3.2 vendor change;no IT action required)|
| [0018](./0018-multi-kb-kb-id-propagation.md) | Multi-KB `kb_id` propagation — Tier 1 commit via dynamic `index_name` injection(reaffirm ADR-0005)| Accepted | W15 D5 closeout audit `audit-W15-d5-vs-spec.md` §CC-1 trigger 2026-05-09;P0.1 P0 batch decision(reaffirm not supersede ADR-0005;Option B b2 Dynamic injection chosen over Option A scope reversal + Option C hybrid + (b1) per-KB instance map);Phase 3 implementation W16+ active flip ~1.5-2 days |
| [0019](./0019-pdf-parser-tier-1-deliver.md) | PDF parser Tier 1 deliver via Docling DocLayNet(reaffirm ADR-0003 multi-format ingestion)| Accepted | W15 D5 closeout audit `audit-W15-d5-vs-spec.md` §1.2 C01 Major drift #1 trigger 2026-05-09;P0.2 P0 batch decision(reaffirm not supersede ADR-0003;Option A deliver chosen over Option B Tier 2 defer;Tier 1 scope = text-extractable PDF only,scanned/encrypted defer Tier 2);Phase 3 implementation W16+ active flip ~1 day |
| [0020](./0020-context-expander-tier-1-deliver.md) | Context Expander step Tier 1 deliver(reaffirm architecture.md §3.1 + V6 9-stage spec via code catch-up)| Accepted | W15 D5 closeout audit `audit-W15-d5-vs-spec.md` §1.5 C05 Major drift #3 + §CC-3 trigger 2026-05-09;P0.3 P0 batch decision(reaffirm not amend §3.1 + §5.7;Option A deliver chosen over Option B amend spec + Option C split delivery;internal-spec inconsistency resolved by code + V6 + C05 design note 5-source align);Phase 3 implementation W16+ active flip ~1.5 days(sequential dependency on ADR-0018 Multi-KB kb_id wiring)|
| [0021](./0021-retrieval-testing-tab-and-search-mode.md) | V4 Retrieval Testing tab §5.5.4 deliver + `HybridSearcher` search-mode parameter(Vector / Full-Text / Hybrid)| Accepted | W15 D5 closeout audit `audit-W15-d5-vs-spec.md` 偏差 #5 / §7 P2.1 trigger;2026-05-10 user-approved(Option A deliver code chosen over Option B amend §5.5.4 + Option C frontend baseline-only;`HybridSearcher.search()` `mode` param touches C04 §3 RAG Core interface → H1 ADR per strict reading;Voyage/ZeroEntropy dropdown items confirmed dropped per ADR-0012)|
| [0022](./0022-auth-transport-hardening.md) | Auth-transport hardening — httpOnly cookie + CSRF double-submit + `/auth/refresh` rotation(amends ADR-0014 transport layer;dual-path `get_current_user` cookie OR Bearer)| Accepted | W17-beta-hardening phase;2026-05-10 user-approved(AskUserQuestion「W17 做 — 寫 ADR + 實作」);closes carry-overs CO_F5_refresh + CO_F5_cookie;H1 ADR(transport-layer change;no new dependency)|
| [0023](./0023-kb-manager-persistent-backing.md) | KB Manager + users_repo persistent backing — Postgres via `psycopg`(reuse docker-compose postgres + dedicated `ekp` DB;`PostgresKBBackend` satisfies `KBStorageBackend` Protocol;in-memory fallback when `DATABASE_URL` empty)| Accepted | W17-beta-hardening phase;2026-05-10 user-approved(AskUserQuestion「Postgres via psycopg」over stdlib sqlite3 / aiosqlite / Cosmos / defer);closes carry-over CO18;H1 storage-layout change + H2 new dependency(`psycopg[binary]`)|
| [0024](./0024-unified-application-shell-ia.md) | Unified application shell IA — `<AppShell>`(top bar + left sidebar + main content)across all authenticated views;**no public marketing landing**(`/` → redirect `/login`);**no "admin" framing**(flatten URLs);login-gate;`login → /dashboard`(new real overview view);auth pages stay outside the shell | Accepted | W17-beta-hardening closeout local-dev-test session 2026-05-10 — stakeholder IA-expectation gap vs the Dify-leaning 3-regime split per ADR-0015;H1 layout-philosophy change → ADR required. Chris answered Q1-Q5 2026-05-10(Q1 remove marketing landing — EKP 非對外 / Q2 真 overview dashboard / Q3 sidebar = functional modules + top bar = global search·lang·theme·profile / Q4 no admin → flatten / Q5 獨立 W18 phase);Q6 = ADR-0015 relationship,explained in the ADR("amends ADR-0015" 3 specific ways:remove V7 Landing / unified `<AppShell>` + drop admin / real `/dashboard`;preserves auth-page designs + shadcn/ui foundation + EKP visual identity + W12-W15 implementation;multi-language stays Tier 2 per §11 — top-bar toggle is a disabled affordance). **Accepted 2026-05-10** — Chris directed the post-acceptance cascade(`W18-app-shell-ia` phase kickoff(`plan.md` per §10 R1)+ `architecture.md v6 §5` amended(§5.0 Application Shell added / §5.3「Admin Dashboard」→「Dashboard」/ §5.9 Landing removed / §5.7「Debug View」→「Traces」/ `/admin/*` → `/kb/*` flatten — inline-tagged amendment per the §3.4/§3.7 precedent, doc version held)+ ADR-0015 References get the amended-by note);implementation = the `W18-app-shell-ia` phase(~1-1.5 weeks) |
| [0025](./0025-kb-detail-8-tabs-expansion.md) | KB Detail 5→8 tabs expansion — Documents · Chunks · **Images** · **Chunking Lab** · Pipeline · Retrieval Testing · **Access** · Settings(amends `architecture.md v6 §5.5`;C02 + C03 scope;Access tab activates Wave C1 with ADR-0027 Option A full RBAC) | **Accepted + Wave A implemented** | W19 F6 Chris approval 2026-05-16(consensus);**Wave A shipped 7-tab `-Access` per W20 F5**(Chris AskUserQuestion A1 pick 2026-05-16 + W19 F4 §3.6 trigger — ADR-0027 Option A ~20 backend days 唔 fit Wave A budget,Access tab activates Wave C1)+ Images/Chunking Lab NEW tabs shipped + Access tab disabled affordance OUTSIDE `VALID_TABS` |
| [0026](./0026-settings-6-tab-hub-and-connections-backend.md) | Settings v1 thin → 6-tab hub + Connections backend — all 6 tabs **fully editable Tier 1**(amends `architecture.md v6 §5.0`)+ Key Vault SDK secret rotation + provider CRUD + Test connection × 9 providers | **Accepted (Option B fully editable)** | W19 F6 Chris pick 2026-05-16;~22 NEW backend endpoints + Key Vault SDK new dependency(H2 trigger;R8 mitigation per ADR-0017);combined with ADR-0027 Option A = ~42 backend days Wave C **MUST split into C1+C2** per F4 §3.6 |
| [0027](./0027-users-tier-1-5-rbac.md) | /users Tier 1.5 RBAC NET NEW — Members + Roles & permissions matrix + Groups + Audit log(promotes RBAC from `§3.7` Tier 2 hook to Tier 1.5 full RBAC) | **Accepted (Option A full RBAC)** | W19 F6 Chris pick 2026-05-16;~20 NEW backend days + 6 NEW Postgres tables(roles + role_permissions + groups + group_members + audit_log + kb_acl)+ Entra Graph SDK(H2 trigger;R8 mitigation per ADR-0017)+ ACL middleware + audit log writes + new C16 Users Service;hard dep for ADR-0025 Access tab(unlocks 8-tab KB Detail with full ACL)|
| [0028](./0028-kb-new-5-step-wizard.md) | /kb/new 5-step wizard + Multimodal Tier 1/2 split + /kb-upload re-ingestion coexistence(amends `architecture.md v6 §5.5.3`)| **Accepted + Wave A implemented** | W19 F6 Chris approval 2026-05-16(consensus);Wave A backend KbConfig +4 multimodal fields landed W20 F4.1 + orchestrator branch F4.2 + `/kb/new` 5-step wizard W20 F4.4 + `/kb/[id]/upload` 3-step rebuild W20 F6;rule-of-3 wizard primitive promotion trigger NOW hit(Wave B+ shared `<Stepper>` candidate)|
| [0029](./0029-doc-detail-3-pane-layout.md) | /doc-detail 3-pane layout — outline · chunks · inspector with embedding vector preview(amends `architecture.md v6 §5.5.2`)| **Accepted (Option C `/kb/[id]/docs/[docId]`)** | W19 F6 Chris pick 2026-05-16(nested-under-KB URL for IA consistency with ADR-0024 flat URL);Wave B backend:GET /kb/{kb_id}/docs/{doc_id} enriched ~1d |
| [0031](./0031-chat-advanced-surfaces.md) | Chat advanced surfaces — Conversation History sidebar + 3 citation placement modes + InlineImageCard + ImageGallery + CitationPill hover + FeedbackBar comment + CRAG strip;**Conversation History server-side Tier 1**(amends `architecture.md v6 §5.2 Chat` + C10 §7 promote Tier 2 to Tier 1) | **Accepted + Wave A implemented (Option B server-side Tier 1)** | W19 F6 Chris pick 2026-05-16;**Wave A shipped W20 F3**:Postgres `conversations` + `messages` tables + 6 `/conversations` CRUD endpoints(12/12 pytest pass)+ 7 NEW frontend components(ConversationHistory + InlineImageCard + ImageGallery + CitationPill + FeedbackBar + CragStrip + chat/page rewrite)+ 3 citation modes;ADR-0030 + ADR-0032 absorbed |
| [0033](./0033-chunker-low-value-tuning.md) | Chunker low-value tuning — `_TOKEN_LOW_VALUE_FLOOR` 100 → 60 + adjacent-short-merge post-process(amends `architecture.md §3.3` chunking philosophy soft floor)| **Accepted** | W25-image-association-deep-fix phase F1 deliverable 2026-05-23;empirical signal `sample-doc-with-image-1` 121 chunks 60% low_value flagged → image-bearing chunks 機率上 < 100 token 被 Azure Search server-side filter `low_value_flag eq false` hard-drop → root cause of image-text association gap surfaced post-BUG-009/010/011 cascade;Path III phase-level full optimization first deliverable;Chris approved as-is 2026-05-23(W25 F1.1.2)|
| [0034](./0034-query-expansion-rag-fusion.md) | Query expansion + RAG-Fusion — multi-variant reformulation via GPT-5.5-mini + RRF fusion(amends `architecture.md §3.7` query orchestration;adds `enable_query_expansion: bool=False` global Settings flag,`backend/pipeline/query_reformulator.py` + `backend/retrieval/result_fusion.py` NEW modules,P95<5s hard cap)| **Accepted** | W25-image-association-deep-fix F3 deliverable;Chris approved chat 2026-05-24「可以開始執行 ADR-0034」;Day 3 user real-world Q-W25-I07「Show me all the Integration scenarios」returns 2/5 + 0 images = empirical evidence single-hop hybrid retrieve insufficient on vocab-divergent multi-section queries;6 alternatives rejected(L4+ multi-agent / multi-query concat / HyDE / per-KB flag / local LLM / skip-D4)|
| [0035](./0035-retrieval-low-value-soft-relax.md) | Retrieval low-value soft-relax — server-side OData filter `low_value_flag eq false` clause 移走 + client-side Python post-filter score weighting × 0.7(amends `architecture.md §3.6` line 384 filter clause + §3.5 line 258 "deboost" spec wording alignment;`Settings.retrieval_image_low_value_weight: float = 0.7` knob;NEW `_apply_low_value_post_filter` helper)| **Accepted** | W25-image-association-deep-fix F5 D2 deliverable + CH-003 co-shipped 2026-05-24;closes spec-implementation divergence(spec says "deboost" but W2 baseline did hard exclude);H1 boundary surfaced via R6 D0 finding (iii) → Chris AskUserQuestion 2-step pick:Path B(ADR-0035 mandatory)+ Accept both content gate;6 alternatives rejected(keep-baseline / hard-retain-no-weight / OData-OR-helper-field / per-KB-config / eliminate-low_value-flag / move-to-reranker);governance symmetry with ADR-0022 / ADR-0033(mechanism change with semantics preserved → ADR)|
| [0036](./0036-react-markdown-chat-answer-rendering.md) | react-markdown frontend dep add for chat answer markdown rendering(adds frontend H2 vendor lock row;BUG-021 chat answer rendering 4-fix batch enabling backend markdown emit to be formatted in `<MessageRow>`)| **Accepted** | BUG-021 user AskUserQuestion 2026-05-24 W25 D2 cont — picked Recommended over inline regex transform / backend prompt downgrade alternatives;npm-registry non-binary install path same as W24b F1 react-hook-form precedent,no R8 mitigation needed;default GFM-subset coverage sufficient for Tier 1 LLM markdown emit |
| [0037](./0037-parent-document-section-retrieval.md) | Parent-Document / Section-Level Retrieval — Tier 1 ceiling for enumeration queries via section_path-based runtime aggregation post-rerank(amends `architecture.md §3.1` query pipeline + reuses ADR-0020 chunk-aggregation pattern;NEW `backend/generation/parent_doc_retriever.py` + 6 NEW Settings;feature-flag default-off `enable_parent_doc_retrieval=False`)**·· amended 2026-05-26 W28 F4** Settings full default flip(max_tokens 4000→2000 + top_k 1→2)per Setting sweep best combo Run 3.A(G1+G2+G4+G5 PASS + G2 超 F1 baseline +1.61pp) | **Accepted; amended W28 F4** | W26-eval-driven-retrieval-tuning F2 PIVOTED 2026-05-25 D1 — F1 RAGAs baseline(`recall_at_5=0.8744` + 5/13 `context_recall=0.00` recall-dominant)+ threshold-probe(Cohere v4.0-pro scores P25=0.83 / min=0.67 cannot differentiate passed vs failed queries)empirically refuted brief §3 方向 A rerank threshold premise → Chris pivot pick (C) escalate brief §6 step 4 parent-doc retrieval ADR;Tier 1 architectural ceiling for「show me all」enumeration query mismatch(per brief §1 「NOT EKP-unique — Dify same query also fails」);zero schema change(section_path already indexed Collection(Edm.String) filterable);H1 trigger — Chris AskUserQuestion 2026-05-25 D1 cont approved Q4 Default OFF + Q1 top_k=1 + Q2 depth=1 + Q6 Both on(all Recommended picks)+ Q3/Q5/Q7/Q8 proposed defaults batch-locked;W26 F2 implementation gate open |
| [0038](./0038-parent-doc-dispatch-append-mode-finding.md) | Parent-Doc Dispatch Chain Append-Mode Finding — Hypothesis Partial Validation + Settings Default Preserve "replace"(NEW Setting `parent_doc_dispatch_mode: Literal["replace", "append"] = "replace"` + prompt_builder branch + 4 NEW unit tests preserved as W28+ enabler;append mode 大幅修復 W26 F2 G replace 嘅 catastrophic regressions but G1 + G4 marginal MISS by < 1pp vs F1 baseline;Phase Gate PARTIAL per Q4 measurement-experiment-fail-policy)**·· reaffirmed 2026-05-26 W28 F4** dispatch=replace VALIDATED at correct Settings(Run 3.A best combo G2 超 F1 baseline + G4 full PASS — W26 F2 G catastrophic root cause reframed = wrong Settings combo,非 dispatch=replace 本身)| **Accepted; reaffirmed W28 F4** | W27-parent-doc-dispatch-experiment F3 closeout 2026-05-25 D3 — Chris α pick PARTIAL closeout per plan §3 Gate verdict policy(G1 faithfulness 0.9591 vs F1 ±2pp [0.9651, 1.0] MISS 0.6pp / G4 Q-W25-I01 control 0.64 vs F1 ≥0.65 MISS 0.01pp / G2+G3+G5 PASS — Q-W25-I07 critical recovery 0.00 → PASS D1.35 H1 validated);D1.35 hypothesis 4-axis re-evaluation:H1 citation invariant breakage VALIDATED PARTIAL / H2 attention dilution PARTIALLY CONFIRMED(emerging primary residual)/ H3 chunk_id drift REFUTED / H4 dispatch variable VALIDATED;W28+ candidates (b) `max_tokens_per_parent` sweep + (c) RAGAs orchestrator-aware tune elevated as highest signal-to-cost priority |
| [0039](./0039-hybrid-semantic-ranker-toggle.md) | Hybrid Mode Semantic Ranker as Configurable Layer — NEW Setting `hybrid_use_semantic_ranker: bool = True`(default preserve W2 baseline)+ `HybridSearcher` toggle;flag False → hybrid drop `queryType="semantic"` 變 BM25+vector+RRF(Azure 自動 fusion)→ Cohere rerank;enable Free-tier 全 pipeline + chat UI test bypass semantic ranker monthly quota 402;semantic ranker per Q21 已證 inferior to Cohere + 被 Cohere override redundant;H1-adjacent(改 §3.1 search behavior)需 Gate 1 R@5 re-verify safety gate | **Proposed**(pending Chris confirm + Accept)| W42-hybrid-semantic-ranker-toggle F0 起草 2026-05-29 — Chris「起 W42 plan 做 Option ①」pick post 三方 trade-off 分析(① drop semantic ranker / ② pgvector H2 swap / ④ Basic $75);402 root cause = semantic ranker NOT vector DB;Option ① $0 + 唔換 vendor + 可逆;F1 retrieval code GATED on H1 confirm + ADR Accept per CLAUDE.md §5.1 |
| [0040](./0040-per-kb-tunable-retrieval-config-scope.md) | Per-KB Tunable Retrieval / Citation Config — config-scope resolution model(per-query > per-KB `KbConfig` > 全域 `Settings` default);把 parent_doc / citation_expansion / neighbour-image 旋鈕 + NEW `max_images_per_answer` 降到 per-KB(Optional 欄位,`None` = inherit,production-preserve)+ on-platform full-pipeline 試跑 harness;MVP runtime-only(chunker 圖洪水深層修 split out W44+ ADR-0041);H1-adjacent(改 §3.1/§3.7 config 解析方式)需 stakeholder scope | **Accepted**(W43 F0 0.5 gate PASS 2026-06-01 — Chris confirm H1 + scope)| W43-per-kb-tunable-retrieval-config F0 起草 2026-06-01 — 2026-06-01 AR 文件 smoke 揭全域配置 content-coupling + 證偽實驗(Fork A 確認 per-KB 必要 / Fork B 未證實 / 圖洪水 ingestion-bound / 試跑 variance ~20%)+ 用戶 foundational vision;formalize ADR-0035/0037 兩度 defer 嘅 per-KB config;F1 code GATED on ADR Accept per §5.1 |
| [0041](./0041-chunker-image-density-deep-fix.md) | Chunker Image-Density Deep Fix — 圖片隨 `doc_order` 分配落 sub-chunk(非 pile-on 整個 section)+ `chunker_max_images_per_chunk` soft cap(amends `architecture.md §3.3`;root = `layout_aware.py:146/207/248` image pile-on,實測單 chunk 57 圖;per-image section 透過細分自然繼承,reuse BUG-026;re-index required;守 H4 — 章節切分 Tier 1,視覺語意揀圖 Tier 2 out-of-scope)| **Accepted**(W44 F0 gate PASS 2026-06-03 — Chris 揀切法 D 混合 + cap 8 + Accept)| W44-chunker-image-density-deep-fix F0 起草 2026-06-03 — ADR-0040 R6 catch 5 split-out 圖洪深層修;ingestion-bound 根因(W43 #8 Test C 證 runtime 解唔到根);[AUDIT-A/B] 校準(實測 `ci=15=57`;doc-level reindex 驗證唔卡 Track A);F1 code GATED on ADR Accept per §5.1 |
| [0042](./0042-per-kb-ingest-time-chunker-image-cap.md) | Per-KB Ingest-Time Chunker Image Cap — 把 ADR-0040 config-scope 延伸到 **ingest-time**;`KbConfig` 加 `chunker_max_images_per_chunk: int \| None = None`(`None`=inherit 全域 / 正整數=per-KB cap,**per-KB 不能設無 cap**);ingest 時 `_run_ingest_pipeline` 解析 `cap_override`,設咗值經 `app.state.make_ingestion_chunker` factory 砌 per-ingest chunker,否則用全域 singleton(inherit,bit-identical);amends `architecture.md §3.3` + §4.5;re-index required;UI 暴露 out-of-scope(roadmap W45 較大 candidate)| **Accepted**(W45-per-kb-image-cap kickoff 2026-06-04 — Chris AskUserQuestion 批准 H1 + ADR-0042 + None 語意揀「None=繼承,正整數=cap」)| W45-per-kb-image-cap F0 kickoff 2026-06-04 — roadmap W44 carry-over「per-KB 圖數 cap 降 KbConfig」;延伸 ADR-0040(query-time only)+ ADR-0041(全域 cap)到 per-KB ingest-time;reuse W20 F4.2 已 thread 嘅 `kb_config` ingest 路徑;F1 code GATED on ADR Accept per §5.1 |
| [0043](./0043-original-file-storage-and-kb-level-reindex.md) | Original-File Blob Storage + Real KB-Level Reindex — NEW source-document blob container `ekp-kb-{kb_id}-sources`(ingest 持久化原檔,best-effort);`POST /kb/{kb_id}/reindex` stub→真(iterate `list_documents` → 由 source container 攞原檔 → re-ingest;同步 summary;pre-W46 無 source 嘅 doc skip+report);`_run_ingest_pipeline` refactor 接受 file path/bytes;production-preserve(零 breaking,新增 best-effort source-upload step);v1→v2 原子切換 + embedding_model unlock + H7 frontend mockup unlock 皆 out-of-scope | **Accepted (H1 backend 方向)**(W46 kickoff 2026-06-04 — Chris AskUserQuestion 揀「原始檔儲存 + UI 一次過」;**H7 frontend mockup design = 獨立 sub-gate**,frontend code 前另需確認)| W46-ingestion-config-and-reindex F0 kickoff 2026-06-04 — R6 grep 揭 ingestion-config UI 自助 loop 真 blocker = 原始檔冇存(非 Track A);unblock W42/W45 per-KB chunk config apply 落 existing KB;backend code GATED on ADR Accept per §5.1,frontend GATED on H7 design confirm per §5.7 |
| [0044](./0044-heading-aware-chunk-strategy.md) | heading_aware chunk strategy(section-bounded)+ chunk_strategy ingest wiring — 實作 `heading_aware`(thin `HeadingAwareChunker(LayoutAwareChunker)` subclass:`target_tokens=hard_cap`[取消 sub-target split] + `min_chunk_merge_floor=0`[取消 merge],仍保 hard_cap split + image-cap + table + section-walk → 更粗/少 chunk)+ wire `chunk_strategy` 入 `strategies.select_chunker`(移除 `NotImplementedError`)+ `documents.py _select_chunker`(按 `kb_config.chunk_strategy` dispatch,strategy≠heading_aware bit-identical fall-through);順帶 close W46 reindex `chunk_strategy` over-promise gap;amends `architecture.md §3.3/§3.5/§4.5` | **Accepted**(W53 kickoff 2026-06-06 — Chris AskUserQuestion 揀比較軸=實作 heading_aware 真 strategy + Recall=per-config 重生 QA)| W53-chunk-strategy-recall-comparison F1 — R6 grep 揭三發現(chunk_strategy degenerate / `_select_chunker` ignore strategy / 跨 reindex chunk_id 變);接 chunker lineage 0041/0042/0043;heading_aware section-bounded 隔離 target-balancing+merge 變量;F2 code GATED on ADR Accept per §5.1 |
| [0045](./0045-contextual-retrieval-section-context-injection.md) | Contextual Retrieval — section-context injection for rerank + embedding — rerank document(`cohere.py` + `azure_semantic.py`)+ ingest-time embedding input 由純 `chunk_text` 改為 `"<section_path join ' > '>\n<chunk_text>"`(共用 helper;section_path 空→fallback 純 chunk_text 零 regression);**stored `chunk_text` 維持原文**(citation / listing / Finding D 圖排序不受影響),只有 embedding 向量 bake context + rerank document query-time 加 context;解 reranker 只餵 chunk_text → generic section heading(「System Instruction for each step」repeated across GL02/GL03/GL05)令章節混淆嘅根因;embedding 端要 re-index 受影響 KB;amends `architecture.md §3.2/§3.6` | **Accepted**(CH-008 2026-06-08 — 74-chunk Cohere rerank A/B 實驗 PASS:off-topic GL05/GL02 清走、GL03-Create 升頂;Chris 批准 H1 + 全套 rerank+embedding scope;Chris Accept 2026-06-08)| CH-008-contextual-retrieval-rerank-embedding — 接 BUG-034 問題1 診斷(presentation 層 Finding D 已 merge,本 ADR 解底層 rerank/embedding 層);Cohere/Azure embedding 維持原 vendor(無 H2);code GATED on ADR Accept per §5.1 |
| [0046](./0046-chat-image-relevance-decorative-dims.md) | Chat image relevance — decorative filter + dims probe + query-relevance ordering — 三項圖片質素改動:① ingest probe PNG IHDR 尺寸(stdlib,零新 dep)+ 標 `decorative`(min(w,h)<64px)+ chat display filter 走裝飾燈泡 icon ② frontend `INLINE_IMAGE_CAP` wire per-KB `max_images_per_answer`(null fallback 8)③ 圖按 owning citation `relevance_score` 揀入 cap(最相關行先)+ cap 內 document-order(Finding D)顯示;**H4 硬邊界:相關性只用文字 rerank 信號,無 image embedding / multimodal(Tier 2 禁)**;要 re-index 受影響 KB populate dims;amends `architecture.md §3.3/§3.5/§3.6` | **Accepted**(CH-009 2026-06-08 — 接 BUG-034 問題1 圖片維度,CH-008 文字已 merged;Chris 揀「一個 CH 三項一齊」+ OD-1/2/3 鎖定 + Accept;V3 live PASS:40 unique images 0 dims-miss、93×62 燈泡判 decorative、39 內容圖保留;**OD-4 amendment 2026-06-08** — decorative filter 加「大正方形 icon」維度 `max(w,h)≤512 && aspect≤1.15`,捉 384×384 燈泡,live verified)| CH-009-chat-image-relevance-and-quality — #3 H4 邊界明文(文字信號 only) |
| [0047](./0047-chapter-overview-image-lead-and-step-completeness.md) | Chapter-overview image lead + step-image completeness — 為 drive-images-1 開 per-KB 完整性 config(`enable_citation_neighbour_images=true` + `citation_neighbour_section_path_prefix_depth=1` + 調大 `citation_neighbour_max_aux_images`;可選 + parent_doc / expansion = memory 已驗證組合)→ 同章節全部 §X.x 步驟圖 + §X.1 概覽圖入候選池;前端既有 document-order 自動令概覽 lead;**一套 section-aware attach 機制解「補齊步驟圖」+「概覽行頭」兩 goal**;只改 drive-images-1 per-KB(隔離可逆,**不** flip 全域 / **不**改 rerank_k);**H4 硬邊界:只用 section/文字信號,無 image embedding/multimodal**;runtime config 零 re-index;frontend 零改動;amends `architecture.md §3.6/§3.7` | **Accepted**(CH-010 2026-06-08 — 接 CH-009 衍生 + 用戶「做到足」;Chris AskUserQuestion 揀 Approach 1 per-KB 完整性 config over Approach 2 +明確 pin;Chris Accept 2026-06-08;跨文件 30-query eval 必須)| CH-010-chapter-overview-image-lead — grounding:drive-images-1 完整性 knobs 全 null(關)+ rerank_k=5 + cap=20 實測 |
| [0049](./0049-section-fair-image-distribution.md) | Section-fair 圖片分配(neighbour-image attach 跨 sub-section round-robin)— `_find_section_neighbour_images` 由「純 document-order front-load」改「跨 sub-section round-robin 取圖」(候選按 `section_path[:depth+1]` 分組,`max_aux` budget 輪流跨組,組內維持 doc order)→ `max_aux=18` spread 過 §3.1.1-§3.1.5,解尾段步驟(Approve/Reject §3.1.4 + Post §3.1.5)零圖;`cap_images_per_answer` section-fair = conditional 次修(視主修實測);**維持 cap=20**(唔升,守圖洪水紀律);退化保證(單層 sub-section / depth=0 bit-identical);**H4:只用 section_path/doc_order/source_section 文字信號,無 image embedding**;**非** re-index/schema/frontend/vendor/8-view layout 改;延伸 ADR-0048「揀圖模式」;amends `architecture.md §3.7` | **Accepted**(CH-012 2026-06-09 — Chris approve 方向 A section-fair over B 純升 cap / C A+升 cap;實測根因 = 20 圖全堆 §3.1.1 lead + 兩重 front-loading cap 餓死尾段) | CH-012-section-fair-image-distribution — 接 CH-011/ADR-0048(C-1 已 merge),實作 platform P1 / Gap C 之 C-2 完整性;grounding:2026-06-09 /query drive-images-1 診斷 13 citations 20 圖全在 §3.1.1、§3.1.4/3.1.5 零圖 |
| [0048](./0048-per-image-doc-order-and-document-span-selection.md) | Per-image `doc_order` propagation + document-order 圖片排序/揀圖(platform P1 / Gap C)— `ImageRef` 加 `doc_order`(ingest orchestrator 由 `"img@<N>"` key stamp,純 propagate 零新 extraction)+ 序列化入 `embedded_images_json` + `parse_embedded_images` 讀 + frontend `dedupeCitationImages` 改真文件位置 `doc_order` 主鍵(source_section fallback)→ 解 §3.1.3 步驟圖 section 內排唔到頁次(Q3)+ 修多位數 sub-section lexical bug;`_find_section_neighbour_images` cap 選擇 nearest-first → document-span(改善 Q1 完整性覆蓋);**per-DOC cap 粒度留 P2**;**H4:只用 doc_order/section/文字信號,無 image embedding**;**H7:frontend 排序屬 reverse-drift fix 例外**;re-index drive-images-1;amends `architecture.md §3.6/§3.7/§4.5` | **Accepted**(Chris 2026-06-08)| CH-011-per-image-doc-order-completeness — 實作 `per-document-config-platform-design.md` P1 / Gap C(layer C 地基);grounding:`ImageRef` 缺 doc_order + `doc_order` ingest 已有但 orchestrator stamp 時掉咗 |
| [0051](./0051-doc-detail-per-document-config-surface.md) | Doc-Detail Per-Document 配置 surface(platform P2b / Gap A UI;design-stage expansion)— doc-detail 頁(`/kb/[id]/docs/[docId]`)由單 3-pane chunk inspector 加 tab strip:[Chunk inspector](視覺不變)+ [Per-doc 配置](新);per-doc 面**只暴露 post-retrieval 旋鈕**(`answer_detail` / citation expansion / neighbour images / max_images / overview_pin),檢索入口旋鈕(top_k/rerank/parent_doc)用 explanatory affordance 指去 KB 設定(per ADR-0050);**繼承 KB**(非全域)語意 + 「還原至 KB」affordance + per-doc config-test(`config-test` body `doc_id`);忠實 mirror KB `TabKbSettings` tuning pattern(`KbTuneGroup`/`KbTuneKnob`/`KbTestResultCard`);消費 W57 已 merge CRUD API,**零後端改動**;**H7 gate** — 先擴 mockup(`ekp-page-doc-detail.jsx`)+ 用戶 review,frontend build(F2-F4)gated on approve,build 後 visual fidelity 仍受 H7 約束;amends `architecture.md §5`(view layout)| **Accepted**(W58 2026-06-09 — 用戶 AskUserQuestion 揀路徑 A 先擴 mockup;mockup review PASS → Accepted;frontend F2-F4 built + tested)| W58-per-doc-config-ui — 實作 `per-document-config-platform-design.md` P2b;ADR-0050 UI 實現;DESIGN_README design-stage expansion ADR-route precedent(KB Detail 5→8 / Settings v1→6 / `/users`)|
| [0050](./0050-per-document-config-scope-and-dominant-doc-resolution.md) | Per-Document 配置範圍 + 主導 doc 解析策略(platform P2a / Gap A)— config 解析鏈由 per-KB 擴一層做 `per-query > per-DOC > per-KB > global`;`DocConfig`(新 schema)**只含 post-retrieval 旋鈕**(`answer_detail` / `citation_expansion_*` / `citation_neighbour_*` / `max_images_per_answer` / `enable_chapter_overview_pin`)— 檢索入口旋鈕(`top_k`/`rerank`/`parent_doc`)維持 per-KB(引文未知前已消耗);用**主導 doc**(reranked chunk set 出現最多嘅 doc_id;單文件 query = 唯一 doc = 全 per-doc)解析;pipeline 喺 `/query` + `/query/stream` retrieve 後、synth 前若 dominant doc 有 config 則重 resolve(downstream `effective.*` 不變);獨立 `DocConfigStore`(新表 `document_configs(kb_id,doc_id,config JSONB)`)平行 `make_kb_backend` factory(`DATABASE_URL` unset → in-memory);config-test 加 `doc_id` scope;**H4:單 KB 內配置非 multi-tenancy**;production-preserve(`doc_config=None` bit-identical);**非** re-index/vendor 改;**UI 拆 P2b**(mockup 無 per-doc config surface = H7);延伸 ADR-0040 config-scope;amends `architecture.md §3.6/§3.7` | **Accepted**(W57 2026-06-09 — 用戶 AskUserQuestion 揀 Q1 主導 doc + 後處理旋鈕 over 逐引文 owning-doc / 全旋鈕 per-doc;Q2 後端先行 P2a UI 拆 P2b)| W57-per-doc-config-backend — 實作 `per-document-config-platform-design.md` P2 / Gap A(vision 核心);grounding:`EffectiveConfig` request 入口 resolve 一次 + 檢索入口旋鈕雞同蛋 + doc-detail mockup 無 config 面 |
| [0052](./0052-chat-rag-quality-flag-production-default-flip.md) | Chat RAG 質素旋鈕 production default flip — `enable_parent_doc_retrieval` False→True + `citation_expansion_max_aux` 2→10 + `citation_expansion_section_path_prefix_depth` 0→1(把 BUG-026 #1 列舉型 query 完整性 fix 由 `.env` override 升為 production default);解析鏈不變(per-KB / per-doc / per-query override 仍可覆蓋);flip ADR-0037 / W32 / W37 嘅 measurement-first default 立場;**非** architectural change(settings default tuning,不改 vendor / storage / component / interface)| **Accepted**(DD-4 2026-06-11 — 用戶拍板 production flip;eval 背書:單檔 13-query + 跨檔 30-query,recall 1.0 / 0 regression / +413ms p95)| DD-4(`CONFIG_PLATFORM_W43-W58_ROLLUP.md` §3.2 + `DEFERRED_REGISTER.md`);memory `project_chat_demo_rag_quality_followups` #1;疊加 DD-7 synth-timeout 風險 |
| [0053](./0053-synthesizer-request-timeout-raise.md) | Synthesizer request timeout 30s→120s — `synthesizer_request_timeout_s` default 由 30 上調 120(`settings.py` + `synthesizer.py` constructor 兩處 mirror);解 W59 圖密 mega query(Q001/Q036 預期 65 圖含 S04 44-圖 section)喺 production 30s 下 `APITimeoutError`;根因 code 驗證 = output-bound generation 時間(image bytes 不進 synth prompt、context 縮減方向否決、reverse DD-4 風險),120s = streaming safety-net 上限非感知延遲;**非** architectural change(client timeout tuning,不改 vendor / storage / component / interface)| **Accepted**(DD-7 2026-06-11 — 用戶揀方向 A 全局上調 + ADR;根因驗證 image 不進 prompt + DD-4 疊加擴大 30s 風險面)| DD-7(`DEFERRED_REGISTER.md` + `CONFIG_PLATFORM_W43-W58_ROLLUP.md` §3.2);ADR-0052 疊加來源;Alternative 3 per-KB timeout 留 DD-7 future |
| [0054](./0054-dedup-before-cap-image-budget.md) | Dedup-before-cap — `cap_images_per_answer` 預算由計 refs 改**計 unique 圖**(key = `checksum_sha256 or blob_url`;重複 ref 剪走唔食預算,即使預算未盡;`max_images=None` 路徑 bit-identical production-preserve;citations 永不 drop)+ drive-images-1 cap 50→70 配套;解 W66/W67 證據鏈:前置 citation 高重複 neighbour aux(150 refs = 48 unique,68% 重複)食盡預算令後面 22 條 **cited** chunks 自有圖清零 = mega-query recall 0.74 封頂真兇(行到第一張漏網需 cap≈360);supersede ADR-0040「blunt no-dedup」實作決定(其餘不變);章節收割新機制 / caption 路線 reject(W67 證全部 GT 已 in reach,唔需要)| **Accepted**(W68 2026-06-12 — 用戶拍板成功定義 = 全圖召回後,W67 closeout STOP+ask 提案 → AskUserQuestion「批准,实施」)| W66/W67 證據鏈;ADR-0040(superseded 該項);BUG-026/031 frontend dedup 對齊 |
| [0055](./0055-inline-image-markers-b2-dual-field.md) | Inline image markers — B2 雙欄位 + per-KB 開關 + 交織顯示(逐字逐圖跟原文順序)— chunker 平行 marked 文字流寫 `[IMG@doc_order]` 佔位 → orchestrator 用現有 `position_to_sha` 改寫 `[IMG#sha8]`(全域唯一 = frontend dedup key);新 index 欄位 `chunk_text_marked`(`searchable:false`,**`chunk_text` bit-identical 檢索層零污染**)只供 synthesis prompt;marked 變體穿三條 dispatch 路(parent / expander / raw)+ per-KB 旋鈕 `enable_inline_image_markers`(ADR-0040/0050 四層解析,全域 False);**現存 index 要 PUT 遷移加欄位先可 re-ingest**(`to_search_doc` 全欄位上傳);驗證先行兩段式:W70 基建 + strip + placement 驗證集(錯位率 ≤3% gate)→ W71 交織 render(mockup `AnswerBody` reverse-drift per H7;anchored 圖唔重複喺 strip,gallery 全量;標記對 ADR-0054 cap 後 surviving 集驗證);aux 圖(post-hoc expansion 收割)結構上留 gallery;唔達標退守方案 A 段落級錨定;amends `architecture.md §3.3/§3.5/§3.6/§3.7` | **Accepted**(雙 session 合併稿 2026-06-12 Chris 拍板「Accept 但先只 commit 文件」;F2+ 實作留後續 session)| 用戶 2026-06-12「無限接近地跟隨原文件內的文字和圖片的顯示順序」;雙 session 合併:B2 機制 + 驗證先行(markers 稿)⊕ mockup reverse-drift 考證 + cap 互動 + 顯示語意(anchors 稿,已併入);W70 plan `docs/01-planning/W70-inline-image-markers/` |
| [0056](./0056-document-profiling-adaptive-recall.md) | 文件畫像驅動自適應 recall 流程 — rule-based profiling(`P1-P5 + FORM` taxonomy）+ profile→preset routing,LLM 退選用保險(H4 label-only);ingestion 抽結構信號(heading 深度 / 圖密度 / list 比例 / table 密度 / **每頁文字密度** / **PDF text-layer 空比例** / **tick-box marker**）判 profile + 信心度 → 自動套 preset 落 per-doc config(ADR-0050）→ query 時 per-doc overlay(W57)決定 image 錨定策略;三層 UI 可調(L1 自動 / L2 一鍵 override / L3 逐 knob + 試跑);接駁 ADR-0040/0050 + W69 preset(延伸非重寫);三輪實證(33 份真文件 v1 70% → v2 90% → v3 預估 95%,內容驗證 ground truth 全中,3 miss 全 rule 可救);P4 scan 偵測已驗 7/7(`Open` 已閉合),xlsx parser = future H2;觸 H1(profiler+routing)/ H4(LLM 邊界)/ H7(新 UI 無 mockup)/ H2(xlsx) | **Accepted**(用戶 2026-06-13 session 確認 — 批准範圍 = 層 A 文件畫像 + 路由;落地按 rolling JIT 分 phase,層 A 內部拆 Profiler engine → render-side → 三層 UI;**§Amendment W98/W99**:leaf 級 `section_anchor_nearest` 圖步驟錨點 + 融入 P1_sop_imgdense 自動配對 + 前端 UI)| W71 末尾堆 tension + 用戶 2026-06-13「唔可以只服務 docx SOP,先判類型再選流程」+ foundational vision(memory `project_per_kb_tunable_config_vision`);勘查 `scripts/explore_doc_profiling*.py` + `explore_doc_content.py`;sample `docs/06-reference/01-sample-doc/` |
| [0057](./0057-pdf-picture-ingestion-per-kb.md) | PDF picture ingestion — per-KB `generate_picture_images`(thread 現有 `extract_embedded_images`)+ 按需 re-index — born-digital PDF embedded picture 入庫缺口(ADR-0056 D8 坐實 0→10/10):thread per-KB image toggle 落 `DoclingPdfParser` + `select_parser`,只 `extract_embedded_images=True` 的 KB 對 PDF 抽圖(text-only 零成本);現有 PDF doc 按需手動 re-index(W46)補圖;production-preserve(`extract_embedded_images=False` bit-identical);成本 +19% parse + scan `std::bad_alloc` robustness 待處理;scan P4 OCR + 方案 A 不在 scope;觸 H1(ingestion behavior + storage layout)| **Accepted**(用戶 2026-06-14 session 確認 per-KB + 按需 re-index + 接受 +19% 成本)| ADR-0056 D8 前置依賴(PDF 圖類 W73 preset 空轉 + ADR-0055 marker 對 PDF 失效);grounding `explore_pdf_picture_flag.py` |
| [0058](./0058-profile-manual-override-write-surface.md) | Profile 人手覆寫 write surface — L3 override `select` → 套 `preset_for` 落 per-doc config(reuse ADR-0050)+ `DocProfileInfo` 加 `manual_override` annotation(保 W76 read-only system fact 分層 + 可解釋);UI effective = `manual_override ?? profile`;D6 守 reuse W73(re-ingest preserve override + skip-if-manual config);scope = ① override only(② threshold persist 不做 — 五輪實證已最佳)；觸 H1(新 endpoint + schema)| **Accepted**(用戶 2026-06-15 confirm ① override only)| W78 L3 override select static → 本 ADR wire functional;前置 ADR-0056 D4/D6 + ADR-0050 + W76 |
| [0060](./0060-l3-image-anchor-knobs-ui.md) | L3 per-doc config UI 暴露 inline marker + section 錨定旋鈕(design-stage expansion)— 缺口 A:backend `DocConfig` 支援 `enable_inline_image_markers`(W70)+ `enable_section_anchored_aux_images` + `section_anchor_max_per_anchor`(W75)per-doc 覆寫,但 L3 `doc-config-tab` UI 從未暴露;section 錨定 mockup + KB 層 frontend 都冇(W75 比 mockup 晚)→ 觸 H7;用戶 STOP+ask 後揀**方案 A**(復用既有 `DocTuneGroup`/`DocSwitchKnob`/`DocTuneKnob` mockup-aligned component 延伸,視覺零發明)+ `DOC_TUNE_KNOB_KEYS` 加 key 自動納入 plumbing;**backend 零改動**(`DocConfig` + `PUT /config` 已支援)；觸 H7(design-stage expansion)+ H1(UI surface 擴展)| **Accepted**(用戶 2026-06-15 揀方案 A)| 層 A UI 可調性 gap 分析缺口 A;前置 ADR-0056 段③ + ADR-0050 + ADR-0055 + ADR-0040 |
| [0061](./0061-doc-detail-content-width-align.md) | Doc-detail 頁面寬度對齊全站 — `content content-wide`(1600px 置中)→ `content`(貼邊)— 用戶報告 per-doc 配置頁「縮到中間」;根因 = doc-detail 是全站唯一用 `.content-wide`(max-width 1600px 置中)嘅頁,其餘一律純 `.content`(無 max-width 貼邊);implementation 本 100% 對齊 mockup `ekp-page-doc-detail.jsx:27`(content content-wide = mockup deliberate design,非 bug)→ 改貼邊 = 偏離 mockup 觸 H7 + H1-adjacent(layout philosophy);移除 `content-wide`(保留 `paddingTop/paddingBottom:16` surgical)+ **同步更新 mockup** 維持 single source of truth;純 frontend backend 零改動 | **Accepted**(用戶 2026-06-15 AskUserQuestion 揀「改貼邊 + 同步 mockup」)| 用戶報告 UI 縮中間;前置 ADR-0029(3-pane layout)+ ADR-0024(unified shell 一致性) |
| [0062](./0062-app-content-max-width.md) | 全 app `.content` responsive 最大寬度 — 超寬螢幕內容居中限 1600px(padding-clamp)— ADR-0061 修 doc-detail 縮中間時誤解方向(改成無上限貼邊),用戶在 `/kb` 發現「貼邊撐滿」本身才是問題(超寬螢幕內容拉到最右 + grid 多列 + 無 responsive);實測根因 = 全站所有頁面用純 `.content`(無 max-width)→ ultrawide 無限撐寬,doc-detail 原 content-wide 反而是唯一有上限的頁;用戶 STOP+ask 揀「全 app 加最大寬度 + 居中」→ `.content` 左右 padding 改 `max(28px, (100% - 1600px) / 2)`(top 24/bottom 40 不變)使超寬螢幕內容區 1600px 居中、窄螢幕撐滿;**用 padding-clamp 而非 max-width+margin:auto**(`.content` 是 flex item,auto margin 破壞 flex-grow 縮到 1008px);chat 用獨立 3-pane grid 不受影響;同步改 canonical `styles.css` + copy `styles-mockup.css`;觸 H1(全 app layout philosophy)+ H7(偏離所有 mockup 純 .content)| **Reverted**(同日 — 走錯方向:沒先對照 mockup,mockup `.content` 本來就撐滿;用戶第三輪指出居中偏離 mockup → `.content` 改回原本撐滿)| amends-then-reverted;前置 ADR-0024 + DESIGN_SYSTEM.md §7.2 |
| [0059](./0059-profile-only-backfill.md) | Profile-only backfill — 對現有 doc 補跑 profiler(不重 chunk / embed / upsert)— profiler 由 W73 先接入 ingest → W73 前 ingest 嘅現有 doc 冇 profile(read surface null);新 endpoint `POST /kb/{kb_id}/profiles/backfill` 復用 `run_kb_reindex` loop 輕量版:download source(W46 blob)→ re-parse → `_PROFILER.profile` → persist(D6 守 preserve `manual_override`)+ route preset,**拔掉** chunk / embed / upsert → 零 retrieval 影響;idempotent(skip 已有 profile)+ skipped_no_source(W46 前 ingest 冇 source)；觸 H1(新 endpoint)| **Accepted**(用戶 2026-06-15 confirm (b) 輕量 backfill)| ADR-0056 層 A 段③ — 令現有 doc 有 profile;前置 ADR-0043(W46 source blob)+ ADR-0056 + ADR-0058 |
| [0063](./0063-settings-preset-mapping-write-surface.md) | Settings 全域 profile→preset 映射 write surface(缺口 B,B 收窄版)— Settings「文件分類規則」tab 由唯讀硬編碼鏡像 → 可寫:新 global persist store `preset_override_store.py`(mirror `doc_profile_store.py`,table `profile_preset_overrides(profile PRIMARY KEY, config JSONB)`)+ async `resolve_preset(profile, store)` overlay(出廠值 `PROFILE_PRESETS` 不動,「還原預設」= 刪 override)+ GET/PUT/DELETE `/profile-presets` + 三 call site(`_route_profile_preset` / `PUT …/profile` / backfill)migrate `preset_for`→`resolve_preset`(無 override bit-identical production-preserve);生效 future-only(改映射唔回溯,現有要 re-index);frontend `settings-doc-profiling.tsx` fetch + 解 disabled 編輯(**復用 Settings 既有 form primitive 視覺零發明**)+ guard 還原預設+確認;**threshold persist 維持不做**(W79 ADR-0058 拍板);觸 H1(config-routing 新 write surface + persist table)+ H7(edit-form design-stage expansion)| **Accepted**(用戶 2026-06-16 兩輪 AskUserQuestion 揀 B 收窄版 + 編輯 UI 復用既有 form + guard 還原預設+確認)| W81 缺口 B / `DEFERRED_REGISTER` DD-11;前置 ADR-0056 + ADR-0050 + ADR-0058 + ADR-0060 |
| [0064](./0064-trailing-image-section-grouping.md) | 末尾無錨圖 gallery 章節分組 — chat 末尾 `trailingImages`(無 inline marker 圖)由一排無分組大卡 → 按 `source_section` 分組 + 章節小標;新純函數 `groupTrailingBySection`(`citation-images.ts`)+ chat render 外層 map(章節 header 復用 ImageGallery `muted mono` primitive 視覺零發明);純前端呈現層零 backend / retrieval 改動,production-preserve(trailing 空 bit-identical);**層 C「圖片按相關性揀」三 query live 診斷實證無效 confirm defer**(召回圖全相關無圖可砍 + 砍圖反噬 W59-W68 全召回 + 文字-relevance 已 ADR-0046 Decision#3 revert + per-image metadata 早 populate)+ **A 調 `max_aux` 補召回乾淨 A/B 否決**(40 vs 80 召回都 40,候選池天花板非 cap);真因 = 圖粒度>文字步驟粒度 anchoring 失配(FA chunk 12 步綁 47 圖);觸 H1(image presentation documented behavior)+ H7(mockup 無分組 gallery design-stage expansion)| **Accepted**(用戶 2026-06-16 AskUserQuestion 揀方案 1 復用既有 gallery primitive)| ADR-0056 vision 層 C / W83-trailing-gallery-grouping / `DEFERRED_REGISTER` DD-12;前置 ADR-0055 + ADR-0046 + ADR-0060 |
| [0065](./0065-p4-scan-ingest-guard.md) | P4 掃描件 ingest 預檢 guard + force override — ingest 同步(Tier 1 no background job),scan PDF Docling OCR 同步 hang request 8-9.5 分鐘 = 壞體驗;`is_scan_pdf(path)` helper(抽自 profiler pypdfium2 pre-OCR probe,秒級不 OCR)→ `_run_ingest_pipeline` 喺 `parser.parse()` 前 `.pdf` 判 P4 + 非 `force_scan` → 422 `ingest.scan_requires_confirm`;POST upload/reindex 加 `force_scan` query 參數;frontend `uploadDoc` 帶 force + 接 422 → 警告 banner(`banner-warning` 復用視覺零發明)+ 「仍要繼續」按鈕 → force 重試;production-preserve(只對 .pdf 跑 probe,born-digital 照常,docx/pptx 跳過);用戶揀預檢警告+force(非硬 reject 保留自調 / 非 async force 同步知情等待);觸 H1(ingest 流程)+ H7(upload 警告 design-stage expansion)| **Accepted**(用戶 2026-06-16 連續 AskUserQuestion 擱置層C→評估層A/B→揀 P4 guard→揀預檢警告+force)| 層 A scan DD-9 收窄 / W84-p4-scan-ingest-guard;前置 ADR-0056 + ADR-0019 + ADR-0057 |
| [0066](./0066-enterprise-retrieval-layer-document-acl.md) | Enterprise retrieval-layer document ACL — P1 目標授權架構(`allowed_principals` Collection + `classification` 索引擴欄位 + 檢索 filter security trimming + KB 繼承→文件級表 + P2.0-P2.3 分段)| **Accepted**(用戶 2026-06-24 拍板 DG5 → P2 解鎖)| W89-rbac-p1-threat-model-arch F3;接 F1 威脅模型 G1-G5 + F2 目標模型 |
| [0067](./0067-document-level-acl-override-and-group-inheritance.md) | Document-level ACL override + group inheritance — P3 加文件級 `doc_acl` override 表(replace 語義)+ P4 群組繼承(復用 `group_members`,query 側 `principals_for_user` 展開,group member 改動零 re-stamp)| **Accepted**(用戶 2026-06-24 拍板 → P3-impl 解鎖,Tier 1.5 等真實 doc-level driver)| W91-rbac-p3-doc-acl-design F3;接 ADR-0066 P2;沿用 `allowed_principals` 零新索引欄位 |
| [0068](./0068-admin-tiering-and-access-governance.md) | Admin tiering + access governance — 加 auditor 唯讀稽核角色(G8 職責分立)+ access-review report + re-certify(G9 存取覆核);零檢索改動 north-star §15 by-construction no-op + 現有 4 role byte-identical | **Accepted**(用戶 2026-06-25 拍板;**P5 impl 2026-06-25 暫緩,F1 已還原** — 治理功能等真實審計 driver,Tier 1.5)| W94-rbac-p5-governance-design F3;接 ADR-0027 |
| [0069](./0069-coverage-oriented-synthesis-variant-branch.md) | Coverage-oriented synthesis 條件式變體/分支還原(乙類緩解)— additive coverage rule 閘新 config knob default OFF,enumerate 每變體分支 + DEDUP;驗收靠 W96 gate paired A/B | **Accepted → 實作後 code REVERT**(2026-06-26;兩輪 W96-gate A/B 反證 mean delta ≈0 + C005 反退,乙類 gap stochastic 非 systematic,fixed-answer 精準路 blocked on `gpt-5.5` H2;§Outcome 記「試過點解唔得」,knob 未採用)| W97-completeness-mitigation;接 W96 完整度 gate + DD-15 hardening |

> §13.10 ("Other v3 inherits") 屬 meta-rollup,不單獨 promote。
> ADR-0013 reserved for AF3 lifespan gate split fix(W11 D5 retro carry-over #1;defer per R5)。
> ADR-0017 **landed 2026-05-10**(R8 corp-proxy mitigation pattern — formalized at the 5th cumulative occurrence:Cohere W3 + argon2-cffi W13 + ACS SDK W13 + Playwright browser CDN W15 D5 + psycopg W17 F1;vendor-decision pivot point also met)。
> ADR-0024 **Accepted 2026-05-10**(unified application shell IA — amends ADR-0015;Chris answered Q1-Q5 then directed the post-acceptance cascade — `W18-app-shell-ia` phase kicked off(`plan.md` per §10 R1)+ `architecture.md v6 §5` amended(§5.0 Application Shell added / §5.3「Admin Dashboard」→「Dashboard」/ §5.9 V7 Landing removed / §5.7「Debug View」→「Traces」/ `/admin/*` → `/kb/*` flatten — inline-tagged per the §3.4/§3.7 precedent, doc version held)+ ADR-0015 References get the amended-by note)。

## Adding a new ADR

Per `CLAUDE.md` §6 — 任何違反 §5.1 H1 / §5.2 H2 嘅變動必須 ADR(approval 後)。Format:

```markdown
# ADR-NNNN: <Title>

**Date**: YYYY-MM-DD
**Status**: Proposed | Accepted | Superseded by ADR-MMMM
**Approver**: <Name>

## Context
## Decision
## Alternatives Considered
## Consequences
## References
```

**Next NNNN**:`0013` reserved for AF3 fix(W11 D5 retro);`0017` **landed 2026-05-10**(R8 corp-proxy mitigation pattern — 5th cumulative occurrence + vendor-decision pivot trigger met);`0018`+`0019`+`0020` landed(P0 batch:Multi-KB kb_id propagation + PDF parser + Context Expander — **P0 batch complete from `audit-W15-d5-vs-spec.md` §6**);`0021` landed 2026-05-10(V4 Retrieval Testing tab §5.5.4 deliver + `HybridSearcher` search-mode param;audit 偏差 #5 / §7 P2.1)— **all 5 audit major drifts now closed**;`0022`+`0023` landed 2026-05-10(W17-beta-hardening phase — auth-transport hardening cookie+CSRF / KB Manager persistent backing Postgres;the two future-ADR candidates from `audit-W15-d5-vs-spec.md` §7 now consumed);`0024` **Accepted 2026-05-10**(unified application shell IA — amends ADR-0015;`W18-app-shell-ia` phase kicked off + `architecture.md v6 §5` amended at acceptance);`0025`+`0026`+`0027`+`0028`+`0029`+`0031` **Accepted 2026-05-16**(W19-frontend-audit-and-adr-draft F6 Chris approval — KB Detail 8-tab / Settings 6-tab fully editable per Option B / `/users` Tier 1.5 full RBAC per Option A / `/kb/new` 5-step / `/doc-detail` 3-pane `/kb/[id]/docs/[docId]` per Option C / Chat advanced server-side Conversation History per Option B;**Wave C MUST split into C1+C2** per F4 §3.6 trigger due to Option A+B picks ~42 backend days exceeds single phase budget);`0030` SKIPPED(W19 F2 §6 — Dashboard richer + Trace 3 viz + /traces list folded into Wave A scope without separate ADR);`0032` SKIPPED(Topbar + Sidebar additive folded into Wave A polish);**`0033` Accepted 2026-05-23**(W25-image-association-deep-fix F1 deliverable — chunker low-value tuning);`0034` **Accepted 2026-05-24**(W25 F3 query expansion + RAG-Fusion);`0035` **Accepted 2026-05-24**(W25 F5 D2 retrieval low-value soft-relax — H1 boundary triggered per R6 D0 finding (iii) confirmed by Chris Path B AskUserQuestion 2-step pick;CH-003 co-shipped);`0036` **Accepted 2026-05-24**(BUG-021 W25 D2 cont — react-markdown frontend dep);**`0037` Accepted 2026-05-25**(W26 F2 PIVOTED parent-document / section-level retrieval — Tier 1 ceiling for enumeration queries;F1 empirical refutation of brief §3 方向 A rerank threshold premise triggered Chris pivot pick (C) per brief §6 step 4;Chris AskUserQuestion approved Q4 Default OFF + Q1 top_k=1 + Q2 depth=1 + Q6 Both on Recommended picks + Q3/Q5/Q7/Q8 proposed defaults batch-locked;W26 F2 implementation contract = ADR §Implementation Deliverables);**`0038` Accepted 2026-05-25**(W27 F3 closeout — parent-doc dispatch chain append-mode finding;Chris α pick PARTIAL closeout per Q4 measurement-experiment-fail-policy + G1+G4 marginal MISS < 1pp;Settings default preserve "replace";W28+ candidates (b) `max_tokens_per_parent` sweep + (c) RAGAs orchestrator-aware tune elevated;D1.35 H1 + H4 validated + H2 partially confirmed + H3 refuted by Q-W25-I07 critical recovery 0.00 → PASS empirical signal);**`0039` Proposed 2026-05-29**(W42-hybrid-semantic-ranker-toggle F0 — hybrid mode semantic ranker configurable layer;Option ① drop semantic ranker config flag to enable Free-tier full-pipeline + chat UI test;H1-adjacent pending Chris confirm + Accept before F1 retrieval code);**`0040` Accepted 2026-06-01**(W43-per-kb-tunable-retrieval-config F0 0.5 gate — per-KB tunable retrieval/citation config-scope model;Chris confirm H1 + scope;validated W43 closeout STRONG PASS 7/7);**`0041` Accepted 2026-06-03**(W44-chunker-image-density-deep-fix F0 gate PASS — chunker image-density deep fix,圖片隨 doc_order 分配 + max_images_per_chunk cap;ADR-0040 R6 catch 5 split-out;Chris 揀**切法 D 混合** + cap **8** + Accept);**`0042` Accepted 2026-06-04**(W45-per-kb-image-cap kickoff — per-KB ingest-time chunker image cap,延伸 ADR-0040 config-scope 到 ingest-time + ADR-0041 全域 cap 到 per-KB;`KbConfig.chunker_max_images_per_chunk`,None=繼承/正整數=cap;Chris AskUserQuestion 批准 H1 + ADR-0042);**`0043` Accepted 2026-06-04**(W46-ingestion-config-and-reindex kickoff — 原始檔 blob 儲存 + 真 KB-level reindex;unblock UI 自助 ingestion-config loop,根因 = 原始檔冇存非 Track A;Chris 揀「原始檔儲存 + UI 一次過」批准 H1 backend 方向;**H7 frontend mockup unlock = 獨立 sub-gate** frontend code 前另需確認;backend code gate open);**`0044` Accepted 2026-06-06**(W53-chunk-strategy-recall-comparison F1 — heading_aware chunk strategy section-bounded + chunk_strategy ingest wiring;R6 grep 揭 chunk_strategy degenerate + `_select_chunker` ignore strategy + 跨 reindex chunk_id 變;接 chunker lineage 0041/0042/0043;Chris AskUserQuestion 揀比較軸=實作 heading_aware 真 strategy + Recall=per-config 重生 QA;順帶 close W46 reindex over-promise gap);**`0045` Proposed 2026-06-08**(CH-008-contextual-retrieval — section-context injection for rerank + embedding;接 BUG-034 問題1 診斷,74-chunk Cohere rerank A/B 實驗 PASS 證 off-topic GL05/GL02 清走 + GL03-Create 升頂;Chris 批准 H1 + 全套 rerank+embedding scope;rerank-only 零 re-index、embedding 端要 re-index;stored chunk_text 不變;**Accepted 2026-06-08** Chris;V5 文字維度 PASS + ff-merge `34c5d7c`);**`0046` Proposed 2026-06-08**(CH-009-chat-image-relevance — 接 BUG-034 問題1 圖片維度,CH-008 文字已 merged;decorative dims-probe filter + per-KB cap wire + relevance-select 排圖;**H4 邊界:文字信號 only,無 image embedding**;Chris 揀「一個 CH 三項一齊」+ OD-1/2/3 鎖定;code gate open pending Accept);`0047`-`0056` landed(見 index table);**`0057` Accepted 2026-06-14**(ADR-0056 D8 PDF picture ingestion 前置 — per-KB `generate_picture_images` thread `extract_embedded_images` + 按需 re-index;用戶 session 確認 per-KB + 接受 +19% 成本);**`0058` Accepted 2026-06-15**(W79-profile-override-write — ADR-0056 D4/D6 profile 人手覆寫 write surface;用戶 confirm scope = ① override only(② threshold persist 不做);override → 套 `preset_for` 落 per-doc config(reuse ADR-0050)+ `DocProfileInfo` 加 `manual_override` annotation 保 W76 read-only system fact 分層 + 可解釋;D6 守 reuse W73);**`0059` Accepted 2026-06-15**(W80-profile-backfill — profile-only backfill,對現有 doc 補跑 profiler;W73 前 ingest 嘅現有 doc 冇 profile → read surface null;`POST /kb/{kb_id}/profiles/backfill` 復用 `run_kb_reindex` loop 輕量版,download source(W46 blob)→ re-parse → `_PROFILER.profile` → persist + route,**拔掉** chunk/embed/upsert → 零 retrieval 影響;idempotent skip 已有 profile + skipped_no_source(W46 前 ingest);用戶 confirm (b) 輕量 backfill 非 (a) 完整 re-ingest)；**`0060` Accepted 2026-06-15**(W81-l3-image-anchor-knobs — 缺口 A:L3 per-doc config UI 暴露 inline marker + section 錨定三旋鈕;mockup 之外 design-stage expansion 用戶揀方案 A 復用既有 component;backend 零改動)；**`0061` Accepted 2026-06-15**(doc-detail 頁面寬度對齊全站 — 用戶報告 per-doc 配置頁「縮到中間」;根因 = doc-detail 全站唯一用 `.content-wide`(max-width 1600px 置中),其餘一律純 `.content` 貼邊;implementation 本 100% 對齊 mockup `ekp-page-doc-detail.jsx:27`(content content-wide = deliberate design 非 bug)→ 改貼邊偏離 mockup 觸 H7 + H1-adjacent layout philosophy;用戶 AskUserQuestion 揀「改貼邊 + 同步 mockup」;移除 `content-wide`(保留 paddingTop/paddingBottom:16 surgical)+ 同步更新 mockup 維持 single source of truth;純 frontend backend 零改動)；**`0062` Reverted 2026-06-15**(走錯方向、同日已 revert — 沒先對照 mockup 就把全 app `.content` 改成 1600px 居中,偏離 mockup 原本撐滿;全 app `.content` responsive 最大寬度 — ADR-0061 修 doc-detail 縮中間誤解方向改成無上限貼邊,用戶在 `/kb` 發現「貼邊撐滿」才係真問題;根因 = 全站純 `.content` 無 max-width → ultrawide 無限撐寬;用戶揀「全 app 加最大寬度 + 居中」→ `.content` 左右 padding 改 `max(28px, (100% - 1600px) / 2)` 使超寬螢幕內容區 1600px 居中、窄螢幕撐滿,用 padding-clamp 因 `.content` 係 flex item auto-margin 破壞 flex-grow;chat 獨立 grid 不受影響;同步 canonical styles.css + copy styles-mockup.css;amends ADR-0061 方向)；`0063` **Accepted 2026-06-16**(W82-settings-preset-write — Settings 全域 profile→preset 映射 write surface,缺口 B 收窄版:新 global `preset_override_store` + `resolve_preset` overlay + GET/PUT/DELETE `/profile-presets` + 三 call site migrate + frontend 復用既有 form;threshold persist 維持不做 per ADR-0058;用戶 2026-06-16 兩輪 AskUserQuestion 揀 B 收窄版 + 編輯 UI 復用既有 form + guard 還原預設+確認);**`0064` Accepted 2026-06-16**(W83-trailing-gallery-grouping — 末尾無錨圖按 `source_section` 分組 + 章節小標;層 C「圖片按相關性揀」三 query live 診斷實證無效 confirm defer〔召回圖全相關無圖可砍 + 砍圖反噬 W59-W68 全召回 + 文字-relevance 已 ADR-0046 revert + per-image metadata 早 populate〕+ A 調 `max_aux` 補召回乾淨 A/B 否決〔40 vs 80 召回都 40,候選池天花板非 cap〕;真因 = 圖粒度>文字步驟粒度 anchoring 失配〔FA chunk 12 步綁 47 圖〕;純前端呈現層復用既有 ImageGallery primitive 視覺零發明 H7 方案 1;用戶 AskUserQuestion 揀方案 1 + 開 phase);**`0065` Accepted 2026-06-16**(W84-p4-scan-ingest-guard — P4 掃描件 ingest 預檢 guard + force override;ingest 同步〔Tier 1 no background job〕scan PDF Docling OCR hang request 8-9.5 分鐘 = 壞體驗 → `is_scan_pdf` helper〔抽自 profiler pypdfium2 pre-OCR probe,秒級不 OCR〕喺 `parser.parse()` 前 `.pdf` 判 P4 + 非 `force_scan` → 422 `ingest.scan_requires_confirm`,POST upload/reindex 加 `force_scan` query 參數,frontend `uploadDoc` 帶 force + 接 422 → 警告 banner〔`banner-warning` 復用視覺零發明〕+ 「仍要繼續」force 重試;production-preserve〔只對 .pdf probe,born-digital 照常,docx/pptx 跳過〕;用戶連續 AskUserQuestion 擱置層C→評估層A/B→揀 P4 guard→揀預檢警告+force〔非硬 reject 保留自調 / 非 async force 同步知情等待〕;觸 H1 ingest 流程 + H7 upload 警告 design-stage expansion;DD-9 收窄至只剩 OCR 慢 Tier 2);**`0066` Accepted 2026-06-24**(W89-rbac-p1-threat-model-arch F3 — enterprise retrieval-layer document ACL;P1 目標授權架構拍板,接 F1 威脅模型 G1-G5〔query 端點無守衛 + 檢索層無文件 filter + 索引無 ACL 欄位 + 合成層 confused deputy 洩漏 + KB ACL 存在但 query 未用〕+ F2 目標模型〔選項 A `allowed_principals` Collection + `classification` 索引擴欄位 + 檢索 filter security trimming + 5.1 KB 繼承→5.2 文件級表 + P2.0-P2.3 分段〕;DG resolution 用戶 2026-06-24 拍板 DG1 2 級分類 / DG2 單租戶 / DG4 P2=Tier 1 上線先決;**Status Accepted 2026-06-24 = 用戶 session 行使 decision owner 拍板,DG5 resolved → P2 implementation 解鎖**〔次序鐵律 5 satisfied〕);**`0067` Accepted 2026-06-24**(W91-rbac-p3-doc-acl-design F3 — document-level ACL override + group inheritance;接 ADR-0066 P2〔5.1 KB 繼承〕→ P3 加 5.2 文件級 `doc_acl` override 表 + P4 群組繼承;F1 補 G6〔同 KB 內無法分文件權限〕+ G7〔group grant 無法 resolve 到 member〕;DG resolution 用戶 2026-06-24 拍板 **DG-P3-A replace 語義**〔doc 有 doc_acl → 用之,無 → 繼承 KB〕/ **DG-P3-B 手動 admin group member**〔復用 `group_members` 表,真 SCIM = Tier 2〕/ **DG-P3-C Tier 1.5 post-launch**〔P2 已達 launch 安全,impl 等真實 doc-level driver〕;沿用 P2 `allowed_principals` Collection **零新索引欄位/無重建**;關鍵洞察 chunk 存 group key 非 member oid → **group member 改動零 re-stamp**〔query 側 `principals_for_user` 展開〕;觸 H1 + H4;**Status Accepted 2026-06-24 = 用戶 session 行使 decision owner 拍板 → P3-impl 解鎖**〔次序鐵律 5;Tier 1.5 = impl 等真實 doc-level driver〕);**`0068` Proposed 2026-06-25**(W94-rbac-p5-governance-design F3 — admin tiering + access governance;接 ADR-0027 RBAC 之上加 **auditor 唯讀稽核角色**〔G8 職責分立 — admin 單層全權 = 被監察者兼監察者〕+ **access-review report + re-certify**〔G9 存取覆核 — 權限永久無 recert + privilege creep / 離職遺留〕;範圍用戶 2026-06-25 拍板**核心 + 存取覆核**〔JIT / break-glass / 自動回收延後 Tier 2〕;DG resolution **DG-P5-A 加 `RoleKey` 第 5 值** / **DG-P5-B 只加 auditor**〔super-admin / KB owner push-back 成立 — `kb_acl.manage` 已近似 owner + super-admin 無 driver speculative,留後期〕/ **DG-P5-C 報告 + 覆核標記**〔`access_reviews` 表 + audit action `access.reviewed`,無審批 workflow〕/ **DG-P5-D Tier 1.5 暫不 implement**;**零檢索改動 north-star §15 by construction no-op + additive 現有 4 role byte-identical**;觸 H1〔role model 演進 92→115 cell〕+ H4〔治理邊界〕;**Status Accepted 2026-06-25 = 用戶 session 行使 decision owner 拍板 → P5-impl 解鎖**〔次序鐵律 5;依 DG-P5-D Tier 1.5 = 暫不 implement 等用戶另批〕);**`0069` Accepted 2026-06-25**(W97-completeness-mitigation — coverage-oriented synthesis 條件式變體/分支還原,乙類緩解;接 W96 完整度 gate〔`4bca795`〕+ DD-15 hardening〔`2102eed`〕;精準根因 = 現有 `SYSTEM_PROMPT_DETAILED` 捉主線步驟但掉**條件式變體/替代分支/scenario 子流程**〔W96 F8 C003 掉 9 nugget 全屬此類 + GL案掉 RMS/RSP 變體+Overview〕;Decision = additive coverage rule **閘新 config knob default OFF**〔mirror `_MARKER_RULE` Rule 9 append,production-preserve〕+ enumerate 每變體分支 + DEDUP 校準,**驗收靠 W96 gate paired A/B**〔研究反證「prompt 消除位置偏置」0-3 → 效果未保證必須 A/B〕;alternatives reject = 就地改 detailed〔無 A/B 安全網〕/ RL inserter fine-tune〔H4〕/ extractive-then-abstractive〔研究反證 1-2〕;risk = length-bias + synth-timeout〔DD-7/ADR-0053〕+ 過度 extractive + 效果<gate ±0.15 解析度則先做 DD-15 fixed-answer;default flip = 另一決定〔類 ADR-0052〕;觸 H1 §3 RAG core synthesis;**Status Accepted 2026-06-25 → 實作 + 兩輪 W96-gate A/B 反證 → code REVERT(2026-06-26),knob 未採用**〔§5.1 H1 + §10 R1;F1-F4 落地後 A/B:iter-1 mean delta 0.000(C005 反退 −0.17)/ iter-2 精修後 −0.031(C005 −0.16,targets 入噪聲);乙類 gap 實證 stochastic 非 systematic,fixed-answer 精準路 blocked on `gpt-5.5`(H2 拒 temp≠1);用戶 decision owner 揀 revert + 保留 ADR 記錄;3 實驗 commit 留 history,本 ADR §Outcome 記「試過點解唔得」;default flip 永不發生〕);**next available NNNN = `0070`**;only `0013` AF3 remains reserved without W-phase context;`0030` + `0032` deliberately skipped per W19 F2 absorb-vs-promote decision。
